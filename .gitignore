# 依赖与构建产物
node_modules/

# 运行时状态
state.json
mesh/store/
scrum/activity.jsonl

# render.mjs 生成产物（可重生成）
scrum/board.json
scrum/KANBAN.md
scrum/kanban.html

# P1-1 第 2 步：v1 文件库退役归档（archive-v1-scrum.mjs 产物，含运行时产物；
# v1 任务数据仍可从 git 历史追溯，归档目录不入库以免 228KB 日志进仓）
scrum/archive/

# 士兵改动的统一 diff 补丁（运行时产物）
scrum/patches/

# 需求讨论群聊记录（运行时产物）
scrum/discussion/

# 守护 worktree 隔离目录
.legion-worktrees/
.worktrees/

# 守护/调解员运行时状态（进程心跳每轮改写，不入库）
scrum/daemon.json
scrum/daemon-mediator.json
scrum/control.json
team-hub/.watch.mjs
_link_deps.mjs
workbench/scripts/serve.mjs.bak

# 日志与系统文件
*.log
.DS_Store
Thumbs.db
/.legion-services.log

# T-093 目标级 CI / 发布运行时产物（可再生成，不入库）
.ci/
releases/
scrum/pending-goals.json
