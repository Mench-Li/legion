# 待裁决清单（一页纸）

> **这份文件只做一件事**：把 `MULTI-AGENT-FEATURE-STATUS.md` §5 里那 **29 条**裁决项，
> 压缩成"**你要说的那一句话**"，并按"不决定的后果"排序。
>
> 全量说明在 §5；这里是**决定用的那部分**。
>
> 生成于第 32 轮（2026-09-19）。读数：台账 **145 行 = 140 ✅ / 4 ⏸ / 1 ⬜**。

---

## 0. ★★★ 只有一条卡在**归属**上，而它一个人挡着五条

**一句话**：`product/process-manifest.mjs` 是**另一会话的在制品**（有未提交改动；
那一组在制品里最近一次改动是 `product/launcher/readiness.mjs`，**今天 08:00**）。
它里面有**一个数组**（`runtime` 进程的 `envNames`）缺 **5 把键**。

**这 5 把键一起通、一起不通。** 补那一个数组，下面五条同时解开：

| 键 | 它管什么 | 今天的后果（实测） | 其余环节 |
|---|---|---|---|
| `LEGION_PATH_SCOPE` | 这一 Run 的读根/写根 | 配了也到不了子进程 ⇒ 路径范围检查**从不运行** | 读取点 + 装配点 + 生产路径用例**都已就绪** |
| `LEGION_CONNECTOR_DECLARATIONS` | F-21 连接器声明 | 同上 ⇒ 连接器判定面**从不运行** | 同上（`root-row.test.mjs` 五条已验） |
| `LEGION_EXECUTION_SCOPE` | 命令/网络/MCP 授权 | 同上 | 同上（`production-scope-wiring.test.mjs` ①d/③d 已验） |
| `LEGION_EXTERNAL_API_SCOPE` | 外部 API 读/写范围 | 同上 | 同上（含真实 `preExecute` 的拒与放） |
| `TEAM_HUB_TOKEN` | 早于本轮、**无归属**的一处 | 根 `root.mjs` 会读它，只是它是**可选**的 ⇒ 今天不致命，但"配了也传不到"一模一样 | —— |

★ **为什么它今天不报错**：这 4 把键在 `runtime/config-schema.mjs` 里**都注册了**
（用户配得进去、schema 校验通过），4 个装配点 `*PortFromEnv()` 在 `root-row.mjs` 里**都接好了**——
于是"能配"与"接好了"两处各自的判据**都是绿的**，而中间那根线不存在。

★ **我要您说的一句话**（二选一）：

- **甲**：「**那个文件我可以动**」⇒ 我把这 5 把键加进那一个数组（5 行），并更新
  `scripts/config/config.test.mjs` 的 `NOT_FORWARDED_YET`（5 条理由删掉）。
  **工时约半小时**，之后四道范围检查 + 连接器判定在真实部署里开始生效。
- **乙**：「**等那个会话**」⇒ 我把它留成第 0 项，不动。上面五条继续停在原处。

★ 顺带说清：**这个判断不是我猜的**——仓库自己的账本就写着
（`scripts/config/config.test.mjs:768`）：*"最后一根线在 `product/process-manifest.mjs` 的 runtime
`envNames` —— 而那个文件是**另一会话的在制品**。"* 我沿用了那条记账。

---

## 1. 需要「产品 / 架构」裁决的（**定语义**，不定语义就会猜）

每条都是**二选一或三选一**，不是开放题。全量论证见 §5 对应条。

| # | 你要定的一句话 | 两个选项 | **不决定**的后果 |
|---|---|---|---|
| **27** | 岗位清单写的是**Legion 能力名**还是**执行面工具名** | ①Legion 名（那要加映射）②DSH 名（那要改清单写作规范） | 两个词汇表**结构上不相交** ⇒ `whitelist` 配了**仍然是全拒**。★ 而且它在生产装配里**今天根本没传值**（`surfaces.whitelist === false`，有用例钉着） |
| **28** | 那条**出站车道**的文件放哪个目录 | 甲：新登记一个键（**会落进第 0 条同一个数组**）／乙：收账侧住进 hub 进程、路径从 `dbFile` 同级派生／丙：照第 19 条的形状按 Run 交付（**唯一与已裁决逐字一致**，最贵） | PRT-610 的车道**已建、环已在真 SQLite 上走通**，而生产里没人调它 ⇒ `decisionSourceRecorded` **永远没有产出者**，发布门禁永远判否——**而它判否的理由读起来完全正确** |
| **24** | 政策门要不要从连接器声明里读「能力」 | ①读 ②不读（那就如实记成"连接器层只能更严") | MCP 公开名不在能力目录里 ⇒ **配了 `allow` 与没接上的最终判决相同** |
| **25** | 「这个岗位能调哪些 MCP 工具」由**哪张表**说了算 | ①`execution-scope.mjs` 的 `mcp` 段 ②F-21 的连接器登记表 | 两张表对**同一个工具**给出不同判决 |
| **26** | 外部 API 授权表要不要管 **scheme** | ①管 ②不管 | `checkExternalApi` 不看 scheme ⇒ `ftp://…` **会被放行** |
| **12** | F-19 Role Pack 的「七类版本」范围 | ①确认这七类 ②增删 | 版本固化的**边界**不明 ⇒ 复现性无从判断 |
| **10** | F-22 / F-24 记 🟡 还是 ⏸ | ①🟡（还没做完）②⏸（按客户需求，设计范围内已完成） | §9 说"按真实客户需求推进；当前落地范围**按设计已完成**"，而功能表记 🟡——**同一个事实被判了两次** |
| **6** | PRT-509 的 C1～C4 | 逐条确认 | 四条停在 🟡 |
| **21** | PRT-256 / PRT-910 要的**不是机器也不是凭据** | 你能提供"一个真实外部用户 / 一个真实用户项目"吗 | 两条 ⏸ 只能继续等 |
| **8** | F-23 / F-25 要不要做 | ①做 ②不做 | 不做则从路线图中明确移除，而不是一直挂着 |

## 2. 需要「项目主 / 项目方」裁决的

| # | 一句话 | 不决定的后果 |
|---|---|---|
| **17** | PRT-707 的**两份实现**（活的 `--wizard` 内联 / 死的 `first-run.mjs` 636 行）用**两个不同的**模型密钥引用名：`model/api-key`（端到端通）vs `legion/model/<id>`（**三段引用，`addressable:false`，运行时拿不到钥匙**） | ★★ **千万别**把 `first-run.mjs` 直接接上去：它**不报错**——向导报"模型已配置"而运行时没有钥匙。已做成读数（`wizard-wiring.test.mjs` 5 例，今天全绿） |
| **20** | Runtime 契约**服务端那一行**要不要进补丁层 | 消费侧（读回发布并注入 `LEGION_RUNTIME_URL`/`LEGION_RUNTIME_TOKEN`）**已接线**，而没有服务端 ⇒ 真实部署里**那份发布永远不会被写出来** |
| **16** | 阶段 9 产品动作的 **CLI 面**（PRT-903/904/905/908/909、PRT-712、PRT-707） | 模块 + 用例齐备，**没有 CLI 面** ⇒ 用户按不到 |
| **1 / 2 / 13 / 23** | 见 §5：PRT-011 分发形态、PRT-214 G1/G4、F-21 判定面接线资格、"命名空间认不出来"按教义拒 | 各自停在原处 |
| **19** | ✅ **已裁决（2026-09-18 业主裁定"选前者"）** —— 把执行面数据放进 `RunRequest`，**不**注入 `TEAM_HUB_TOKEN` | ★ 它把第 **13/14/15/18** 条从「待裁决」变成「**待施工（纯代码工作量）**」。**唯一剩下的施工缝就是第 0 条那个数组** |

## 3. 不是裁决——等日期 / 等环境（**您不需要做任何事**）

| 台账行 | 等什么 | 现状 |
|---|---|---|
| PRT-253 | 一次**真的自动执行** | 真额度**已花过且已验**（`dsh --profile headless` → exit=0、9.2s）；剩下是平台边界（Windows 上不会有自动执行） |
| PRT-009 | 一台**执行期外**的机器采样峰值资源 | 两个采样器**均已落地**，只差一次实跑读数 |
| PRT-256 | **真实外部用户** | 环境 |
| PRT-910 | **真实用户项目** | 环境 |
| PRT-316 | **日期闸门**：最早 **2026-09-24** | 评审闸门**已过**，纯等待 |

★ **PRT-009 顺带一条**：`supervisor.peakResource()` 算出来的峰值读数
**今天到不了磁盘**——`launcher.mjs:1261-1267` 建 `processes` 时只带 `key`/`pid`/`image`，
而 `buildRunRecord` 是**闭合映射**（不认识的字段**静默丢掉**）。
记录层已能带、能校验、能落盘、能读回 ⇒ **缺的是生产者的那一行**。
修法与第 0 条同理（在另一个会话的在制品文件里，1 行）。

---

## 4. 您点了头之后，我立刻能做的（按工时排）

| 优先级 | 做什么 | 工时 | 依赖 |
|---|---|---|---|
| 1 | 第 0 条：5 把键进那一个数组 + 更新 `NOT_FORWARDED_YET` | ~30 分钟 | **您说"那个文件我可以动"** |
| 2 | `peakResource` 那一行（生产者带上它） | ~20 分钟 | 同上 |
| 3 | 第 27 条定语义后：补 `whitelist` 的装配 + 词汇表 | 半天 | 第 27 条 |
| 4 | 第 28 条定路径后：接上那条出站车道 | 半天～一天 | 第 28 条 |
| 5 | 第 17 条定引用名后：删掉死的那份实现 | ~1 小时 | 第 17 条 |

## 5. 诚实边界（这一页**不**主张什么）

- 它**不**主张"其余都做完了"。它主张的是：**第 32 轮把目标文档独立核了一遍**，读数如下
  （探针都在 `scratch/`，可复跑）：
  - §4 正文的 **6 条**含"必须/不得/禁止"的规范性句子 → 5 条有落点，第 6 条是 `RunRequest.env`；
  - §4 的 **9 组**可枚举名目（**50 个** token：7 接口方法 / 6 事件类型 / 6 投递状态 /
    5 权限档 / 3 渠道 / 5 计划字段 / 7 Manifest 项 / 4 校验项 / 7 Launcher 动作）
    → 全部有落点（唯一"零命中"的 `catchup` 是拼写变体，实现用的是 `catch-up`）；
  - §7 的 **7 项故障注入 + 5 项安全覆盖** → 全部有真套件。
    ★ 其中"**审批超时**"我第一遍搜出**零命中**，第二遍才找到
    `team-hub/approval-ttl.mjs`（PRT-615，**40 例**）——**代码里说的是 TTL，不是超时**；
  - §1/§8 引的**路径逐个解得开**（含 §8 引的 **11 个仓外文件**，
    以及 **16 处** `file:line` 引用、合计 **22 个行号**——**全部在范围内，0 处越界**）；
  - `reachability` 的 **46 项**不可达，每一项都带分类（`by-design=13 / gap=25 / deliberate=8`）
    与一个裁决处编号。
- **这一轮我自己报过 4 次假发现**（worktree 副本把 `server.mjs` 撑成 38 个同名、
  两次"零命中"其实是拼写变体、一次是我的解析器不懂表格惯例）。
  ⇒ 所以上面每一条读数都注明**它可能错在哪**：关键词零命中**不等于**没实现。
- 它**不**替任何一条做决定。上面每一格的"两个选项"我都没有倾向性结论——
  它们各自都自洽，而"猜一个两边都同意的"正是 PRT-253 §3 禁止的"发明默认值"。
- 第 0 条那个判断**依赖一个外部事实**（"那个文件属于另一个会话"）。
  如果那个会话其实**已经结束**，那么第 0 条不是裁决项，而是**我该直接做掉的一件事**。
