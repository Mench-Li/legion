# 待裁决清单（一页纸）

> **这份文件只做一件事**：把 `MULTI-AGENT-FEATURE-STATUS.md` §5 里那 **29 条**裁决项，
> 压缩成"**你要说的那一句话**"，并按"不决定的后果"排序。
>
> 全量说明在 §5；这里是**决定用的那部分**。
>
> 生成于第 32 轮、**第 33 轮按 §9 投影重排**（2026-09-19）。
> 读数：台账 **145 行 = 140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜**。

---

## 0A. ★★★ 产品自己写的「完成标准」是一条九节的链，**它断在第 5 节**

目标文档 §9 把"做完了"写成一条链（逐字）：

```text
安装/配置 → Launcher 启动 Runtime → 创建空间与 TeamPlan
→ 认领 Task / 生成 Snapshot → DshRuntimeAdapter 执行 Run
→ 工具审批与 hard floor → 产物验收/交接/审计/用量
→ Runtime 崩溃可恢复 → 升级失败可回滚且业务数据不丢失
```

第 33 轮把这条链**逐节投影到真实接线**上（`scripts/prt/alpha-chain-trace.mjs`，
8 条变异钉住）。读数：

| 节 | 内容 | 判定 |
|---|---|---|
| L1 | 安装/配置 | ✔ 有活实现 |
| L2 | Launcher 启动 Runtime | ✔ 有活实现 |
| L3 | 创建空间与 TeamPlan | ✔ 有活实现 |
| L4 | 认领 Task / 生成 Snapshot | ✔ 有活实现 |
| **L5** | **DshRuntimeAdapter 执行 Run** | **✖ 硬断** |
| L6 | 工具审批与 hard floor | ✔ 有活实现 |
| L7 | 产物验收/交接/审计/用量 | △ 软缺口（`spool`/`toolcall-drain` 没人挂）★ **第 84 轮**：根因已定 —— 不是缺一个落点决定，是 hub **没被登记进派生表**（第 75/76 轮） |
| L8 | Runtime 崩溃可恢复 | ✔ 有活实现 |
| L9 | 升级失败可回滚 | △ 软缺口（`retention.mjs` 没人挂） |

### L5 为什么是断的，以及它**正好就是第 20 条裁决项**

worker（orchestrator 进程）与 DSH Runtime 是**两个进程**，所以同进程的
`bindDshRuntime()` **填多好都不会改变 worker 的读数**——
这句话是 `runtime-contract-server-row.mjs:9-10` **自己写的**。
那一行就是这条缝上**唯一的监听器**，而它：

- **不在** `PATCH_LAYER_ROWS` 里（补丁层五行：hard-floor / root / pre-execute / approval-answerer / permission-presets）；
- 不在 `legion-host.patch.yml` 里；
- **没有任何生产 importer**（全仓只有测试文件 import 它）。

⇒ 那份发布**永远不会被写出来** ⇒ Launcher 的消费侧读不到 ⇒ 不注入
`LEGION_RUNTIME_URL`/`LEGION_RUNTIME_TOKEN` ⇒ worker 报具名码
`EXECUTOR_HOST_PORT_REQUIRED`、**不认领任何任务**。

★★ **而这是"可见的降级"，不是静默失效**——这一点很重要，也是代码自己强调的取舍：
出口挂不上时产品照常起、worker 明说自己干不了活，而不是"看起了却执行了错的写操作"。
所以 L5 的问题**不是**"它在偷偷做错事"，而是"**它今天做不了这件事**"。

★ `product/launcher/launcher.mjs:473-476` 逐字记着同一个事实：
> worker…读两个键…**在本批之前没有任何生产方写它们**——
> 白名单只放行清单声明过的键，所以真实部署里它们会被丢掉，worker 永远报 `EXECUTOR_HOST_PORT_REQUIRED`。

### 我要您说的一句话

第 20 条给的两个选项（§5 原文）：

- **甲**：把那一行**挂进补丁层**，但让 `probeRuntime` **如实报 unknown** 并以具名码拒绝
  （fail closed）。效果与今天"没挂"**等价**，但读数从"**没人挂**"变成"**查过、且拒了**"。
- **乙**：明确本阶段只走**同进程绑定**这一条路，把跨进程契约**显式降级为未启用**并写进产品边界。

★ **两条都不许"编"**——这正是 §5 第 20 条写明的红线：给一张**全 true** 的能力表
会让 `checkCompatibility` 在一个**从未验过**的引擎上判"兼容"。
**那比不接更坏，因为它会以"已兼容"的样子通过。**


> ✅ **2026-09-20：甲已执行（`2f5a4b3`）—— 这一节可以按"已办"读。**
>
> 挂的**不是一个环节，是两个**：除了题面那一行 `runtime-contract-server-row.mjs`，
> 还有 `runtime-host-registrar-row.mjs`（生产 `probeRuntime` 输入的注册方）。
> 只看题面会漏掉第一个，而漏掉它，`probeRuntime` 一次都不会跑。
>
> ★★ **这条交付不是"产品终于能执行了"。** 行为与接线前**等价**：
> 四项必需能力里三项**如实报未确认**（`usage-reporting` 的理由是硬的——引擎的
> `SubagentResult` 契约**没有**任何用量字段）⇒ 自检不兼容 ⇒
> `autoExecutionForbidden: true` ⇒ 禁止自动执行；服务端那一行今天没有生产输入工厂，
> 报 `RUNTIME_CONTRACT_ROW_NO_INPUTS_FACTORY`。worker 仍然不认领任何任务。
>
> 挂之前把"会不会把产品弄挂"逐字核过（不是推测）：两行都**不在 `apply` 期抛**——
> `runtime-host-row.mjs:744-789` 只对**我们自己的接线错误**抛，对"自检不兼容"是
> `provide` + `return`；`runtime-contract-server-row.mjs:34` 文件头写明永不抛。
>
> ★★★ **一个该记下来的读数教训**：改完门禁 **10/10 全绿**，我据此一度写下
> "探针看不见 YAML 挂载"——**错了**，`reachability.test.mjs` 纠正了我：
> 基线里 4 条 `gap` 其实**已经变成可达**。`--diff` 只保证「现实里不可达的，基线里都有
> 分类」（防漏登），**不**保证「基线里的，现实里仍然不可达」（防过期）。
> 后者只有单元测试管——而那个盲点，测试自己在 `L160-172` 逐字写着。
> ⇒ "十道门禁全绿"与"这一批是对的"，仍然不是同一件事。
>
> 可达性基线 48 → **44**（`gap` 27 → 23）。

---

## 0B. ★★★ 另一条：一个数组缺 5 把键，**一个人挡着五条**

**一句话**：`product/process-manifest.mjs` 是**另一会话的在制品**（有未提交改动；
那一组在制品里最近一次改动是 `product/launcher/readiness.mjs`，**今天 08:00**）。
它里面有**一个数组**（`runtime` 进程的 `envNames`）缺 **5 把键**。

**这 5 把键一起通、一起不通。** 补那一个数组，下面五条同时解开：

| 键 | 它管什么 | 今天的后果（实测） | 其余环节 |
|---|---|---|---|
| `LEGION_PATH_SCOPE` | 这一 Run 的读根/写根 | 配了也到不了子进程 ⇒ 路径范围检查**从不运行** | 读取点 + 装配点 + 生产路径用例**都已就绪** |
| `LEGION_CONNECTOR_DECLARATIONS` | F-21 连接器声明 | 同上 ⇒ 连接器判定面**从不运行** | 同上（`root-row.test.mjs` 五条已验） |
| `LEGION_EXECUTION_SCOPE` | 命令/网络/MCP 授权 | 同上 | 同上（`production-scope-wiring.test.mjs` ①d/③d 已验） |
| `LEGION_EXTERNAL_API_SCOPE` | 外部 API 读/写范围 | 同上 | 同上（含真实 `preExecute` 的拒与放） |
| `TEAM_HUB_TOKEN` | 早于本轮、**无归属**的一处 | 根 `root.mjs` 会读它，只是它是**可选**的 ⇒ 今天不致命，但"配了也传不到"一模一样 | —— |

★ **为什么它今天不报错**：这 4 把键在 `runtime/config-schema.mjs` 里**都注册了**
（用户配得进去、schema 校验通过），4 个装配点 `*PortFromEnv()` 在 `root-row.mjs` 里**都接好了**——
于是"能配"与"接好了"两处各自的判据**都是绿的**，而中间那根线不存在。

★ **我要您说的一句话**（二选一）：

- **甲**：「**那个文件我可以动**」⇒ 我把这 5 把键加进那一个数组（5 行），并更新
  `scripts/config/config.test.mjs` 的 `NOT_FORWARDED_YET`（5 条理由删掉）。
  **工时约半小时**，之后四道范围检查 + 连接器判定在真实部署里开始生效。
- **乙**：「**等那个会话**」⇒ 我不动它。上面五条继续停在原处。

★ 顺带说清：**这个判断不是我猜的**——仓库自己的账本就写着
（`scripts/config/config.test.mjs:768`）：*"最后一根线在 `product/process-manifest.mjs` 的 runtime
`envNames` —— 而那个文件是**另一会话的在制品**。"* 我沿用了那条记账。

> ★★★ **2026-09-20 复核（另一会话）：这一节的"乙"没有完成信号，所以它其实不是一个选项。**
>
> **① 那个文件仍然是别人的在制品** —— 距本节写成已过 **2 天、约 25 轮**，
> `git status` 仍是 ` M product/process-manifest.mjs`（`product/config-schema.mjs`、
> `product/launcher/launcher.mjs` 同样）。
>
> **② 更要紧：那个在制品里根本没有这 5 把键。** 实测它的未提交 diff 新增 **157 行**，
> 而 `LEGION_PATH_SCOPE` / `LEGION_CONNECTOR_DECLARATIONS` / `LEGION_EXECUTION_SCOPE` /
> `LEGION_EXTERNAL_API_SCOPE` / `TEAM_HUB_TOKEN` 在里面**各出现 0 次**；
> 它的改动落在 `L31` / `L67` / `L151` / `L340` / `L347` / `L355` / `L368` 一带
> （`PROCESS_SPECS` 与 `materializeProcessPlan` 的方向），**没有碰** runtime 的
> `envNames` 数组（在**那一份在制品**里它在 `L255`；`HEAD` 里的位置更靠前，
> 所以别把 `255` 当稳定引用，按符号找）。
> ⇒ **等它提交，等不到这 5 把键。** 那个会话在做另一件事；它提交之后这个数组
> **照样**缺这 5 把键，本节的两个选项会原样回到我面前。
>
> > 一个"等别人提交"的阻塞，与一个"等一件永远不会发生的事"的阻塞，
> > 在状态栏里都是 `等待`——只不过前者的等待有人在推进。
>
> **③ 那道闸是真的（不是装饰），所以修法是两处协同的。** `scripts/config/config.test.mjs`
> 对这一节下了六条：① schema `fields` ↔ 清单 `envNames` 的集合关系；② 名单**查旧**
> （已放行的键留在名单里即红）；③ 每条理由 ≥ 40 字；⑤ 理由里指的装配点**符号必须还在**
> 那个文件里、且逐字出现在理由正文里；反向要求每条都有锚点；④ 把缺口**精确计数**成
> 恰好这 5 把（`deepEqual`）。⇒ 补数组必须同时改 `config.test.mjs`（那一份是**干净的**）：
> 删 5 条理由与锚点、把计数断言改成空。**这不是"5 行"，是"5 行 + 一处协同"。**
>
> **④ 顺带核清一条容易混的**：失败形态取决于走哪条路——`buildChildEnv()` 对未声明的键
> 在 `values` 里**直接抛**、在 `baseEnv` 里**静默丢掉**（`scripts/config/config.test.mjs:745`
> 已经这么记着，两边并不矛盾）。部署配置的来源是 Launcher 自己的环境 ⇒ 走 `baseEnv`
> ⇒ 落到**静默**那一侧，与本节的描述一致。
>
> **⑤ 现在有一条已验证的做法，可以只提交我那一处、"甲"比 9-18 那次问的时候更安全了。**
> 做法：用 `git hash-object` + `update-index --cacheinfo` 造一个「`HEAD` + 我这几行」的 blob
> 放进索引，再**不带 pathspec** 提交。9-20 已经在 `product/launcher/launcher.mjs` 上
> 走通过一次：提交统计正好是我那 4 行，对方的 130 行内容**逐字未动**、仍未提交。
> ⚠️ 而**带** pathspec 的 `git commit -- <path>` 会按路径重新暂存工作区，**反而**会把
> 对方的改动一起提交——9-20 因此出过一次事故并当场恢复（见 `896fcc8` 的说明）。
>
> **⑥ 所以请在这两句话里挑一句**（与本节原有两个选项相同，只是"乙"的代价现在有了读数）：
>
> - **甲**：「**那个文件我可以动**」⇒ 我按 ⑤ 的做法加那 5 把键 + 协同改
>   `config.test.mjs`，**只提交我这一处**，对方的在制品一个字不动。
> - **乙**：「**继续等**」⇒ 我记下"等待无完成信号"这个读数就停手；
>   但它不会自己变好——需要有人去推那个会话，或把 `envNames` 拆出来让两处不再互相挡。


> ✅ **2026-09-20：甲案已执行完毕（`6ff673d`）。** 那一页至此可以按"已办"读。
>
> **改了三处，不是两处**（上面那行只写了清单 + `config.test.mjs`）：
>
> | 文件 | 改动 |
> | --- | --- |
> | `product/process-manifest.mjs` | runtime `envNames` +4 把（13 → 17） |
> | `product/config-schema.mjs` | `CHILD_ENV_NAMES` +4 把（它 == 清单 `envNames` 的并集） |
> | `scripts/config/config.test.mjs` | `NOT_FORWARDED_YET` 删 4 条、`ASSEMBLY_ANCHORS` 删 4 条、计数 5 → 1 |
>
> **第三处是动手时才暴露的**：只改前两处的话，`scan --check` 会报出 **4 个未处理字面量**
> ——扫描器在 **product 那一层**看见了 4 个没人认领的 env 字面量，因为
> `CHILD_ENV_NAMES` 没有同步。*"清单和 schema 两张表"这种事，只有在真的动第一张时才会发现。*
>
> ★★ **加的是 4 把，不是 5 把。** 上面把 5 把键写成"一起通、一起不通"——**第 5 把
> `TEAM_HUB_TOKEN` 不能这么做**：§5 第 19 条已裁决不把控制面凭证注入执行面，
> 且 `allowlist.test.mjs` 有一条边界不变量逐字要求 `runtime` 只持有
> `LEGION_RUNTIME_TOKEN`。照字面执行"5 把一起通"会**破坏一条有测试守着的安全边界**。
> 它要走的另一条路是**从 schema 的 `fields` 里拿掉**（会改配置面），**留给单独一次裁决**——
> `NOT_FORWARDED_YET` 里现在只剩它一条，且把这一点写明了。
>
> **行为读数（不是集合读数）**：用真的 `buildChildEnv()` + 真的 `PROCESS_SPECS`，
> 在 `baseEnv` 里配好这 4 把键 ⇒ **四把全部进了 runtime 子进程环境**；
> 负对照 `TEAM_HUB_TOKEN` **仍进不去**（`dropped` 里就是它）。
> ⇒ 四道范围检查 + 连接器判定在真实部署里**开始生效**，而执行面依旧拿不到控制面凭证。
>
> **提交里只有我这几行**：那两个文件都是另一会话的在制品（157 行 / 40 行未提交）。
> 做法是 `hash-object` + `update-index --cacheinfo` 造「`HEAD` + 我这几行」的 blob，
> 再**不带 pathspec** 提交；实测索引里 3 个文件、`7 0` / `17 0` / `15 49`，
> 对方两个 diff 的**内容行逐字未动、仍未提交**。

---

## 1. 需要「产品 / 架构」裁决的（**定语义**，不定语义就会猜）

每条都是**二选一或三选一**，不是开放题。全量论证见 §5 对应条。

| # | 你要定的一句话 | 两个选项 | **不决定**的后果 |
|---|---|---|---|
| **27** | 岗位清单写的是**Legion 能力名**还是**执行面工具名** | ①Legion 名（那要加映射）②DSH 名（那要改清单写作规范） | 两个词汇表**结构上不相交** ⇒ `whitelist` 配了**仍然是全拒**。★ 而且它在生产装配里**今天根本没传值**（`surfaces.whitelist === false`，有用例钉着） |
| ~~**28**~~ | ~~那条**出站车道**的文件放哪个目录~~ ★ **第 84 轮：这一条已不是裁决，见本行末** | 甲：新登记一个键（**会落进第 0 条同一个数组**）／乙：收账侧住进 hub 进程、路径从 `dbFile` 同级派生／丙：照第 19 条的形状按 Run 交付（**唯一与已裁决逐字一致**，最贵） | PRT-610 的车道**已建、环已在真 SQLite 上走通**，而生产里没人调它 ⇒ `decisionSourceRecorded` **永远没有产出者**，发布门禁永远判否——**而它判否的理由读起来完全正确**。<br>★★★★★ **第 84 轮撤回上面那三个选项**：第 75 轮量到这件事**不是位置决定** —— `product/config-schema.mjs:88` 定义了这个产品**唯一**的 `dataDir`（`LEGION_DATA_DIR`，落点由**冻结的目录布局**决定），`:1043`/`:1045` 已把它派给 **runtime** 与 **orchestrator**，而 `product/process-manifest.mjs:111` 显示 `team-hub` 是**同一份进程清单里的兄弟进程**，只是**没被登记进那张表** ⇒ **两边用的是同一个 `layout.dataDir`，不是猜的**。缺的是**三处**改动（第 76 轮已钉到行），被**另一会话正占着那三个文件**挡着，**不是被裁决挡着**。⇒ **这一条不需要您说话。** |
| **24** | 政策门要不要从连接器声明里读「能力」 | ①读 ②不读（那就如实记成"连接器层只能更严") | MCP 公开名不在能力目录里 ⇒ **配了 `allow` 与没接上的最终判决相同** |
| **25** | 「这个岗位能调哪些 MCP 工具」由**哪张表**说了算 | ①`execution-scope.mjs` 的 `mcp` 段 ②F-21 的连接器登记表 | 两张表对**同一个工具**给出不同判决 |
| **26** | 外部 API 授权表要不要管 **scheme** | ①管 ②不管 | `checkExternalApi` 不看 scheme ⇒ `ftp://…` **会被放行** |
| **12** | F-19 Role Pack 的「七类版本」范围 | ①确认这七类 ②增删 | 版本固化的**边界**不明 ⇒ 复现性无从判断 |
| **10** | F-22 / F-24 记 🟡 还是 ⏸ | ①🟡（还没做完）②⏸（按客户需求，设计范围内已完成） | §9 说"按真实客户需求推进；当前落地范围**按设计已完成**"，而功能表记 🟡——**同一个事实被判了两次** |
| **6** | PRT-509 的 C1～C4 | 逐条确认 | 四条停在 🟡 |
| **21** | PRT-256 / PRT-910 要的**不是机器也不是凭据** | 你能提供"一个真实外部用户 / 一个真实用户项目"吗 | 两条 ⏸ 只能继续等 |
| **8** | F-23 / F-25 要不要做 | ①做 ②不做 | 不做则从路线图中明确移除，而不是一直挂着 |

## 2. 需要「项目主 / 项目方」裁决的

| # | 一句话 | 不决定的后果 |
|---|---|---|
| **20** | ★★★ **Runtime 契约服务端那一行要不要进补丁层** —— **见 §0A：这就是那条九节链唯一的硬断** | 消费侧（读回发布并注入 `LEGION_RUNTIME_URL`/`LEGION_RUNTIME_TOKEN`）**已接线**，而没有服务端 ⇒ 真实部署里**那份发布永远不会被写出来** ⇒ worker 报 `EXECUTOR_HOST_PORT_REQUIRED`、**不认领任何任务** |
| **17** | PRT-707 的**两份实现**（活的 `--wizard` 内联 / 死的 `first-run.mjs` 636 行）用**两个不同的**模型密钥引用名：`model/api-key`（端到端通）vs `legion/model/<id>`（**三段引用，`addressable:false`，运行时拿不到钥匙**） | ★★ **千万别**把 `first-run.mjs` 直接接上去：它**不报错**——向导报"模型已配置"而运行时没有钥匙。已做成读数（`wizard-wiring.test.mjs` 5 例，今天全绿） |
| **16** | 阶段 9 产品动作的 **CLI 面**（PRT-903/904/905/908/909、PRT-712、PRT-707） | 模块 + 用例齐备，**没有 CLI 面** ⇒ 用户按不到 |
| **1 / 2 / 13 / 23** | 见 §5：PRT-011 分发形态、PRT-214 G1/G4、F-21 判定面接线资格、"命名空间认不出来"按教义拒 | 各自停在原处 |
| **19** | ✅ **已裁决（2026-09-18 业主裁定"选前者"）** —— 把执行面数据放进 `RunRequest`，**不**注入 `TEAM_HUB_TOKEN` | ★ 它把第 **13/14/15/18** 条从「待裁决」变成「**待施工（纯代码工作量）**」。**唯一剩下的施工缝就是 §0B 那个数组** |

## 3. 不是裁决——等日期 / 等环境（**您不需要做任何事**）

| 台账行 | 等什么 | 现状 |
|---|---|---|
| PRT-253 | 一次**真的自动执行** | 真额度**已花过且已验**（`dsh --profile headless` → exit=0、9.2s）；剩下是平台边界（Windows 上不会有自动执行） |
| PRT-009 | 一台**执行期外**的机器采样峰值资源 | 两个采样器**均已落地**，只差一次实跑读数 |
| PRT-256 | **真实外部用户** | 环境 |
| PRT-910 | **真实用户项目** | 环境 |
| PRT-316 | **日期闸门**：最早 **2026-09-24** | 评审闸门**已过**，纯等待 |

★ **PRT-009 顺带一条**：`supervisor.peakResource()` 算出来的峰值读数
**今天到不了磁盘**——`launcher.mjs:1261-1267` 建 `processes` 时只带 `key`/`pid`/`image`，
而 `buildRunRecord` 是**闭合映射**（不认识的字段**静默丢掉**）。
记录层已能带、能校验、能落盘、能读回 ⇒ **缺的是生产者的那一行**。
修法与第 0 条同理（在另一个会话的在制品文件里，1 行）。

★★ **第 40 轮把这根链逐段走完了，结论是"真的只差那一行"**（此前是推断，现在是读数）：

| 段 | 位置 | 状态 |
| --- | --- | --- |
| 生产者 | `supervisor.mjs:508` `status()` 每行带 `peakResource: readPeakResource()` | ★ **已经在产出** |
| 记录层收不收 | `run-record.mjs` `buildRunRecord` 遍历 `RUN_RECORD_OPTIONAL_FIELDS` | ★ **收**（第 40 轮改成机械的） |
| 校验层认不认 | `run-record.mjs` `validateRunRecord` 遍历同一张表 | ★ **认** |
| **中间那一跳** | `launcher.mjs:1261-1267` 把 `status()` 的行映射成三个字段 | ★★ **就是这里丢的** |

★ 第 40 轮**顺手修掉了记录层一个真缺陷**（`launcher.mjs` 那一行没动，不属于我）：
`RUN_RECORD_OPTIONAL_FIELDS` 那张声明表在加这条判据之前**没有任何机械消费者**——
`buildRunRecord` 与 `validateRunRecord` **各自手写** `'peakResource'` 这个名字。
于是照那张表的 ★★★ 注释"新读数一律加在这里"**照做一次**的后果是：
新字段**写不出去**、也**不被校验**，而记录**看起来完全正常**
（实测：生产者交 `diskUsageBytes` ⇒ 写出没有它、`problems` 为 `[]`）。
现在两侧都遍历声明，声明了没登记 ⇒ **具名上抛**（`RUN_RECORD_FIELD_NOT_WIRED`），
并且 `recordWiring()` 把"声明 ↔ 接线"的四个方向都报出来（套件 `run-record` +6 例，**53/53**）。

> 一张只写在注释里的扩展点，与一条真的能扩展的通路，
> 在"下一个人照做之后会不会发现问题"这个读数上是同一个东西：都不会发现。

---

## 4. 您点了头之后，我立刻能做的（**按"它挡着产品完成标准"排序**）

> ★★★ **第 45 轮复核**：这张表原先是 6 行，其中**前 3 行的依赖都已经满足了**，
> 而表里那三行**仍然写着"等您点头"**。逐条核过今天的树：

| 优先级 | 做什么 | 状态 |
|---|---|---|
| ~~1~~ | ~~**第 20 条**：把契约服务端那一行挂进补丁层（甲）或显式降级为未启用（乙）~~ | ✅ **已交付** —— 您 2026-09-20 裁决「甲」，`2f5a4b3` 落地；`alpha-chain-trace` 读数：**L5 ✔ 有活实现** |
| ~~2~~ | ~~**第 0B 条**：5 把键进那一个数组 + 更新 `NOT_FORWARDED_YET`~~ | ✅ **已交付** —— `6ff673d`（"四道范围检查第一次真的能到执行面"）|
| ~~3~~ | ~~`peakResource` 那一行（生产者带上它）~~ | ✅ **已交付** —— `896fcc8`；`product/launcher/launcher.mjs:1270` |
| **1** | 接上 `spool`/`toolcall-drain` 那条车道（解 **L7** 软缺口） | 半天～一天。★ **第 84 轮更正**：原来写"等 **第 28 条**"，而第 28 条**已不是裁决**（见上）⇒ 它等的是**三处接线**（第 76 轮已钉到行），而工期被**另一会话占着那三个文件**挡着 |
| **2** | 第 27 条定语义后：补 `whitelist` 的装配 + 词汇表 | 半天，等 **第 27 条** |
| **3** | 第 17 条定引用名后：删掉死的那份实现 | ~1 小时，等 **第 17 条** |

★ 排序的依据是**目标文档自己的 §9**，不是我的偏好：
剩这三项里，第 1 项解 **L7**，后两项**不在那条链上**
（它们解的是 §4 的功能面，不是"做完了"的定义）。

★★★ **而这次复核本身值得记一句**：那三行不是"我说错了"，
而是**它们在被写下的那一刻是真的**，之后**事情做完了、句子没跟着改**。

> 一句现在时的"我随时能做 X"，在 X 被做完的那一刻就变成了一句假话——
> 而它**不会红**，因为它只是一句话。
> （这正是本批反复出现的那个形状的**文档版**：声明与实现各走各的，
> 而"声明还停在原地"与"事情还没做"在读者眼里是同一个东西。）

★ 另外，"第 1 项解 L5（九节链里**唯一**的硬断）"那句也一并删掉——
`alpha-chain-trace` 今天的读数是 **`最先硬断的一节：（没有）`**：
L5 已通，剩下 L7/L9 是**软缺口**（支撑模块没人挂），且**都有归属**。

## 5. 诚实边界（这一页**不**主张什么）

- 它**不**主张"其余都做完了"。它主张的是：**第 32 轮把目标文档独立核了一遍**，读数如下
  （探针都在 `scratch/`，可复跑）：
  - §4 正文的 **6 条**含"必须/不得/禁止"的规范性句子 → 5 条有落点，第 6 条是 `RunRequest.env`；
  - §4 的 **9 组**可枚举名目（**50 个** token：7 接口方法 / 6 事件类型 / 6 投递状态 /
    5 权限档 / 3 渠道 / 5 计划字段 / 7 Manifest 项 / 4 校验项 / 7 Launcher 动作）
    → 全部有落点（唯一"零命中"的 `catchup` 是拼写变体，实现用的是 `catch-up`）；
  - §7 的 **7 项故障注入 + 5 项安全覆盖** → 全部有真套件。
    ★ 其中"**审批超时**"我第一遍搜出**零命中**，第二遍才找到
    `team-hub/approval-ttl.mjs`（PRT-615，**40 例**）——**代码里说的是 TTL，不是超时**；
  - §1/§8 引的**路径逐个解得开**（含 §8 引的 **11 个仓外文件**，
    以及 **16 处** `file:line` 引用、合计 **22 个行号**——**全部在范围内，0 处越界**）；
  - `reachability` 的 **48 项**不可达，每一项都带分类（`by-design=13 / gap=27 / deliberate=8`）
    与一个裁决处编号。
- **这一轮我自己报过 4 次假发现**（worktree 副本把 `server.mjs` 撑成 38 个同名、
  两次"零命中"其实是拼写变体、一次是我的解析器不懂表格惯例）。
  ⇒ 所以上面每一条读数都注明**它可能错在哪**：关键词零命中**不等于**没实现。
- 它**不**替任何一条做决定。上面每一格的"两个选项"我都没有倾向性结论——
  它们各自都自洽，而"猜一个两边都同意的"正是 PRT-253 §3 禁止的"发明默认值"。
- 第 0 条那个判断**依赖一个外部事实**（"那个文件属于另一个会话"）。
  如果那个会话其实**已经结束**，那么第 0 条不是裁决项，而是**我该直接做掉的一件事**。
