# PRT 阶段 0～1 实施计划（基线冻结 + Runtime Contract）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在不与在途功能争用主干文件的前提下，冻结当前系统基线、立起 DSH 执行面边界棘轮，并交付可用内存 Fake Adapter 测试的 Runtime Contract。

**Architecture:** 阶段 0 以「只读采集 + 新增文档 + 新增脚本」为主，不触碰 `plugins/src/index.ts` 与 `team-hub/server.mjs`；阶段 1 全部落在新目录 `runtime/contracts/`，由契约测试与 Fake Adapter 驱动。两者共同构成后续大规模编排提取（阶段 3～4）的安全边界与比较基准。

**Tech Stack:** Node.js ESM、`node:test`、零第三方依赖（与仓库既有 CI 脚本一致）、`scripts/ci/run-ci.mjs` 阶段化 CI。

**Spec:** `docs/superpowers/specs/2026-09-11-legion-product-runtime-design.md`（位于分支 `codex/product-runtime-design`；本计划分支从 `main` 切出，合并该 spec 后方可直接引用）

## Global Constraints

- **不动主干文件**：本批次不修改 `plugins/src/index.ts`、`team-hub/server.mjs`。这两个文件近 60 个提交里分别被改 51 / 35 次，是与其他功能争用的唯一热点；阶段 3 之前不进入。
- **棘轮只紧不松**：`dsh-boundary` 基线只许在**真实迁移**后下移，且 `runtime/contracts/`、`orchestrator/`、`product/`、`security/` 永不允许基线例外。
- **契约不依赖 DSH**：`runtime/contracts/` 不得出现 Cordis/DSH 类型或 `ctx.*`（由 `dsh-boundary` 的 `must-be-zero` 判定强制）。
- **基线必须在系统还是当前形态时采集**：PRT-004/007/009 的数值会随在途功能持续漂移，越晚采集越无法证明新路径不是性能回退。
- **留痕按仓库纪律**：`docs/STATUS.md` 是「当前状态」唯一入口；`docs/**-evidence/` 是带 banner 的历史快照；CI 单入口 `node scripts/ci/run-ci.mjs`。
- **本批次结束即到闸门**：阶段 2.5（M1.5）未达标前不启动阶段 3。〔spec §13 Program 级停止条件〕

---

### Task 1: DSH 执行面依赖清单与静态边界棘轮　✅ 已完成

**对应：** `PRT-002`、`PRT-108`

**Files:**
- Create: `scripts/ci/dsh-boundary.mjs`
- Create: `scripts/ci/dsh-boundary-baseline.json`
- Create: `scripts/ci/dsh-boundary.test.mjs`
- Create: `docs/superpowers/prt/PRT-002-dsh-boundary-inventory.md`
- Modify: `scripts/ci/run-ci.mjs`（新增 `boundary` 阶段，紧随 `env`；`test` 阶段新增 `dsh-boundary` 套件）

**Interfaces:**
- Produces `scanSource(source)`、`scanRepo()`、`diffAgainstBaseline(scan, baseline)`、`totalOf(hits)`；CLI 支持 `--check` / `--report` / `--update-baseline` / `--json`。

**结果：** 3 个文件 / 26 处执行面记号进入基线；`plugins/src/index.ts` 是本仓库唯一导入 DSH 执行面包的源文件（22 处，含第 63 行 `inject` 声明 4 个执行面服务）。22 条单测全绿，`boundary` 阶段秒级门禁生效。

- [x] 扫描器 + 基线 + 单测 + CI 阶段
- [x] 端到端证明：真实探针文件会让 `--check` 以 1 退出并点名归因
- [x] 依赖清单文档（口径、分类、迁移目标、未覆盖项）

### Task 2: 拓扑与配置来源清单

**对应：** `PRT-001`、`PRT-003`

**Files:**
- Create: `docs/superpowers/prt/PRT-001-topology-inventory.md`
- Create: `docs/superpowers/prt/PRT-003-config-secret-inventory.md`
- 复用（只读）：`scripts/config/scan.mjs`、`scripts/config/check.mjs`、各进程 `config-schema.mjs`

**Interfaces:**
- Produces 两张清单：① 进程 / 端口 / 组件 / 数据拓扑；② 配置项 / 环境变量 / 密钥 / 路径来源。

- [ ] 用 `scripts/config/scan.mjs` 导出各进程读取的环境变量全集，与 `config-schema` 声明集合对账，列出**未声明却可读**的变量（这是 §6.10「所有环境变量必须在配置 Schema 中声明」的差距清单）。
- [ ] 记录进程拓扑：team-hub、Workbench 静态服务、whiteboard、plugins 守护、services-plugin 的启动顺序、端口与就绪判据。
- [ ] 记录数据拓扑：SQLite 文件、附件目录、worktree 目录、`~/.dsh` 下的状态文件（含 `super-injector` 日志、`daemon.json`）。
- [ ] 标注每项在 `InstallDir/DataDir/Workspace/CacheDir/LogDir` 五类目录中的归属，指出当前**越界**的写入（尤其写入安装目录或仓库根的情况）。
- [ ] 密钥来源清单：现状是否已有明文密钥落盘（`.credentials.yaml` 等），标注为 PRT-505 的迁移输入。

### Task 3: 术语冻结与 DSH 组合基线

**对应：** `PRT-008`、`PRT-010`

**Files:**
- Create: `docs/superpowers/prt/PRT-010-dsh-composition-baseline.md`

**Interfaces:**
- Produces ① Task/Attempt/Run/Session/Lease/TeamPlan/ContextSnapshot 术语表（spec §4.6 的落地副本，避免读者必须跳分支）；② DSH 组合层与 patch 锚点基线。

- [ ] 记录 DSH 组合分层：`packages/bundle/base/cordis.patch.yml`（dsh-base）→ 模式 bundle（`web-app`/`headless`）→ 用户 profile 层，并记录实际生效顺序。
- [ ] 记录 Legion 当前如何被挂载（`services-plugin` 与外部包构建路径）与 `DSH_HOME` 布局。
- [ ] 用 `apps/cli/composition.md` 生成物（`gen-doc-graphs`）对账当前组合行，落成可 diff 的锚点快照。
- [ ] 明确标注：本基线是 `dshCompositionPatchVersion`（spec §9.1）出现前的对照起点。

### Task 4: DSH 分发形态决策

**对应：** `PRT-011`

**Files:**
- Create: `docs/superpowers/prt/PRT-011-dsh-distribution-decision.md`

**Interfaces:**
- Produces 一份决策记录，回答：是否内置 Node 运行时、DSH 代码与依赖树如何进安装包、首次运行是否必须联网、`DSH_HOME` 位置与 preset 安装位、升级时如何保证 shipped preset install 不被编辑。

- [ ] 评估三条路线（内置完整 Node+DSH / 依赖宿主已有 DSH / 混合），各自给出安装体积、首启耗时、升级复杂度与法务影响。
- [ ] 确认「不得编辑 shipped preset install」在升级时的具体机制（只读目录 + 产品自有 profile 层注入）。
- [ ] 输出结论与不做项，作为 `PRT-257`（Launcher 装载 DSH 运行时与组合补丁层）的输入。

### Task 5: 黄金流程定义与基线采集　🟡 部分完成（可机器测量部分已采集；真实执行部分待采）

**对应：** `PRT-004`、`PRT-007`、`PRT-009`

**Files:**
- Create: `scripts/prt/golden-flow.mjs`（黄金流程与固定夹具定义）
- Create: `scripts/prt/golden-flow.test.mjs`
- Create: `scripts/prt/baseline-snapshot.mjs` + `baseline-snapshot.test.mjs`
- Create: `scripts/prt/baseline-measure.mjs`
- Create: `docs/superpowers/prt/prt-007-baseline.json`、`prt-009-baseline.json`、`PRT-004-golden-flow.md`、`PRT-009-baseline.md`
- Create: `docs/PRT-009-evidence/verify-evidence.md`（带历史快照 banner）
- Modify: `scripts/ci/run-ci.mjs`（`test` 阶段新增两个套件）

**结果（已完成部分）：**
- 黄金流程 `GF-001` 定义冻结，夹具哈希 `5cab66e2…`，3 段岗位交接，验收四项全部可机器判定。
- 平台契约基线：**85 路由 / 22 表 / 7 任务状态 / 20 迁移边 / 4 目标状态 / 5 权限模式**，含源文件哈希可归因。
- 进程就绪基线：team-hub 首次就绪 **240 ms**（判据为端口可连接，非「进程未退出」）。
- 源码规模：`plugins/src/index.ts` 3718 行、`team-hub/server.mjs` 4488 行。
- 30 条单测（golden-flow 14 + baseline-snapshot 16）全绿。

**结果（未完成部分，需一次真实模型执行）：** `token-usage`、`estimated-cost`、`end-to-end-latency`、`peak-resource`、**旧路径实测任务状态序列**、`human-intervention-rate`。已登记在 `baseline-measure.mjs --pending` 与证据文档 §3，**拒绝编造数值**。

- [x] 选定黄金流程：软件交付、单目标、三段岗位交接，固定输入仓库与固定验收契约
- [x] 记录当前 HTTP 契约基线与数据库 schema 快照（`prt-007-baseline.json`）
- [ ] 用 F-01 事件流与 audit 记录旧路径**实测**任务状态序列（需真实执行）
- [ ] 采集成本/延迟基线：token、费用估算、端到端耗时、峰值内存与 CPU（需真实执行）
- [x] 结果落 `docs/PRT-009-evidence/`，并在 `docs/STATUS.md` 增一行指向它

### Task 6: 备份与恢复验证

**对应：** `PRT-006`

**Files:**
- Create: `docs/PRT-006-evidence/verify-evidence.md`（带 banner）
- 复用（只读）：既有备份路径与 `scripts/ci/dual-write-smoke.test.mjs` 的并发启动经验

**Interfaces:**
- Produces 一次真实的「备份 → 干净目录恢复 → 校验」证据，并固化恢复命令。

- [ ] 在真实数据副本上执行备份与恢复，校验任务数、audit `seq` 连续性与附件完整性。
- [ ] **显式复查启动期并发迁移保护**：`team-hub/server.mjs` 中「`PRAGMA table_info` 与 `ALTER TABLE` 无互斥」及「WAL 切换不受 `busy_timeout` 保护」两处修复仍在位，并列入 §14.1 回归用例。
- [ ] 记录恢复耗时与所需磁盘，作为 PRT-812 定期恢复演练的基准。

### Task 7: Runtime Contract 定义与契约测试　✅ 已完成

**对应：** `PRT-101`、`PRT-102`、`PRT-103`、`PRT-104`、`PRT-105`、`PRT-106`、`PRT-109`

**Files:**
- Create: `runtime/contracts/`（`index.mjs` + `index.d.mts` + `errors.mjs` / `model.mjs` / `run.mjs` / `adapter.mjs` / `fake-adapter.mjs`）
- Create: `runtime/contracts/contract.test.mjs`
- Modify: `scripts/ci/run-ci.mjs`（`test` 阶段新增套件）

**语言选择**：运行时用 `.mjs` + `index.d.mts` 类型声明，而不是纯 `.ts`。与仓库既有约定一致
（`plugins/config-schema.mjs` + `.d.mts`、`team-hub/server.mjs` + `server.d.mts`）：测试用
`node --test` 直接 import，不需要 `--experimental-strip-types`。契约的核心价值是「双方都能
无摩擦地引用」，运行时可加载性是其中一半。

**结果：** 43 条契约断言全绿；`dsh-boundary --check` 证明 `runtime/contracts/` 对 DSH 依赖为 **零**
（该目录甚至不出现在扫描结果里）。

- [x] 契约测试：正常、失败、取消、超时、恢复、`UnknownOutcome` 六条路径的类型与语义断言
- [x] `cancel` 幂等 + 「取消与完成并发时以首个成功提交的终态为准，迟到事件只作诊断」
- [x] `execute()` 事件流必以唯一终态事件结束，且终态携带或引用 `RunResult`
- [x] `recover()` 不修改 Task 状态，只返回恢复判断（含 `mutatesTaskState: false` 否定性断言）
- [x] 精确主版本匹配 + `RuntimeCapabilities` 探测；必需能力缺失返回 `UNSUPPORTED_CAPABILITY`
- [x] `dsh-boundary --check` 证明 DSH 依赖为零

### Task 8: FakeRuntimeAdapter　✅ 已完成

**对应：** `PRT-107`

**Files:**
- Create: `runtime/contracts/fake-adapter.mjs`
- Create: `runtime/contracts/fake-adapter.test.mjs`

**结果：** 19 条断言全绿，含一段**真正的编排模拟**（执行 → 判终态 → 决策重试/恢复/人工处置），
跑完正常 / 限流重试 / 取消 / 超时 / 崩溃恢复 / 协议违规六条路径。

- [x] 可脚本化 RunEvent 序列生成器（含 `usage.updated`、`artifact.produced`）
- [x] 故障注入覆盖 11 个可由执行场景产生的标准错误码，另有 `no-terminal` 协议违规注入
- [x] 确定性：注入时钟，零随机数、零真实计时器、零 I/O——编排测试不应是 flaky 测试
- [x] 断言「不启动 DSH 即可测试正常、失败、取消、超时和恢复编排」（阶段 1 完成标准）

### Task 9: 阶段 0～1 收口

**Files:**
- Modify: `docs/STATUS.md`
- Modify: `docs/superpowers/specs/2026-09-11-legion-product-runtime-design.md`（仅回填「已落地」指针，不改设计）

- [ ] 全量 `node scripts/ci/run-ci.mjs --only env,boundary,deps,build,test,doc` 并落证据。
- [ ] `docs/STATUS.md` 记录新套件数、用例数与 `boundary` 阶段。
- [ ] 确认阶段 1 完成标准：不启动 DSH 即可测试正常、失败、取消、超时和恢复编排。
- [ ] 评审闸门：确认 `plugins/src/index.ts` 与 `team-hub/server.mjs` 的日更节奏是否已降温，再决定阶段 2 启动时机。

---

## 阶段 3 启动前置（本批次不做，但必须现在说清）

`PRT-315` / `PRT-316` 直接改 `plugins/src/index.ts`（3586 行 / 222KB）与 `team-hub/server.mjs`
（4328 行 / 260KB），而这两个文件近 60 个提交里分别被改 51 / 35 次。在途功能与它们争用同一批
文件，冲突不是「分支对不上」而是「持续追着 main 跑」。因此：

- 启动阶段 3 前，先确认这两个文件的日更节奏已降温；
- `PRT-316` 必须排在 team-hub 那轮启动期并发迁移加固完全沉下来之后（见 Task 6）；
- 阶段 3 的切片每个都要能独立对拍与回滚，不允许一次性重写两个大文件。〔spec §2.1、§11 迁移规则 3〕
