# PRT 阶段 0～1 实施计划（基线冻结 + Runtime Contract）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在不与在途功能争用主干文件的前提下，冻结当前系统基线、立起 DSH 执行面边界棘轮，并交付可用内存 Fake Adapter 测试的 Runtime Contract。

**Architecture:** 阶段 0 以「只读采集 + 新增文档 + 新增脚本」为主，不触碰 `plugins/src/index.ts` 与 `team-hub/server.mjs`；阶段 1 全部落在新目录 `runtime/contracts/`，由契约测试与 Fake Adapter 驱动。两者共同构成后续大规模编排提取（阶段 3～4）的安全边界与比较基准。

**Tech Stack:** Node.js ESM、`node:test`、零第三方依赖（与仓库既有 CI 脚本一致）、`scripts/ci/run-ci.mjs` 阶段化 CI。

**Spec:** `docs/superpowers/specs/2026-09-11-legion-product-runtime-design.md`（位于分支 `codex/product-runtime-design`；本计划分支从 `main` 切出，合并该 spec 后方可直接引用）

## Global Constraints

- **不动主干文件**：本批次不修改 `plugins/src/index.ts`、`team-hub/server.mjs`。这两个文件近 60 个提交里分别被改 51 / 35 次，是与其他功能争用的唯一热点；阶段 3 之前不进入。
- **棘轮只紧不松**：`dsh-boundary` 基线只许在**真实迁移**后下移，且 `runtime/contracts/`、`orchestrator/`、`product/`、`security/` 永不允许基线例外。
- **契约不依赖 DSH**：`runtime/contracts/` 不得出现 Cordis/DSH 类型或 `ctx.*`（由 `dsh-boundary` 的 `must-be-zero` 判定强制）。
- **基线必须在系统还是当前形态时采集**：PRT-004/007/009 的数值会随在途功能持续漂移，越晚采集越无法证明新路径不是性能回退。
- **留痕按仓库纪律**：`docs/STATUS.md` 是「当前状态」唯一入口；`docs/**-evidence/` 是带 banner 的历史快照；CI 单入口 `node scripts/ci/run-ci.mjs`。
- **本批次结束即到闸门**：阶段 2.5（M1.5）未达标前不启动阶段 3。〔spec §13 Program 级停止条件〕

---

### Task 1: DSH 执行面依赖清单与静态边界棘轮　✅ 已完成

**对应：** `PRT-002`、`PRT-108`

**Files:**
- Create: `scripts/ci/dsh-boundary.mjs`
- Create: `scripts/ci/dsh-boundary-baseline.json`
- Create: `scripts/ci/dsh-boundary.test.mjs`
- Create: `docs/superpowers/prt/PRT-002-dsh-boundary-inventory.md`
- Modify: `scripts/ci/run-ci.mjs`（新增 `boundary` 阶段，紧随 `env`；`test` 阶段新增 `dsh-boundary` 套件）

**Interfaces:**
- Produces `scanSource(source)`、`scanRepo()`、`diffAgainstBaseline(scan, baseline)`、`totalOf(hits)`；CLI 支持 `--check` / `--report` / `--update-baseline` / `--json`。

**结果：** 3 个文件 / 26 处执行面记号进入基线；`plugins/src/index.ts` 是本仓库唯一导入 DSH 执行面包的源文件（22 处，含第 63 行 `inject` 声明 4 个执行面服务）。22 条单测全绿，`boundary` 阶段秒级门禁生效。

- [x] 扫描器 + 基线 + 单测 + CI 阶段
- [x] 端到端证明：真实探针文件会让 `--check` 以 1 退出并点名归因
- [x] 依赖清单文档（口径、分类、迁移目标、未覆盖项）

### Task 2: 拓扑与配置来源清单　✅ 已完成

**对应：** `PRT-001`、`PRT-003`

**Files:**
- Create: `scripts/prt/topology-inventory.mjs` + `topology-inventory.test.mjs`
- Create: `docs/superpowers/prt/prt-001-003-inventory.json`（机器清单）
- Create: `docs/superpowers/prt/PRT-001-topology-inventory.md`、`PRT-003-config-secret-inventory.md`
- 复用（只读）：`scripts/config/scan.mjs`、`check.mjs`、各进程 `config-schema.mjs`

**结果（3 条实测结论）：**
- **声明缺口为 0**。spec §6.10「所有 env 必须声明」这条要求**已由 P3-2 统一配置系统满足**，
  `scan --check` PASS（97 个疑似字面量全部已处理），PRT 不需为此再做工作。单测锁定它。
- **4 个 path 字段默认落在安装目录内**（越界写入）：`TEAM_HUB_DB=team-hub/team.db`、
  whiteboard 的 `DB_PATH`/`WB_ROOMS_DIR`/`WB_AUDIT_DIR`。安装目录会被升级覆盖 → PRT-505/257 输入。
- **仓库内明文凭证 0 处**；但 `$DSH_HOME/.credentials.yaml` 存在，且其 `{version,refs,records}`
  结构正是 `secretRef` 所指的既有机制 → **PRT-505 应复用它，不要另建密钥库**。

- [x] 用既有扫描器导出各进程 env 全集并与 Schema 对账（**复用而非重写**：第二份 env 正则
      会与第一份漂移，漂移的表现是「两份都不可信」）
- [x] 进程拓扑：入口、端口、就绪判据、托管关系（发现 services-plugin **只托管 2 个**进程，
      whiteboard 是独立子项目）
- [x] 数据拓扑与五类目录归属，指出越界写入
- [x] 密钥来源清单（拥有者、跨进程一致性、明文落盘）
- [x] 46 个被跟踪数据产物（`scratch/**` 证据快照 + 仓库根的 `.tmp-l1db/`）已登记并归因

### Task 3: 术语冻结与 DSH 组合基线　✅ 已完成

**对应：** `PRT-008`、`PRT-010`

**Files:**
- Create: `scripts/prt/composition-baseline.mjs` + `composition-baseline.test.mjs`
- Create: `docs/superpowers/prt/PRT-010-dsh-composition-baseline.md`、`prt-010-composition-baseline.json`

**结果：**
- 术语表（Task/Attempt/Run/Session/Lease/Lease Epoch/TeamPlan/Context Snapshot）冻结为
  落地副本，并补了「反例表」——术语出错多因为用错，不是因为不知道定义。
- 组合分层实测：profile 根是**空列表**，整树由 `dsh-base`（insert）→ `dsh-web-app`（patch-over）
  → 用户层（patch-over）合成；`patchReload: live`。
- Legion 挂载 **6 行 / 4 个 `file:` 依赖**，全部指向本仓库对应目录。
  `dsh-scrum-worker` 被挂 **3 次**（`scope` 是启动期常量，一实例一空间）。
- 空值配置键单独记录：`teamToken: ''` + `hubToken: ''` → **worker→hub 未鉴权**（与 Task 2 呼应）。

**两个刻意的采集口径**（都写进了代码注释与文档）：
- 快照**不记 config 值、不记绝对路径**：否则基线与机器绑定、且凭证字段会进版本库。
  契约是「有哪些行、每行配了哪些键」。
- 快照**不记注释**：用户层文件末尾有自动写入的 `# worker junction sync <时间戳>`，
  记注释会让每次同步后 diff 必红，基线立刻被当噪音忽略。

- [x] 记录分层链与实际 bundles
- [x] 记录 Legion 挂载形态（`file:` 复制快照——改了源码不重新 install 运行时不生效）
- [x] 可 diff 的锚点快照（`--diff` 无需 DSH_HOME；无 DSH_HOME 的机器上对账用例 skip 并说明原因）
- [x] 明确标注本基线是 `dshCompositionPatchVersion`（spec §9.1）出现前的对照起点

### Task 4: DSH 分发形态决策　🟡 决策输入已备齐（**结论待业主裁决**）

**对应：** `PRT-011`

**Files:**
- Create: `docs/superpowers/prt/PRT-011-dsh-distribution-decision.md`

**结果（3 条决定性事实）：**
- **DSH 已经是可分发的 npm 包**：`@deepseek-ai/dsh` `0.1.5-rc.2`、`bin.dsh`、**MIT**、
  Node `^22.19.0 || >=24.0.0`，且仓库已有完整发布校验链路 → 问题从「自建打包」
  变成「是否依赖既有 npm 包」，工程量差一个数量级。
- **当前部署是开发布局**：`$DSH_HOME/profiles/node_modules/@deepseek-ai/` 下 **244 个 junction**
  指向源码 checkout；DSH_HOME 总 270 MB 里 **258 MB 是用户 sessions 数据**，运行时代码根本不在其中。
- **DSH checkout ≈ 1845 MB**（node_modules 1490 MB + .git 200 MB + packages 112 MB）
  → 路线 A「内置完整运行时」的代价有了具体数字。

**推荐路线 C**（依赖 npm 包 + Launcher 装进 DataDir + 原子切换），理由与代价见文档 §2。

- [x] 评估三条路线，给出安装体积 / 首启耗时 / 升级复杂度 / 法务影响
- [x] 确认「不得编辑 shipped preset install」的机制（产品内容只进
      `profiles/<profile>/cordis.patch.yml` 用户层——实测该文件已是 Legion 唯一挂载点）
- [x] 输出决策输入作为 `PRT-257` 的输入
- [ ] **待裁决**：A/B/C 选哪条；是否接受首启需网络；`0.1.5-rc.2` 是否可作对外依赖
- [ ] **新发现（原计划未覆盖）**：Legion 自身四个 `file:` 包如何分发给用户
      （发布成 npm 包 or Launcher 做 junction）——这与分发形态是同一个问题的两半，
      PRT-257 必须先回答，否则装好 DSH 也挂不上 Legion 的包

### Task 5: 黄金流程定义与基线采集　✅ 已完成（GF-001 已在 `gf001` 真实跑通）

**对应：** `PRT-004`、`PRT-005`、`PRT-007`、`PRT-009`

**Files:**
- Create: `scripts/prt/golden-flow.mjs`（黄金流程与固定夹具定义）
- Create: `scripts/prt/golden-flow.test.mjs`
- Create: `scripts/prt/baseline-snapshot.mjs` + `baseline-snapshot.test.mjs`
- Create: `scripts/prt/baseline-measure.mjs` + `baseline-measure.test.mjs`
- Create: `scripts/prt/dsh-session-usage.mjs` + `dsh-session-usage.test.mjs`（会话用量提取）
- Create: `scripts/prt/gf001-run.mjs` + `gf001-run.test.mjs`（黄金流程执行台）
- Create: `scripts/prt/old-path-evidence.mjs` + `old-path-evidence.test.mjs`、`schema-scan.mjs`
- Create: `docs/superpowers/prt/prt-007-baseline.json`、`prt-009-baseline.json`、
  `PRT-004-golden-flow.md`、`PRT-009-baseline.md`，以及三份证据 JSON
- Create: `docs/PRT-005-evidence/verify-evidence.md`、`docs/PRT-009-evidence/verify-evidence.md`
- Modify: `scripts/ci/run-ci.mjs`（`test` 阶段新增五个套件）

**结果（定义与基线部分）：**
- 黄金流程 `GF-001` 定义冻结，夹具哈希 `9d4d958c…`（首版 `5cab66e2…` → `2ad47fc4…` → 现值，
  两次修正的原因见 `PRT-004-golden-flow.md` 的哈希变更记录），3 段岗位交接，
  验收**五项**全部可机器判定。
- **第一次真实执行暴露出夹具与验收契约各一处缺陷**：
  1. 夹具 `src/cli.mjs` 的入口判据手拼 `file://${argv[1]}`，在 Windows 上恒不成立
     （`import.meta.url` 是 `file:///D:/…`，手拼得到 `file://D:\…`）→
     `node src/cli.mjs --version` **退出码 0 且无任何输出**。
     单元测试直接 `import { main }` 调函数，看不到；是 planner 在现状勘察里实测发现的。
  2. 旧验收四项（`filesChanged`/`testCommand`/`testPassed`/`readmeUpdated`）
     **全部能被「只加函数、不接通入口」的改动满足**——改与不改该缺陷都会判「通过」。
     已补第五项 `cli`（真跑命令：正常路径、缺参退出码、`--version` 回归、`help` 可发现性）。
- **流水线配置自纠两处**：`gate:1` 被误用作「逼出计划书」的手段，实际语义是
  「停在 `in_review` 等将军验收」（`transitionTask` 硬性要求 `to==='done'` 时 `by==='general'`），
  会把端到端执行变成人工接力；契约文档路径 `docs/PLAN.md` 不在守护的 `GOAL_DOC_NAMES`
  槽位名单里 → 改用 `docs/TASK_BREAKDOWN.md`，并让提示词字面点名文件名。
- 平台契约基线：**85 路由 / 22 表 / 7 任务状态 / 20 迁移边 / 4 目标状态 / 5 权限模式**，含源文件哈希可归因。
- 进程就绪基线：team-hub 首次就绪为单次值，同日多次采集落在 **229 / 240 / 361 ms**
  （判据为端口可连接，非「进程未退出」）→ **抖动约 1.6 倍，不足以当回退判据**，
  阶段 3 前应取 3 次中位数（已登记为未覆盖项）。
- 源码规模：`plugins/src/index.ts` 3718 行、`team-hub/server.mjs` 4488 行。

**结果（GF-001 真实执行，2026-09-11）：**

目标 `G-mtwxx7an-2`，`planner(T-144) → implementer(T-145) → reviewer(T-146)` 三环全 `done`，
提交链 `fd2b155 → db072ea → b3266af → c7d8315`。执行台独立重算验收：
`testPassed: true` / `readmeUpdated: true` / CLI **4/4** 通过 / **`accepted: true`**。

- **`token-usage`（原「无数据」）**：input 68,822 / output 50,122 / cacheRead 1,563,264 /
  合计 1,682,208（provider 上报，非估算）。口径 `total = in + out + cacheRead`，8 条会话转录全部自洽。
  采集时发现每岗位任务下有**两条**会话（0 token 的派工工头 + 真正干活的 worker），
  证据里按 token 取主会话并把工头列进 `supersededSessions`——不去重会把 `runs` 翻倍，
  直接过滤又会让「6 条会话 / 3 个岗位」这个事实消失。
- **`end-to-end-latency`（原「无数据」）**：目标级 **402.0s**；逐岗位 planner 219.6s /
  implementer 53.6s / reviewer 103.4s。目标级与三岗位之和差 28.6s，是派工轮询间隔。
- **`old-path-task-state-sequence`（原「只有预期」）**：实测把预期**推翻**了——
  76 个有轨迹的完成任务里只有 4 个（5.3%）走过字面预期序列，42 个（55%）不经过 `in_review`。
  基准改为「模态序列 `in_progress → advanced` + 可接受集合」。
- **`human-intervention-rate`（原「无样本」）**：生产空间 `software` 95 次 / 46 任务 /
  每完成任务 **1.2** 次；受控 `gf001` 每完成任务 **0.33** 次（且那 1 次是中止轮次的取消，
  不是验收）→ 受控路径确实全链 `gate=0` 自动跑完。
- **PRT-005 新增的可用性证据**：旧路径**没有独立守护**。2026-09-11 两个独立空间
  （`software` / `gf001`）的心跳几乎同时断流、同时恢复，各约 **1.98h**，间隔相差 9 秒
  → 归因是宿主进程停机而非某空间守护的 bug，且**没有任何外部告警**。
  （并纠正了代码里「死锁 3 小时」的旧笔记——那是个没量过的印象。）
- **一处必须记下的实测偏差：模型分工没有生效**。planner/reviewer 声明用
  `deepseek-v4-pro-openai`，实际三轮全部跑在 `deepseek-v4-flash-openai` 上。
  根因是守护派工取全局默认模型，`agent_models` 里的按岗位模型只在聊天回帖路径上被读。
  本批次**不修**（阶段 3 前不动 `plugins/src/index.ts`），但已做成机器可判定的对拍
  （`gf001-run.mjs` 的 `modelDrift`，实测输出 2 条偏差）。
- **一处更严重的实测发现：声明的状态机不是被强制执行的状态机**。
  `team-hub/server.mjs` 里有**两条**写 `status='done'` 的路径：
  `transitionTask`（`:2682`）查 `TRANSITIONS` 且要求 `by === 'general'`；
  `advanceTask`（`:2705`）**两者都不查**，只要求操作者是该岗位本人。
  后果：`prt-007-baseline.json` 的 20 条迁移边**只覆盖前者**（基线不完备）；
  「只有将军能在用户接受后把任务移到 done」这条规则**可被岗位自己绕过**——
  这正是 42/76（55%）个任务不出现 `in_review` 的直接原因。
  已登记为 `GOLDEN_TASK.transitionBypass` 并由两条用例守住（其中一条在
  `advanceTask` 被改动时立刻变红，提醒对拍基准必须重算）。
  **待裁决**（PRT-316 / 阶段 3）：旁路应保留并补进迁移表，还是收窄为必须经
  `in_review` + 将军收尾。两个方向对 §14.2 的对拍基准影响相反，必须先定。
- **仍未采集的两项**（原因已不再是「需要真实执行」）：
  `estimated-cost`（缺**有来源**的单价，自建网关无公开报价，`PRICING.asOf = UNSET`）、
  `peak-resource`（会话转录不记录进程资源；目标平台取决于 Task 4 的分发形态裁决）。
  `baseline-measure.test.mjs` 有一条用例断言这两条的阻塞原因里不再出现「需要一次真实执行」
  ——清单长期挂着同一个理由就会变成噪音。

- [x] 选定黄金流程：软件交付、单目标、三段岗位交接，固定输入仓库与固定验收契约
- [x] 记录当前 HTTP 契约基线与数据库 schema 快照（`prt-007-baseline.json`）
- [x] 用 F-01 事件流与 audit 记录旧路径**实测**任务状态序列（`--scope=software` 与 `--scope=gf001` 两个总体）
- [x] 采集成本/延迟基线：token、端到端耗时**已采**；费用与峰值资源仍缺（原因见上）
- [x] 结果落 `docs/PRT-005-evidence/` 与 `docs/PRT-009-evidence/`，并在 `docs/STATUS.md` 增一行指向它
- [ ] **未覆盖**：token 用量与端到端耗时各只有一个样本（一次真实执行）。它足以证明
      「能采到、口径是什么」，不足以谈论分布；阶段 3 前应至少重复 2～3 次
- [ ] **未覆盖**：旧路径的 token / 费用 / 资源**永不补采**——全库 23 张表无任何 token/cost 列
      （`scripts/prt/schema-scan.mjs` 可复核）。因此阶段 3 的成本维度只能对比
      「新路径有数据、旧路径无数据」，不存在可比的旧路径基线

### Task 6: 备份与恢复验证　✅ 已完成

**对应：** `PRT-006`

**Files:**
- Create: `scripts/prt/backup-restore-verify.mjs` + `.test.mjs`（14 例，**合成夹具**）
- Create: `docs/PRT-006-evidence/backup-restore-evidence.md`（带 banner）
- Update: `docs/DEPLOY.md`（新增 §6.1、回滚表与发布检查单各补一条）

**结果（现场库实测；源库只读，全部实验在临时副本上做）：**

| 验证项 | 结果 |
| --- | --- |
| ① 只复制 `.db`（不含 `-wal`） | ❌ **丢 253 条 audit 记录，且无任何报错** |
| ② 三件套（DEPLOY.md §6 写法） | ✅ 完整恢复 |
| ③ `VACUUM INTO` 一致性快照 | ✅ 完整恢复，只需源库**读**权限 |
| ④ 从快照恢复到干净目录 | ✅ 单文件即可用 |
| ⑤ 恢复 `.db` 但残留陈旧 `-wal` | ❌ **陈旧 WAL 被重放，且 `integrity_check` 仍报 ok** |
| ⑥ 源库写入期间的文件复制 | ⚠️ 非同一时点视图（「恰好一致」不能证明原子性） |

**最危险的是 ⑤**：把 A 时点的 `.db` 与 B 时点的 `-wal` 放一起，SQLite 不拒绝、
库能正常打开、`integrity_check` 报 ok，只是内容混合了两个时点 →
**无法靠完整性校验发现**。而 DEPLOY.md 原回滚步骤正是「替换 .db（含 -wal/-shm 同批）」，
执行时漏掉一批就会落到这个状态。已把「恢复前必须先删 `-wal`/`-shm`」写进回滚表。

**一条被纠正的验收口径**：原计划写「校验 audit seq **连续性**」，但真实数据
**有 1 个缺口**（seq 4501–4502）。原因是分配器 `MAX(seq)+1`
（`team-hub/server.mjs:1196`）——回滚后作废号不回填，这是**有意设计**，不是缺陷。
口径改为「恢复前后**缺口集合**一致」。坚持「必须连续」会让 PRT-006 在真实数据上永远红，
而**永远红的验收项等于没有验收项**，还会训练人忽略它。

- [x] 三种备份路线对照（含 DEPLOY.md 既有写法），在真实数据**副本**上执行
- [x] 恢复到干净目录 + 逐表行数 / integrity / audit 统计核对
- [x] 陈旧 WAL 与活写竞态两个**危害实验**（不只验「正常路径可用」）
- [x] 验证工具**只读源库**（单测锁死源库与 `-wal` 的大小、mtime 前后不变）
- [x] DEPLOY.md 更新；备份建议补上**原因**（不写原因的通配迟早被人「优化」掉）
- [x] **显式复查启动期并发迁移保护**：`enableWal` 有界重试与 `ensureColumn` 的
      `BEGIN IMMEDIATE` 内重读均仍在位（`team-hub/server.mjs:178` / `:206`），
      两处原有注释与理由完整未损 → 列入 §14.1 回归用例（本批次**未改动**该文件）
- [ ] **未覆盖**：跨版本恢复（旧代码读新库 / 新代码读老库）→ 属 PRT-316，
      须排在 team-hub 启动期迁移加固完全沉下来之后
- [ ] **未覆盖（真实缺口）**：`uploads/` 等**文件类附件**不在 SQLite 里，
      `VACUUM INTO` 覆盖不到。现场库所在目录当前**无** `uploads/`，本次无法验证；
      spec 把附件与库并列作为数据分类，但备份路径只覆盖了库这一半
- [ ] **未记录恢复耗时与磁盘占用**（原计划要求作为 PRT-812 演练基准）。
      本次未计时——工具跑完约 1 秒量级，但那是内存/临时目录的机械操作，
      与真实灾备恢复（停机 + 人工步骤）不同量级，计时会产生误导性基准

### Task 7: Runtime Contract 定义与契约测试　✅ 已完成

**对应：** `PRT-101`、`PRT-102`、`PRT-103`、`PRT-104`、`PRT-105`、`PRT-106`、`PRT-109`

**Files:**
- Create: `runtime/contracts/`（`index.mjs` + `index.d.mts` + `errors.mjs` / `model.mjs` / `run.mjs` / `adapter.mjs` / `fake-adapter.mjs`）
- Create: `runtime/contracts/contract.test.mjs`
- Modify: `scripts/ci/run-ci.mjs`（`test` 阶段新增套件）

**语言选择**：运行时用 `.mjs` + `index.d.mts` 类型声明，而不是纯 `.ts`。与仓库既有约定一致
（`plugins/config-schema.mjs` + `.d.mts`、`team-hub/server.mjs` + `server.d.mts`）：测试用
`node --test` 直接 import，不需要 `--experimental-strip-types`。契约的核心价值是「双方都能
无摩擦地引用」，运行时可加载性是其中一半。

**结果：** 43 条契约断言全绿；`dsh-boundary --check` 证明 `runtime/contracts/` 对 DSH 依赖为 **零**
（该目录甚至不出现在扫描结果里）。

- [x] 契约测试：正常、失败、取消、超时、恢复、`UnknownOutcome` 六条路径的类型与语义断言
- [x] `cancel` 幂等 + 「取消与完成并发时以首个成功提交的终态为准，迟到事件只作诊断」
- [x] `execute()` 事件流必以唯一终态事件结束，且终态携带或引用 `RunResult`
- [x] `recover()` 不修改 Task 状态，只返回恢复判断（含 `mutatesTaskState: false` 否定性断言）
- [x] 精确主版本匹配 + `RuntimeCapabilities` 探测；必需能力缺失返回 `UNSUPPORTED_CAPABILITY`
- [x] `dsh-boundary --check` 证明 DSH 依赖为零

### Task 8: FakeRuntimeAdapter　✅ 已完成

**对应：** `PRT-107`

**Files:**
- Create: `runtime/contracts/fake-adapter.mjs`
- Create: `runtime/contracts/fake-adapter.test.mjs`

**结果：** 19 条断言全绿，含一段**真正的编排模拟**（执行 → 判终态 → 决策重试/恢复/人工处置），
跑完正常 / 限流重试 / 取消 / 超时 / 崩溃恢复 / 协议违规六条路径。

- [x] 可脚本化 RunEvent 序列生成器（含 `usage.updated`、`artifact.produced`）
- [x] 故障注入覆盖 11 个可由执行场景产生的标准错误码，另有 `no-terminal` 协议违规注入
- [x] 确定性：注入时钟，零随机数、零真实计时器、零 I/O——编排测试不应是 flaky 测试
- [x] 断言「不启动 DSH 即可测试正常、失败、取消、超时和恢复编排」（阶段 1 完成标准）

### Task 9: 阶段 0～1 收口　✅ 已完成

**Files:**
- Modify: `docs/STATUS.md`
- Create: `scripts/prt/hot-file-churn.mjs` + `.test.mjs`（评审闸门的可复现测量）
- Modify: `docs/superpowers/specs/2026-09-11-legion-product-runtime-design.md`（仅回填「已落地」指针，不改设计）

- [x] 全量 `run-ci --only env,boundary,deps,build,test,doc` 六阶段全 PASS，证据在 `.ci/<run>/`
- [x] `docs/STATUS.md` 记录新套件数（46）、用例数（1217）与 `boundary` 阶段
- [x] 确认阶段 1 完成标准：**不启动 DSH 即可测试正常、失败、取消、超时与恢复编排**
      —— 由 `runtime/contracts/fake-adapter.test.mjs` 的 `runOrchestration()` 覆盖六条路径
- [x] 评审闸门：`plugins/src/index.ts` 与 `team-hub/server.mjs` 的日更节奏（见 §评审闸门结论）

#### 评审闸门结论：**已降温，阶段 3 可以安排开工**

| 文件 | 最近 40 个提交触及 | 历史峰值 | 最近一次改动 |
| --- | --- | --- | --- |
| `plugins/src/index.ts` | **1/40** | 9/40（第 161–200 个提交） | 2026-09-10（35 个提交之前） |
| `team-hub/server.mjs` | **2/40** | 7/40（第 41–80 / 81–120） | 2026-09-10（37 个提交之前） |

判据：最近窗口内任一热点文件触及 ≤ 2 次。

**⚠️ 原计划里这个数字是错的，且错得刚好相反。** 原文写「近 60 个提交里分别被改
51 / 35 次」。实测：**51 / 35 量级是全history累计次数**（准确值 52 / 39），
不是近期频率。原因是 `git log -n 60 -- <file>` 会**先按路径过滤再截断**，
只要该文件被改过 ≥60 次就恒返回 60 ——「最近 60 个提交都在改它」。

正确写法是先取提交区间再做路径过滤：

```bash
oldest=$(git rev-list -n 40 HEAD | tail -1)
git log --format=%h "$oldest^..HEAD" -- plugins/src/index.ts | wc -l   # → 1
```

修正后（1/40、2/40）与原文（40/40 的观感）**结论正好相反**：
前者说「可以开工」，后者说「不能开工」。这个坑很容易重踩，因此做成了
`scripts/prt/hot-file-churn.mjs`（含单测直接锁定两种写法的差异，
防止有人把正确写法「简化」回去）。

**降温的直接证据**：09-11 一整天 16 个提交（P4-3/4/5/6 与本次 PRT）**没有一个**
碰过这两个文件——在途工作已经转移到 `whiteboard/**`。

### 阶段 3 启动前置（评审结果：可启动，但顺序不变）

- [x] 确认两个热点文件日更节奏已降温 → **成立**（1/40、2/40）
- [ ] `PRT-316` 仍须排在 team-hub 启动期并发迁移加固完全沉下来之后
      —— 该加固已由 `0db37af`（2026-09-10）落地并在 Task 6 复查为在位，
      但按原计划要求，仍应在它**沉淀一个完整发布周期**后再动该文件
- [ ] 阶段 3 的切片每个都要能独立对拍与回滚，不允许一次性重写两个大文件

---

## 后续批次：阶段 2 强制面（PRT-212～215）　✅ 已完成（**未落盘应用**，见下）

**Files:**
- Create: `runtime/dsh-composition/patch-layer.mjs`（补丁层声明 + `dshCompositionPatchVersion`）
- Create: `runtime/dsh-composition/enforcement.mjs`（三个强制点原语 + canonical operation 哈希）
- Create: `runtime/dsh-composition/selfcheck.mjs`（沙箱实际管制探测 + 启动自检）
- Create: `runtime/dsh-composition/render.mjs` + 生成物 `legion-host.patch.yml`
- Create: `runtime/dsh-composition/index.mjs`、`enforcement.test.mjs`（34 例）、`composition.test.mjs`（28 例）
- Modify: `scripts/ci/run-ci.mjs`（新增 `dsh-enforcement`、`dsh-composition` 两套件）

**结果：62 例全绿；`dsh-boundary` 仍是 3 文件 / 26 处（新目录 DSH 依赖 0 处）。**

### 实测读到的 DSH 强制面（非文档抄录）

| 机制 | 实际形态 |
| --- | --- |
| `ctx.tools.guard(fn)` | `fn: (exec) => string \| undefined`——**只有降级语义、无 allow 返回值** |
| `'tools/pre-execute'` | waterfall → `{kind:'allow'\|'deny'\|'ask'}` |
| `'approval/request'` | waterfall → 闭集 `'allowed-once'\|'rejected'\|'cancelled'\|'unavailable'` |
| `ctx.sandbox.confine(argv, policy)` | → `{argv, enforcement: 'full'\|'partial', denialSignatures, runnerFailureRules}` |

**最有价值的一条**：`enforcement: 'full' | 'partial'` 就是 spec §6.8「必须探测实际后端和
enforcement；仅有配置名不算生效」的**可判定落点**。判据取 `full`：`partial` 的字面含义是
「这条 argv 只被部分管制」，即**存在不被管制的路径**，把它当可用等于在已知有漏洞的沙箱上
宣称已限制。用例另有两条同族判据：`confine` **原样返回**输入 argv（没做包装）、
`denialSignatures` 为空（沙箱拒绝会退化成普通失败）。

### 与原设计假设的两处出入

1. `ctx.sandbox` 是**抽象进程沙箱**（只有 `confine(argv, policy)`），不是文件沙箱；
   文件读写走 `fs-sandbox` 行 + `ctx.fs` 的 `sandboxPolicy` 参数。因此「沙箱是否生效」
   不能用一次文件读来判定。
2. `sandbox` 与 `approval` 是**独立旋钮**，由 `permission` 行捆绑。DSH 默认表把
   `workspace-write↔ask`、`danger-full-access↔never` 绑定——这正是 §6.9 那条警告成立的原因：
   按默认表实现「无人值守 = `never`」会**同时**把沙箱升级为 `danger-full-access`。
   用例把 Legion 的 `legion-unattended` 锁死在 `workspace-write`。

### ⚠️ 未交付的部分（不得当成已完成）

- [x] 补丁层**声明**、三个强制点**原语**、canonical op **哈希**、沙箱**探测**、**自检**门禁
- [ ] **落盘应用**（把 `legion-host.patch.yml` 合并进 `$DSH_HOME/profiles/web/cordis.patch.yml`）
      —— **刻意不做**：profile 层是 `patchReload: 'live'`，写入会**立刻改变正在运行的
      harness 的强制面**，包括当前会话自己。理由写在 `runtime/dsh-composition/index.mjs`
      的文件头，不只写在文档里。
- [ ] **真实审批箱接线**：`createApprovalAnswerer` 的 `request` 端口尚未接 team-hub 的审批写入
      → 排在 `PRT-316`（team-hub 路由层提取）之后；现在直连会把新代码焊死在 `server.mjs` 上
- [ ] `SUPPORTED_RUNTIME.supportedMajor` 仍是占位 → 属 PRT-802/809（需真实兼容矩阵）

> **因此：这些原语目前只被自己的用例驱动，还没有任何生产调用方。**
> 「有测试」不等于「已生效」——这是这一类工作最容易犯的错，故在此显式写明。
> 详见 `docs/PRT-212-evidence/verify-evidence.md` §2 / §5。
