# 交接报告 —— 第 22–23 轮（2026-09-18）

> 这份报告是**给项目方读的**：一句话结论、最终读数、这几轮交付了什么、
> **要您裁的那一件事**（以及它为什么不是三件事）、以及仍然需要外部环境的项。
> 逐轮的技术细节在 `PRT-SESSION-REPORT-2026-09-17.md`（第 23 轮见 §10.54）；
> 总账在 `MULTI-AGENT-FEATURE-STATUS.md`；裁决记录在
> `DECISION-RUNREQUEST-EXECUTION-PLANE.md`。
>
> ★ **第 23 轮补记见第九节**（F-15 的「告警 / 降级」——spec 的一半此前没有任何实现）。

---

## 一、一句话结论

功能实现**已完成到"代码侧能做的都做了"这一步**：台账 **145 行 = 140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜**，
全量门禁 **9/9 PASS**。

★ 第 23 轮补上了 spec 里**最后一块真的没写的东西**（F-15 的「告警 / 降级」——
它此前**一件都没有实现**，而某个文件头**声称**它已实现，见第九节）。
补完之后复核了一遍**剩余的全部非 ✅ 项**，结论是：

> **剩下的没有一项是"再写点代码"能推进的。**
> 4 条 ⏸ 要外部环境、1 条 ⬜ 要到期日、3 条 🟡 与 **29** 条裁决项里
> 每一条都压在一个**人的决定**上。

**要您做的决定集中在第五节**（9 项，按"不决定的后果"排序）——
其中最要紧的仍是**第 1 项（配置键进不了 runtime 的 `envNames`）**：
它让三道范围检查与连接器判定在**真实部署里全都不生效**，而各自的用例**全绿**。

---

## 二、最终读数（都是机器读数，可复跑）

| 项 | 读数 | 出处 |
| --- | --- | --- |
| 台账 | **145 行 = 140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜** | `node scripts/prt/spec-progress.mjs --check` → `140/145，未完成 5` |
| 4 条 ⏸ | PRT-009 / PRT-253 / PRT-256 / PRT-910 | 需要一台执行期外的机器 / 一次真的自动执行 / 真实外部用户 / 真实用户项目 |
| 1 条 🟡 | PRT-316 | **日期闸门**：最早 **2026-09-24**（评审闸门已过）。★ 第 44 轮前后从 ⬜ 变 🟡（"部分"），闸门日期不变 |
| 全量 CI（**第 45 轮封板**） | ⚠️ **7/9，exit 1** —— `test` + `doc` 红（HEAD `1a62c38`，`.ci/r45c`，**脏树 20 改 + 511 未跟踪**，指纹 `ab3a77b49ee6a67e`） | `test` 1254292ms、`skipped=1`（secret-store，已知）。★ **8 个红族没有一族在本会话的改动上**：`config`(52/53) + `prt-baseline`(23/25) + `suite-counts`（`compaction-http.test.mjs` claim **5** real **13**）+ `*-dsh-process` × 3 + `runtime-contract-cross-process`（`2f5a4b3` 回归、净树复现）+ `model-api`（脏树）—— 全是另一会话；`doc` 报的是 `docs/PRT-316-evidence/slice-07-automation-family.md` **缺历史 banner**（它的切片 7） |
| ★★★ **第 51 轮** | **唯一一份写给人照做的文档，此前一条判据都没有** —— `git grep -l PRT-HUMAN-INTERVENTION -- '*.mjs'` **零命中**，而 `PRT-PROGRESS`/`PRT-HANDOVER`/`MULTI-AGENT-FEATURE-STATUS`/`prt-reachability-baseline` 四份都有人核 ⇒ "一份**要人照着做**的清单，如果它自己没有人核，它错的时候只有**读它的人**会发现 —— 而那个人正是**因为不知道答案才来读它**的"。★ 而它**已经错了**：标题停在"第 45～47 轮"（家族表已到 50）、总数停在 201（真值 **216**）⇒ `boundary-facts` 补第 20/21 条（标题轮次 = 家族表最大轮次；总数 = 块内各项之和）。★★ **三次被机制抓住我自己**：① 新判据**当场**抓到我（改了 49→51 却把总数留在旧值 214）；② 框架**通用反面控制**抓出 claim **只钉住一端**（区间标题 `第 45～50` 的左端纯装饰，改成 `第 46～50` 判据不红 —— "一条钉住'两端说法'其中一端的判据，与一条钉住整句的判据，在改动**另一端**之前是同一个读数"）；③ `ANCHOR_AMBIGUOUS` 抓出文档里有**两块**同名读数块（第 43 轮那块是历史快照）⇒ 用独有后缀 `（全部可复跑）` 钉死。★ 而**我自己的用例**犯了同一个歧义错（`replace` 改第一块、判据看第二块）⇒ 报的是「判据测不出过期」，**而坏的是控制不是判据**。★ 顺带修掉一处不属于我的漂移：`handover-tracked-suites` 报 373 vs 产物 374（另一会话新落一套） | `scripts/prt/boundary-facts.mjs`、`scripts/prt/boundary-facts.test.mjs`、`scratch/_mutate-r51-intervention.mjs` |
| ★★★ **第 52 轮** | **交付物自己的目录**也犯了同一族 —— 最终报告 §三 是**逐轮留档**（34 个小节，16→51），而 **39～43 整段倒着放**（43/42/41/40/39）、44/45 又跳到 39 前面，顶层标题还写着"第 15 轮做了什么" ⇒ "一份**看起来按编号排好了**的文档，与一份真的排好了的文档，在**只看前几节**的时候是同一个读数 —— 读者从 16 一路看到 38 都是升序，**合理地**假设后面也是"。★ 按**块**搬，带**行数守恒 + 多重集相等**两条断言 ⇒ 行级差异只有 **2 行**、**一个字符没改**。★★ 补 `boundary-facts` 第 22 条 + 三条用例；锚点**刻意只圈一个数**（第一版想钉整句，而通用反面控制改的是**起点**那个数 ⇒ 控制不红 —— **第 51 轮刚在完全相同的形状上踩过**）。★ 探针**第一版**又踩同坑：认不出 `第 24～29 轮` ⇒ `3.0j` **被静默跳过**，输出里看起来"该查的都查了"（真值 34、它报 26）——"一个认不出某种写法的模式，报出来的是'这种写法不存在'，而它在输出里和'这种写法没问题'长得一模一样"。★ 破验 W1/W2 全咬住；W2 后第二次执行被 **590s 上限杀掉、`finally` 没跑到、源文件留在变异态**——**哨兵当场报出来**，按备份还原并复核。★ 顺手修两条**悬空行号引用**（`checkPermission` 2351→2363；「写侧只有一个入口」`server.mjs:7296` → `routes/tool-calls.mjs:61`），成因是**另一会话正在重构 `server.mjs`** ⇒ "一条行号引用与它指的东西之间，隔着一份**别人正在改的文件** —— 而'引用漂了'与'被引用的东西没了'，在读数上长得一样"。⚠️ **`PRT-PROGRESS.md` 被另一会话 `git add` 了**（我改的两处被卷进他们的暂存区）⇒ 本次**不提交那个文件**，本提交的干净检出上那条判据仍会红，直到他们下次提交带上 ⇒ **两个写者共树的必然，如实记下** | `scripts/prt/boundary-facts.mjs`、`docs/superpowers/prt/PRT-FINAL-REPORT-2026-09-18.md`、`scratch/_mutate-r52-report.mjs` |
| ★★★ **第 50 轮** | **第 49 轮那条规则的"已知残留"当天就兑现了** —— 我写下的残留是"两条相邻、同类、都不声明裁决处的条目看不出来"，而 `product/release/checklist.mjs` 的"同上"紧挨着 `product/metrics-spec7.mjs`（一个**指标口径**模块），仅因为"都是 gap、都写 §5 第 16 条"被放行 ⇒ "一条判据的'已知残留'如果**当天就能兑现**，那它不是残留，是它**允许**的一种错"。⇒ 改成**严格**：**一条位置指称都不许**，**21 → 0 条**、无残留；14 条全部改成 **指名 + 量出来的事实**。★ 而"量出来"逼出第二课：探针第一版走进 `.worktrees/`（兄弟工作树）且按**文件名**匹配（`from './context-retention.mjs'` 被算成 `retention.mjs` 的 import 者）—— "一个按**子串**匹配的探针，报出的是'某个名字里含这个名字的东西'，而它和'这个名字本身'在输出里长得一模一样"；★ 同坑第 49 轮已踩一次 ⇒ 这版把"路径分段匹配"与"跳过兄弟工作树"写成显式并**断言**其生效。修好后量到：7 条零生产 import 者；`compiled-plan.mjs` 两个 import 者自己也不可达。破验 Y1/Y2 全咬住。★ 本轮先读全 §5 第 28 条，确认 L7/L9 是产品+架构的**三选一决定**（甲/乙/丙），**不是缺实现** ⇒ 保持不动 | `scripts/prt/reachability.mjs`、`docs/superpowers/prt/prt-reachability-baseline.json`、`scratch/_probe-importers.mjs`、`scratch/_mutate-r50-positional.mjs` |
| ★★★ **第 49 轮** | **一张声明表里 21 条理由靠"位置"说话** —— `prt-reachability-baseline.json` 的 `reason`（44 条里 21 条用"同上"/"上面那条"）。★★ **而有一处已经指错了**：`runtime/toolcall/spool.mjs` 写着「它的消费者就是**上面那条**收账侧」，而"上面那条"今天是 `runtime/packs/store.mjs`（裁决处 **§5 第 19 条**），真正的收账侧在下标 **2** —— "一条相对指称在表里插进一行无关条目之后，会**改变另一行说的话**，而不改变那一行的任何一个字"。量出 **5 条违规**（3 处"同上"想指的是**同组但不紧邻**的条目）⇒ 新增 `positionalReasonViolations()`（`no-predecessor`/`class-mismatch`/`owner-mismatch`）+ 8 处改为**指名引用**。★★ **变异抓出我自己的一条假控制**：`X2` 没咬住 —— 用例里测"裁决处不同"的那条上一条是 `by-design`，**先**被"类不同"抓住 ⇒ 那一支从没被任何用例碰到过（"一条'顺便也被另一种情形覆盖'的用例，与一条'真的覆盖了这一种情形'的用例，在绿灯下没有区别"）。★ 探针第一版也报过一个**假问题**。★ 已知残留：相邻同类且都无裁决处的两条，本规则看不出"同上"是否同组（修完后 14 条全落此形态）| `scripts/prt/reachability.mjs`、`docs/superpowers/prt/prt-reachability-baseline.json`、`scratch/_probe-positional-reason.mjs`、`scratch/_mutate-r49-reason.mjs` |
| ★★★ **第 48 轮·续** | **第五份**手写解析 —— `feature-table-status.parseFeatureRows()`，由一次**全仓扫**（`!= 5 && != 6`）找到，而不是"逐个文件找"。★★ 而这一份**不太一样**：它把跳过**报出来**（`skipped`，第 23 轮 F-15 的成果）⇒ 缺陷不是"静默少读"而是**归错类**：4 格 / 7 格的真功能行被说成"另一张表"，那一行的状态与「还差什么」**都不再被管**，而报告里它只是"已跳过 16 行"里的一个数 —— "一份'跳过并报出来'的清单比'静默跳过'好得多，但它仍然可能把**自己看不懂的东西**说成**别人的东西**"。⇒ **所有者说是行就是行**（分类权在所有者，报告权在本模块）。★ 破验 W4 咬住；真文档 `rows=29 skipped=16` 改前改后一致。★★ **全仓现无一份手写**。★ 另清掉一个**空的承诺**（`feature-landing-paths.FEATURE_ROW_RE` 零使用者）| `scripts/prt/feature-table-status.mjs`、`scripts/prt/feature-landing-paths.mjs`、`scratch/_mutate-r48-landing.mjs` |
| ★★★ **第 48 轮** | **功能对照表的第四个手写解析器** —— `feature-landing-paths.parseLandingCells()`。量（`scratch/_probe-landing-owner.mjs`）：真文档上四个解析器**一致**（都是 29 行），但 4 格 / 7 格的功能行被它**静默跳过**。★★ **后果比第 47 轮更重**：该模块的职责是"声明的代码落点必须指向存在的文件"，一行被跳过 ⇒ **它声明的路径一个都不会被核**，而门禁报"全部通过" —— "一个'这一行我没看懂所以跳过'的默认动作，与'这一行真的没问题'，在输出里都是'没有报错'"。⇒ 把**行识别器**也放进叶所有者（`featureTableRow` + `FEATURE_ROW_MIN_CELLS`，与 `ledgerTaskRow` 对称），**四个消费者全部转手**；其中 `spec-status-calibration` 里是**同一文件两处**各自手写（第 47 轮只对齐了阈值，这一轮连识别权一起交出去）。★ 破验 W1/W2/W3 全咬住（29/29、15/15）| `scripts/prt/progress-check.mjs`、`scripts/prt/feature-landing-paths.mjs`、`scripts/prt/spec-status-calibration.mjs`、`scratch/_probe-landing-owner.mjs`、`scratch/_mutate-r48-landing.mjs` |
| ★★★ **第 47 轮** | **功能对照表的第三个手写解析器** —— `spec-status-calibration.parseStatusTable()`。量（`scratch/_probe-status-table-owner.mjs`）：真文档两解析器**一致**（25 个 F-NN / 29 行），但 ① 状态格 `🔵` ⇒ 它静默算成 **🟡**（`aggregate` 的 `其余 ⇒ 🟡`），而所有者**抛**；② 4 格 / 7 格的状态行 ⇒ 它**静默跳过**，所有者收下。★ 旁证**就在同一文件里**：`parseCalibration()` 用 `< 4` 而它用 `≠5 且 ≠6`。⇒ 判定权交回所有者 + 认不出就抛 + 阈值对齐。★★ 而本轮**把我自己第 45 轮的断言拦住了**：它要求生产消费者写出本地别名 `STATUS_RE = FEATURE_STATUS_RE`，而新消费者**直接**用那个正则 —— "一条钉住**写法**的断言，会在写法被换成一个**更好**的写法时变红" ⇒ 改成钉**性质**，并复跑 F1 确认仍咬住。★ 破验 V1/V2 全咬住（15/15、19/19）| `scripts/prt/spec-status-calibration.mjs`、`scripts/prt/feature-table.test.mjs`、`scratch/_probe-status-table-owner.mjs`、`scratch/_mutate-r47-status-table.mjs` |
| ★★★ **第 46 轮** | **同一个问题在本仓被回答了两遍** —— `tallyLedger` 是台账行规则的**第二个所有者**。量（`scratch/_probe-tally-owner.mjs`）：真台账两所有者**今天一致**（145 = 140/1/4/0），但 ① 四种脏格子里 **3 处**接受规则分歧（`✅🟡`/`✅（待复核）`/`⏸→🟡`：所有者抛、它照收）；② 词表加第 5 个标记 ⇒ `total=5` 而四档之和 **4**。⇒ 判定权交回 `ledgerTaskRow`；"归哪一档"写进所有者那张表（`tallyKey` ⇒ 加状态只改一处）；平衡自检**不返回**不相等的读数；`marks` 可注入。★★ 顺手挖出**闸门自身**的潜在缺陷：`handover-ledger-tallies` 两侧用 `JSON.stringify(对象)`，于是**对象的插入顺序**成了一条没人打算立的规矩 —— 改动后当场报红而**五个数一个都没变** ⇒ 加 `canonicalJson()`（键按排序）。★ 破验 T1/T2/T3 全咬住（49/49）。★★ **T3 被强杀** ⇒ 源文件留在变异形态（第 44 轮后第二次）⇒ 给变异脚本加了**哨兵**（它下一次的症状「锚点命中 0 次」与"锚点打错字"同形） | `scripts/prt/boundary-facts.mjs`、`scripts/prt/progress-check.mjs`、`scratch/_probe-tally-owner.mjs`、`scratch/_mutate-r46-tally.mjs` |
| 全量 CI（**第 45 轮中段**） | ⚠️ **8/9，exit 1** —— `test` 红（HEAD `0f0985c`，`.ci/r45b`，脏树 21 改 + 509 未跟踪，指纹 `c746d564abe798ce`） | `test` 1342610ms、`skipped=1`。★ 6 个红族同样全在另一会话：`suite-counts` 过期的那**唯一**一条是 `compaction-http.test.mjs`（F-17）——我特意核过 `git grep "<我改的每个套件>（"` **全部没有被声明计数**；`env` 一次瞬时红报的是 `team-hub/routes/secrets.mjs`（它新建、还没 `git add`） |
| 全量 CI（**第 45 轮**） | ⚠️ **8/9，exit 1** —— `test` 阶段红（HEAD `60810c4`，`.ci/r45`，**脏树 18 改 + 498 未跟踪**，指纹 `c61b4d166631338d`） | `test` 974483ms、`skipped=1`（secret-store，已知）。★ 红的仍是**同一批**：`*-dsh-process` × 3 + `runtime-contract-cross-process`（另一会话 `2f5a4b3` 的回归，净树复现）+ `model-api`（**脏树**造成的）⇒ "同时红 ≠ 同一个原因"。★★ `env` 那次 FAIL 是**瞬时**的：单独复跑 `--only env` 是 **PASS（3684ms）**——CI 从另一会话的改动中间跑了过去 ⇒ "CI 报红"与"代码是红的"也是两个读数 |
| 全量 CI（**第 44 轮**） | ⚠️ **8/9，exit 1** —— `test` 阶段红（HEAD `1ce5b90`，`.ci/r44`，**脏树 26 改 + 483 未跟踪**，指纹 `2aff62dcfe0f4d42`） | `test` 1068676ms、`skipped=1`（secret-store，已知）。★ 红的是**另一个会话的 `2f5a4b3` 引入的 4 族回归**（`*-dsh-process` × 3 + `runtime-contract-cross-process`），**净树复现**；不在本会话改的面上，详见 `PRT-HUMAN-INTERVENTION-2026-09-20.md` §二之二 |
| 全量 CI（**交付 HEAD**） | **9/9 PASS，exit 0**（HEAD `eeedd21`，`.ci/r43`，**脏树 14 改 + 471 未跟踪**，指纹 `cacda41048a5a459`）★★ 这是**最近一次** 9/9 —— 第 43 轮收口。第 44～47 四轮都是 **7～8/9**（见上面各行），所以"交付 HEAD"仍是它 | `test` 1088879ms、`skipped=1`（secret-store，已知） |
| 全量 CI（**第 47 轮封板**） | ⚠️ **7/9，exit 1** —— `env` + `test` 红（HEAD `4276bf2`，`.ci/r47`，**脏树 16 改 + 522 未跟踪**，指纹 `4829823fcd9d4831`） | `test` 1025504ms、`skipped=1`。★ **9 个红项逐条取证，没有一项在本会话的改动上**：`suite-counts` 的 5 条过期声明**全部**是另一会话的 `*-http.test.mjs`（`automation` 9→18、`compaction` 5→13、`pack-facts` 8→17、`role-pack` 10→21、`experience` 14→24）——我特意核过**我这两轮动过的 7 个套件一个都没被声明计数**；`config`(52/53) + `prt-baseline`(23/25) + `*-dsh-process` × 3 + `runtime-contract-cross-process`（`2f5a4b3` 回归、净树复现）+ `model-api`（脏树）全是那边；`env` 报的是 `team-hub/routes/connectors.mjs`（它新建、还没 `git add`）。★ 本轮 `doc` **PASS**（上一轮它红的 slice-07 banner 已被那边补上 ⇒ 又一次证明那个红是它们的） |
| 全量 CI（**第 51 轮**） | ⚠️ **6/9，exit 1** —— `test` + `smoke` 红（HEAD `acffc8d`，`.ci/r51`，**脏树 19 改 + 564 未跟踪**，指纹 `a126fd4d6e50394e`，`test` 2486033ms，skipped=1）。★★★ **而这一次的读数比前几次更不可信，请按这个限定读它**：跑的时候**我自己在改树** —— `test` 阶段耗时 **41 分钟**，期间我落了三批编辑 ⇒ `boundary-facts` 那 3 条红是**改到一半的瞬间快照**（那批编辑恰好把清单里的 `boundary-facts 49→51` 改了、而总数还留在旧值），**当前树本地 `boundary-facts` 51/51 + CLI 21/21 全绿**。另 4 个套件（`run-plane` / `event-delivery` / `context-tokenizer-wiring` / `p13-host-injection`）**300s 超时被杀**，而当时我正**并发**跑自己的套件扫描。⇒ 这一行只记"某一瞬间的那棵树"，**不当作对任何一个提交的判决**。★ 相比 `.ci/r47`：`env` 已转绿（另一会话把那个未跟踪文件提交了）、`config` 与 `prt-baseline` 已修好；`suite-counts` 与三条 `*-dsh-process`、`runtime-contract-cross-process`、`model-api` 仍在（逐条归属见 §二之二） | `test` 2486033ms |
| 全量 CI（第 34 轮收口） | **9/9 PASS，exit 0**（HEAD `ff8d9f7`，`.ci/r34`，脏树 14 改 + 446 未跟踪） | `test` 873594ms |
| 全量 CI（第 33 轮收口） | **9/9 PASS，exit 0**（HEAD `af3a440`，`.ci/r33`，脏树 14 改 + 444 未跟踪） | `test` 877363ms |
| 全量 CI（第 32 轮收口） | **9/9 PASS，exit 0**（HEAD `89590fc`，`.ci/r32b`，脏树 14 改 + 443 未跟踪） | `test` 873289ms |
| 全量 CI（第 31 轮收口） | **9/9 PASS，exit 0**（HEAD `741d037`，`.ci/r31b`，脏树 14 改 + 440 未跟踪） | `test` 875250ms |
| 全量 CI（第 30 轮收口） | **9/9 PASS，exit 0**（HEAD `35214c8`，`.ci/r30`，脏树 14 改 + 438 未跟踪） | `test` 875135ms |
| 全量 CI（第 29 轮收口） | **9/9 PASS，exit 0**（HEAD `cef163b`，`.ci/r29b`，脏树 14 改 + 436 未跟踪） | `test` 870442ms |
| 全量 CI（第 28 轮收口） | **9/9 PASS，exit 0**（HEAD `5aa61df`，`.ci/r28b`，脏树 14 改 + 433 未跟踪） | `test` 872428ms |
| 全量 CI（第 27 轮收口） | **9/9 PASS，exit 0**（HEAD `dd6eb8f`，`.ci/r27b`） | `test` 873171ms |
| 全量 CI（第 26 轮收口） | **9/9 PASS，exit 0**（HEAD `4ccdf86`，`.ci/r26b`） | `test` 937905ms |
| 全量 CI（第 25 轮代码收口） | **9/9 PASS，exit 0**（HEAD `bacd407`，`.ci/r25`） | `test` 824992ms |
| 全量 CI（第 23 轮代码收口） | **9/9 PASS，exit 0**（HEAD `3624e96`，`.ci/r23b`） | `test` 807541ms |
| 全量 CI（第 22 轮，上一轮读数） | **9/9 PASS，exit 0**（HEAD `466239d`，`.ci/r22a`） | `test` 811934ms |
| 套件清单完备 | **376 个 `*.test.mjs` 全部有归属**（357 → 359 → 360 → 361 → 362 → 363 → 364：第 23 轮两套、第 25 轮一套、第 26 轮零套、第 27/28/29/31 轮各一套；→ **373**：第 41 轮一套；→ **374～376**：另一会话的 PRT-316 切片持续新增） | `stageTest` |
| 第 23 轮新套件 | `budget-alert` **29/29**（20 例求值 + 9 例真 HTTP；**9 处变异逐条咬住**） | `team-hub/budget-alert{,-http}.test.mjs` |
| 第 24 轮（坐标判据） | `boundary-facts` **PASS** + 自身套件 **36/36**（34 → 36） | `scripts/prt/boundary-facts{.test,}.mjs` |
| 第 25 轮（计数判据） | 新判据 `suite-counts` **PASS**；主表计数声明 **32 处已核、0 处不符**；自身套件 **10/10**；变异 **4/4** | `scripts/prt/suite-counts{.test,}.mjs` |
| 第 26 轮（报告自检） | `boundary-facts` **PASS**（参与比对 **19/19**，红 0）+ 自身套件 **43/43**（36 → 43）；抓到 **1 处真漂移** | `scripts/prt/boundary-facts{.test,}.mjs` |
| 第 27 轮（表内自洽） | 新判据 `feature-table-status` **8/8**；功能表 **2 行**「没做完却说不差什么」已订正（F-22 / F-24）；变异 **5/5** | `scripts/prt/feature-table-status{.test,}.mjs` |
| 第 28 轮（读数的适用范围） | 新判据 `ci-reading-integrity` **16/16**；**当场红在自己报告的那一行上**；`run-ci.mjs` 开始把**树的状态**记进 `summary.json` | `scripts/prt/ci-reading-integrity{.test,}.mjs` |
| 第 29 轮（跨文档校准） | 新判据 `spec-status-calibration` **13/13**；**目标点名的输入文档**里 **7 条**过期状态注记已建校准表；变异 **8/8** | `scripts/prt/spec-status-calibration{.test,}.mjs` |
| 第 31 轮（落点可解析） | 新判据 `feature-landing-paths` **13/13**；**恰好 1 处**真缺陷（F-21 的短路径）已订正；变异 **7/7** | `scripts/prt/feature-landing-paths{.test,}.mjs` |
| 第 35 轮（✅ 的证据） | 台账 **140 条 ✅ 的"可复跑证据"必须解得开**（套件别名要真的是一行 CI 套件 / 带目录路径原样存在 / 裸名全仓唯一）：读数 套件 96 + 路径 33 + 裸名 39，抓出 **3 处歧义裸名**（已修文档）；★★ 顺带抓出并修掉**我自己**的两个坑：① 探针把"套件别名"当文件名 ⇒ **40 个假发现**（本族第 6 次误报）；② `--only boundary` **不跑** `boundary-facts` ⇒ 第 34 轮的收尾把一个**红的**判据发了出去 + 新判据 `stage-scope`（判据上线第一次就抓住了自己）。套件 **365 → 368**；★★★ 还咬出**我自己制造的假读数**：一个用例的**名字**改写了 CI 的读数（`tests=21 pass=20 skipped=1`，真值 10/10/0/0）⇒ 解析抽成 `scripts/ci/parse-suite-output.mjs` 取**最后**一个匹配 + 摘要行报 `⚠计数被输出干扰` + 根因守卫扫全部用例名 | `scripts/prt/ledger-evidence.mjs`、`scripts/prt/stage-scope.mjs`、`scripts/ci/parse-suite-output.mjs` |
★★ **第三段：两处"只写了一半"**（一处是**我自己的**）。**Lead A**：我在上一段的注释里**已经**写下「外加 `NON_DONE_STATUSES` 那张子集表」，**却只改了前两处** —— 而那段注释**已经把第三处写成"已收敛"** ⇒ 后果**具体**：`ledgerNotDone()` 少收一条 ⇒ 那条非 ✅ 的任务**不出现在人工介入清单里** ⇒ **"清单里没有它"与"它已经完成了"读数同形**。★★ 修它时又撞上同一个形状：**"派生"不能靠"读结果"确认**（手写同序的表与派生结果**逐字节相等**）⇒ 把派生做成**可注入的纯函数** `nonDoneStatuses(marks)`。**Lead B**：同一模块两条判据都管"生成物里不许出现状态词"，各用一份表（6 项 / 5 项），而**给它们背书的普查用的是第三份**（并集 9 项）—— 判据 B 漏**四个标记全部**，而它的 `why` **正是**在引用那次普查当理由 ⇒ 先量再改（窄表与并集今天读数**都是 0**，差异只存在于理论上，**而这正是最该修的时候**）⇒ 共用一份由台账词表派生的 `GENERATED_STATUS_VOCAB`，普查改 import ⇒ 破验 **D1/D2/D3 三条全咬住** | `scripts/prt/progress-check.mjs`、`scripts/prt/intervention-coverage.mjs`、`scripts/prt/boundary-facts.mjs`、`scratch/_probe-generated-status-vocab.mjs`、`scratch/_mutate-r45-derived.mjs` |
★★ **同一形状在第二份文档上又遇到一次**（本轮续）：`feature-table.mjs:59` 明写「状态词表**已经有一个所有者**」并把所有者指到**用例** ⑥ —— 话是对的，但**生产脚本没法 `import` 一个 `.test.mjs`** ⇒ `feature-evidence.mjs:49` 自己抄了一份（7 种），而用例 ⑥ 许可 **20** 种 ⇒ **13 种被静默丢掉**（实测：喂 `🟡→⏸` ⇒ 收 0 行、不报错；而真文档只用 5 种、全在交集里 ⇒ 差异当时**只存在于理论上**）⇒ 修法同上（词表提到模块 + 派生 + 认不出就抛；阈值 `>=4` 是**量出来的**：收下的 29 行全 ≥4 格、16 行非状态格的全恰 3 格、3 格且是状态的行 **0**）⇒ 破验 **F1/F2/F3 三条全咬住**（`10/2`、`11/1`、`24/2`）。⚠️ 而本轮**自己踩了两个同类的坑**：① 判据**先扫到记录这个缺陷的注释**⇒假红（**那个文件开头自己就写着**"这正是不该让判据去扫源码的原因"）；② 名单里**照抄真路径**⇒被 `criteria-files-do-not-impersonate-manifests` 与可达性探针读成"清单声明"、当成**生产入口**⇒处置是**删掉名单改为扫目录**（手写名单会漏掉新消费者，**而漏掉的那一个不会报错**） | `scripts/prt/progress-check.mjs`、`scripts/prt/feature-evidence.mjs`、`scripts/prt/feature-table.test.mjs`、`scratch/_probe-feature-status.mjs`、`scratch/_mutate-r45-feature.mjs` |
| 第 45 轮（状态词表**一处所有** —— 以及**怎么证明**"一处所有"这件事） | ★★★ **一个真缺陷的两处余党 + 一条有分辨力的判据**。第 44 轮修掉了 `tallyLedger` 认不出 🟡，但那只是**一处**；本轮把问题做成**可证伪**的：往合成台账里放一个今天**不存在**的第 5 个标记（🔵），看每个解析器的**行数**有没有少（`scratch/_probe-status-poison.mjs`）⇒ 读数：**两个**解析器静默丢行（`ledgerEvidenceRows` 4→3、`ledgerRows`／`ledgerRowTexts` 4→3），而 `progress-check` 记 `status=null` 由下游报"读不出来"（正确）。★ 而 `intervention-coverage.mjs` 的**文件头自己**就写着「两处各写一遍取法，正是本模块警告的那种漂移」——**然后它把状态正则写了三遍**。**修法**：`progress-check.mjs` 成为台账格式（含状态词表）的**唯一所有者**（`LEDGER_STATUS_MARKS = STATUS_MARKS.map(...)`、`STATUS_CELL_RE` 派生、`ledgerTaskRow(line,{marks})` 可注入），三个消费者取它且**认不出来就抛**。★★★ **本轮最要紧的一步是"怎么证明它"**："🔵 会抛"**区分不了**①查那张表 ②自己抄一份——两者在今天行为**完全一样** ⇒ 破验 `scratch/_mutate-r45-vocab.mjs`：**M1**（往唯一所有者加 🔵 ⇒ 三个消费者必须**都**收下；✔ `2/2/2` 零抛出）、**M2**（把 🟡 删掉 ⇒ 真台账必须读不动；✔ 两个都 `THREW`）、**M3 假变异检验**（把一个消费者改回手抄 + 加 🔵 ⇒ M1 必须**不再**通过；✔ `tally=2 rows=2` 但 `evidence=null`）——**M3 最要紧**，因为前两条只说明"今天的实现通过"，**不能**说明 M1 有**分辨力**。★★ **一条"两个实现必须一致"的判据在两者一起错时是绿的**：⑬b 本来就在比 `ledgerRowTexts` 与 `ledgerRows` 的编号集合，**它抓不到**这个缺陷（两边以**同样的方式**各丢一行）⇒ 新增 ⑬c 直接钉"两个取法都必须抛"。★ 破验自己踩两个坑（都是"同一个读数"）：**CRLF**（`progress-check.mjs` 497 CR/497 LF，锚点用 `\n` 写 ⇒ `split` 静默命中 0 次）与**子进程没跑起来**（绝对路径 `import` 在 Windows 上要 `file:`，崩了却报成 `Cannot read properties of undefined`）；★ 探针本身也**假绿过两次**（`parseProgress` 返回对象被读成"1 行"；合成台账没有 `## 阶段` 标题 ⇒ 数出 0 行，"0 vs 0"是假对照） | `scripts/prt/progress-check.mjs`、`scripts/prt/ledger-evidence.mjs`、`scripts/prt/intervention-coverage.mjs`、`scripts/prt/boundary-facts.mjs`、`scratch/_mutate-r45-vocab.mjs` |
| 第 44 轮（PRT-009 `peak-resource` 的"剩下那一半" + **§9 链上唯一的硬断没了**） | ★★ **功能 + 判据同步 + 一条我自己的错判更正**。① **端到端真读数第一次走通**：起一台真子进程（真实 140MB）→ 真采样器 → 真 `status()` → 逐字复刻 launcher 那句映射 → 真落盘 → 真读回，`peakWorkingSet=194.5 MiB`、`cpuMs=47`、`samples=1`，磁盘读数与内存**逐字段相等**、退出恰一条日志（此前两条证据**都不是端到端**：一条只到采样器、一条用假 io）。② **抓到"只在失败路径才印"的读数是反的**：`handleExit` 里 `stopping \|\| disposed` 那条分支**直接 return**，`reportPeakResource()` 只在**非主动退出**时走到 ⇒ 并排实测 A 主动停止 **0 条日志** / B 非主动退出 **1 条**，再叠加 `forgetRunRecord()` 正常停止后删记录（有意）⇒ **一次成功 Run 的峰值两处都不留**；修后 A 1 / B 1（`supervisor.test.mjs` **22 → 25/25**，破验 **5/5**）。③ **我自己往台账格子里引用了一行带 `\|\|` 的代码** ⇒ 表格被切开，CI `doc-table` + `prt-progress` 当场判红（"第 33 行有 6 条未转义的竖线"）⇒ 改写成不放竖线的说法；**定责靠读数**：`git worktree add --detach <我那个提交>` 在**只有我的提交**的树上复现同样的红。④ ★★★ **而我修这个缺陷时自己犯了同一类错**：看到另一个会话已把它转义成 `\|\|`，**凭印象**断言"反引号里反斜杠是字面字符 ⇒ 引用错了代码"——查 GFM 规范才知**恰好相反**：`\|` 在**任何 inline span 里**都被渲染成一个竖线（规范原文 "including inside other inline spans"），那个修法本来就是对的。★ 更值得记的是**判据里早就有答案**：`progress-check` 有一条用例写的就是"转义成 `\|` 之后必须**不**报"，它当时是绿的，我读了它的失败信息却没读那条。⑤ **`2f5a4b3`（另一会话，§5 第 20 条 · 甲）让 §9 链上唯一的硬断消失**：`PATCH_LAYER_ROWS` 5→**7** 行 ⇒ 探针读数变 `L5 ✔ 有活实现`、`最先硬断的一节：（没有）`；探针自己的 `owner-stale` 报"L5 今天没有断点却还写着归属第 20 条 ⇒ 那个指针该删"⇒ 按它说的删；两处写死的期望**翻过来而不是删掉**（①"是 L5"→"没有"：删掉则下一条硬断出现时**没人在看**；⑨"恰三节声明归属"→"恰两节"，并**保留**"有断点的节 == 声明归属的节"那条反向对照，让数字由断点集合算出来）⇒ `alpha-chain-trace` **12/14 → 14/14**。⑥ ⚠️ **本轮不能给出"9/9 PASS"**：`test` 阶段红，红的 4 族（`*-dsh-process` × 3 + `runtime-contract-cross-process`）是 `2f5a4b3` 引入的、**净树复现**；★ 用 `worktree` 分树复跑还纠正了一处会写错的定责——CI 日志里 5 族**同时**红看着像同一原因，其中 `model-api` 在**净树上是 15/15 绿的**，是**脏树**造成的："**同时红 ≠ 同一个原因**" | `product/launcher/supervisor.mjs`、`scripts/prt/alpha-chain-trace{.test,}.mjs`、`scratch/_probe-peak-e2e.mjs`、`scratch/_probe-peak-on-stop.mjs`、`scratch/_mutate-r44.mjs` |
| 第 43 轮（**豁免表空了**：剩下两条也收回，两次都是"修好代码"而不是"继续豁免"） | ★★★ **两处真缺陷 + 一个不可证伪性质的第二次消除**。第 41 轮留的 3 条豁免，第 42 轮收回 1 条，本轮收回剩下 2 条 ⇒ `DECLARED_MIRRORS` **空**。做法是**先量不先信**：`git grep` 数每个名字的读者点（`scratch/_probe-exempt-consumers.mjs`），发现 `PREFLIGHT_VERDICTS` 的**唯一读者就是我自己的豁免条目**、`BPE_ARTIFACT_FIELDS` **没有任何人读它的成员**。① `BPE_ARTIFACT_FIELDS` 声明 6 个字段而产物有 7 个——**`ranks` 没被声明**，而 `bpe.test.mjs` 只断言"名字在不在"⇒"它声明的是产物的字段清单"是空话 ⇒ 清单升格成唯一形状判据、构造处校验（少了/多了都抛）并可注入（`bpe` 27→32）；② `PREFLIGHT_VERDICTS` 在汇总里被手写复述两行 ⇒ 往词表加第四个裁决 `degraded` 后实测 `ok=true`、`blocked=[]`、`unknown=[]`、`reasons=[]`，而**同一份返回值**里 `remedies.disk` 还在给处置建议 ⇒ 归类总函数化 + `classifyPreflightChecks()`（归类表可注入），未声明/无处置当场抛（`preflight` 16→21）；修完后 `PREFLIGHT_VERDICTS` 有了真消费者 ⇒ R2 把最后一条豁免判成过期 ⇒ **238 张表、装饰 0 张、豁免 0 条**。★★★ 破验首轮 **10/12**，两条漏网**原因一模一样**：A1（汇总改回手写两行）与 B2（删掉构造处校验）都是**行为等价**——今天词表正好是那三个词、产物与清单恰好一致，**任何行为用例都分不开** ⇒ 处置不是加断言，而是**把被跟随的声明做成可注入的参数**，用例才能拿第四个裁决/坏清单去试 ⇒ **12/12 咬住 0 漏网**（与第 42 轮同出一辙）。★ 我顺手新写的 `PREFLIGHT_VERDICT_KIND_NAMES` 被 R1 **当场判成装饰表**（声明了却没有遍历点、成员被手写 2 次）⇒ **删掉，而不是写第四条豁免** | `product/upgrade/preflight.mjs`、`runtime/context/bpe.mjs` |
| 第 42 轮（把上一轮**自己发的豁免**收回：`WRITABLE_ROLES` 不是耦合，是两条静默失效路径） | ★★ **真缺陷修复 + 一个"不可证伪"性质的消除**。第 41 轮我把 `WRITABLE_ROLES` 列进 `declaration-mirrors` 的豁免表（理由"手写映射无法机械派生，属已知耦合"），本轮按自己写的"需另行裁决"去裁决，**结论是豁免错了**：`product/paths.mjs` 两张声明表**各有一份手写复述**，两份都有静默失效路径（实测 `scratch/_probe-path-roles.mjs`）——① `DIR_ROLES` 的遍历是嵌套三元、**兜底支是 `layout?.logDir`** ⇒ 加一个角色就被**当成 logDir** 检查，输出 `PATH_NOT_ABSOLUTE [role=backup] → …当前为「relative-log」`，**名字对、文案对、值是别人的**，真正的目录从未被看过；② 安装目录那条检查自己手写四元对象当清单 ⇒ 往 `WRITABLE_ROLES` 加角色，那条**安全检查根本不查它**。处置：取法收敛到 `DIR_ROLE_READERS`、两侧遍历声明、三表不对齐**一进模块就抛**（`dirRoleWiring()`），并把那条检查抽成可注入的 `writableDirsInsideInstall()`。读数：`paths` **18 → 26/26**（接线①…⑧）、破验 **10/10 咬住 0 漏网**、`declaration-mirrors` 装饰 **3 → 2** 张。★★ 豁免**会过期**且这次是**判据自己**报出来的（R2 `stale-exemption` 拦的是写它的人）。★★★ 破验两次漏网（M3/M4）暴露一个**不可证伪**性质：手写四个 / `DIR_ROLES.slice(1,5)` / 遍历声明在今天**结果完全等价**，任何行为用例都分不开 ⇒ 处置不是加断言，而是**把角色清单做成可注入的**，用例才能拿**第五个**角色去试；同理那条只在加载时跑一次的守卫也做成可注入的。★ 我自己的 API 错误（传一张只有 `backup` 的 readers 表替换默认表）被**错误文案当场叫出来**——若当时写的是"没有取法就 continue"，我会得到一个**少查四个目录**的绿 | `product/paths.mjs` |
| 第 41 轮（把第 40 轮那个形状**做成可证伪的门禁**） | ★★ **判据化 + 第三个真缺陷**。先测"那个形状有几处"：逐条打开 27 条 `gap`，写了一版量"声明表有没有被遍历"的判据，**报了 366/702 张**——`*_CODES`/`*_ERRORS` 这类**字典**本来就该按 `CODES.FOO` 取值 ⇒ **本族第七次误报**（"没被遍历" ≠ "没有消费者"）。收窄到"注释里**自己承诺**可扩展"的表后全仓**只剩 1 张**（就是第 40 轮修掉的那张）⇒ 那条矿脉采完，于是把形状做成门禁 `scripts/prt/declaration-mirrors.mjs`（CI 第 **18** 套）。★ 口径只认**键位**复述（`member:`/`, member,`）——认"字符串出现过"会报 **17 张**假阳性（枚举值在**值位**是正常使用）；门槛"≥2 且 ≥半数"；**自证**（修复前必须被报出、修复后必须放行）；豁免必须写 why(≥20字)+owner 且**过期判红**。读数 238 张表/3 张装饰/12 例全绿/变异 **12/12**。★ **第三个真缺陷**：`team-hub/budget-alert.mjs` 的 `BUDGET_ALERT_CONFIDENCE` ↔ `ACTION_FLOOR_FOR_CONFIDENCE` 漂移是**静默**的——未登记置信度查表得 `undefined`，`strictestAction(fromLevel, undefined)` 把它当成**新档位** ⇒ **动作下限凭空消失而一行错都没有**；改成遍历词表、缺项**具名上抛**。★ 两次破验漏网都留档：M3 是**夹具没构造出键位形状的注释**（`// data:` 前面有 `// ` 本就匹配不上）；M4 是"整行跳过声明"那道守卫**走不到**，但**不是无害**——声明与真代码**同一行**时会把真凭据一起丢掉 ⇒ 改成"只挖声明那一段"。★ **性能也是判据**：第一版每名字一次 `git grep` = 238 次子进程 / **236 秒**，改成一次调用 ⇒ **1.9 秒**（快 124 倍）。★ 未动 `product/launcher/launcher.mjs`（另一会话在制品） | `scripts/prt/declaration-mirrors.mjs` |
| 第 40 轮（`RUN_RECORD_OPTIONAL_FIELDS` 声明表**没有消费者**） | ★★ **功能修复**：把 PRT-009「峰值读到不了磁盘」那条链**逐段走完**（此前是推断）⇒ 生产者 `supervisor.mjs:508` **已在产出**、记录层收、校验层认，**中间那一跳** `launcher.mjs:1261-1267` 就是丢的地方（该文件是**另一个会话的在制品**，未动）。★ 走这条路撞到**我自己的**文件里一个真缺陷：`RUN_RECORD_OPTIONAL_FIELDS` 是一张 ★★★ 声明表（注释写"新读数一律加在这里"），而 `buildRunRecord` 与 `validateRunRecord` **各自手写** `'peakResource'` 这个名字 ⇒ **那张表没有任何机械消费者**。**照注释做一次**的实测后果：生产者交 `diskUsageBytes` ⇒ **写不出去**、**也不被校验**，而记录**看起来完全正常**（`problems` 为 `[]`）。处置：两侧改成**遍历声明**，声明了没登记 ⇒ **具名上抛** `RUN_RECORD_FIELD_NOT_WIRED`（**不跳过**）；新增 `recordWiring()` 报**四个方向**（含"登记了却没声明"——另一个方向的静默）。读数：`run-record` **47 → 53**、变异 **9/9**、launcher 全部 **30 套件/688 例**绿。★★ 两条反向控制：旧记录仍算好记录（否则**孤儿进程清不掉**）、`peakResource` 不许挪进必填表。★ 本轮我**三次误报**（口径太窄，与第 39 轮同族第四到第六次）：把 `data-classes.mjs` 判成没用例（其实被两套覆盖）、把"只有文档/模块路径的 50 条 ✅"判成薄弱（其实都是真落点）、F-12 以为没被校准（只读了表前六行） | `product/launcher/run-record.mjs` |
| 第 39 轮（spec §7 第 6 条六指标**落地**） | ★★ **功能**（不是判据）：第 37 轮我写"第 6 条不是完成标准"——**前半句对，后半句读窄了**；「不是退出条件」≠「不该实现」。★ 起因是我自己的**假阴性**：第 38 轮按关键词量（`eventLoss\|遗漏\|rollbackRate\|/api/metrics`）**全部零命中**，而 `product/metrics.mjs`（spec §6.6 的 9 格 + 34 例）、`metrics-source.mjs`、`upgradeResultMetric()` **早就在**——**按关键词搜代码的探针与"仓库里没有"，在零命中这个读数下同形**。真缺口：§7 六格**一格都不在**§6.6 那九格里，而原始数据（`event_deliveries`/`run_attempts`/`permission_requests`/`budget_reservations`）**已在库里** ⇒ 缺的是**汇总读出口**。交付 `product/metrics-spec7.mjs` + `-source.mjs`：**6 条要求 → 8 格指标 + 真 SQL 生产者**，17/17 用例、13/13 变异。三条纪律：分母为 0 是"还没观察过"**不是 0%**；★ **分子分母必须同一批人**（超限率分母是"结清过用量"的预留）；**"没这张表"与"表是空的"、"没有生产者"与"这次读不出来"都要分开**。★ 刻意**不**并进 §6.6 那九格——那是**远程心跳的允许名单载荷**，加键=加数据外流，该由人点头 | `product/metrics-spec7.mjs` |
| 第 38 轮（spec §2 不可突破的边界） | 目标文档 §2 那 5 条「不可突破的边界」**整仓里只有它自己提到过**：新判据 `design-boundaries` **12/12**（变异 **11/11**）要求每条边界**都有人认领**、机械判据**必须真的跑且真的查了东西**（扫到 0 个文件 ⇒ 红）、设计档**必须写出为什么它没有机械形状**。**逐条量下来：4 条可机械化，1 条如实记为设计决定**（③ worktree 不是安全沙箱——`worktree` 出现在 125 个文件里，把出现次数当违反会得到一片假阳性；实测没有任何一处代码把 worktree 与安全边界并列）。★★★ 最值钱的教训：探针第一版是**整套假阴性**——它调的 `rg` **不在 PATH 上**（ENOENT），而 `catch { return [] }` 把 ENOENT 与"没有匹配"**吞成了同一个值** ⇒ 5 条边界全报 0 命中。**"零命中"可能来自"没跑"。** | `scripts/prt/design-boundaries.mjs` |
| 第 37 轮（spec §7 测试与指标） | 目标文档 **§7** 此前**没有任何判据、也没有任何文档引用它**：新判据 `spec-tests-7` **13/13**（变异 **10/10**）把 6 条要求逐条投影 → 每条必须自报**托着 §6 哪条退出条件**（该条必须逐字出现在 §6 里）**或为什么它不是完成标准**，落点必须解得开。**逐条读下来的结论：§7 的 5 条可核对要求全部已满足**（六项契约面各有具名用例 / 隔离库对拍 / **7 类故障各有落点** / 安全 5 项 / ★ 静态检查**已有** = `dsh-boundary.mjs` 棘轮在 CI 的 `boundary` 阶段）；★★★ 而第 6 条那 6 个「持续观察」指标**不在 §6 任何一条退出条件里** ⇒ 记成**观测档 + 一处如实记录的缺口**，**不**做成红。★ 我的表第一版栽在"看起来是证据、实际谁都没查"：`budget-alert` 漏了 ``套件 `` 前缀 ⇒ 补守卫，落点 **17 → 18** | `scripts/prt/spec-tests-7.mjs` |
| 第 36 轮（目标文档自己的表） | **F-01～F-25 那张实现投影表**此前没有任何判据：新判据 `feature-evidence` **10/10**（变异 9/9）核「点名的 PRT / 套件 / 用例文件解得开」+ ★ R4「**🟡 行不许说『不差什么』**」（§5.11 记的那个真形状）；真读数 29 行 / 57 指针，抓出 **1 处真的**（F-01 的裸名歧义，全仓 2 个同名）已订正。★★ 而**同一处缺陷被第二套判据又犯了一次**：`suite-counts.mjs` 的 `name: m[1].split('/')` **把目录砍掉** ⇒ 一条准确的全路径被当成裸名 ⇒ 记 `ambiguous` ⇒ **静默跳过**（`已核 32、跳过 1`）⇒ **修文档永远消不掉那个告警**；修法是全路径优先 + 带目录归一化 ⇒ **已核 33、跳过 0**。★ 我自己的两处错也记下来：判据第一版读到 **0 行却报绿**（已补下限）、R5 削限定词去重造出 **4 处假阳性** | `scripts/prt/feature-evidence.mjs`、`scripts/prt/suite-counts.mjs`（补 3 例回归） |
| 第 34 轮（断点归属） | 链上每个断点必须**声明**一个 §5 条目编号，判据核它解得开：①断点无归属 ②指到空处 ③已过期 ④没写理由 ⑤表读不出。读数：**全部有归属**（L5→20 / L7→28 / L9→16）；★ 起因是我的探针**误报**（§5 用中文名「保留策略」指 `retention.mjs`）——本族**第 5 次**误报；`alpha-chain-trace` 8→**14/14**，变异 8→**15/15** | `scripts/prt/alpha-chain-trace.mjs` |
| 第 33 轮（§9 链投影） | 新判据 `alpha-chain-trace` **8/8**：把目标文档 §9 那条九节链逐节投影 → **L5 硬断**（= §5 第 20 条）、L7/L9 软缺口；★ 顺带修掉我自己那条判据的**序数/计数**误报（`intervention-coverage` 10→**11/11**）；变异 **8/8** + **9/9** | `scripts/prt/alpha-chain-trace{.test,}.mjs` |
| 第 32 轮（决策摘要） | 交付 `docs/DECISION-BRIEF.md`（29 条压成一页纸）；`intervention-coverage` 加"简报条数 ↔ §5 裁决表"判据 → **10/10**（原 4 例 + 新 6 例）；★ 它**当天抓到我自己第 30 轮**把第 29 条插在**空行之后**（掉到表外）；变异 **7/7** | `scripts/prt/intervention-coverage{.test,}.mjs` |
| 注释里的坐标引用 | 坏引用 **9 → 0**（另 2 处属别的会话在制品） | `scratch/_audit-citations.mjs` |
| 可达性 | 不可达 **44** 条，**全部已定性**（`by-design=13` / `gap=23` / `deliberate=8`；2026-09-20：§5 第 20 条那四条断链接上后由 48 降为 44）。★ 第 39 轮 +2：新增的 §7 指标口径与生产者**自己**被这条门禁拦下——它们出自用例、零生产入口，与 `metrics-source.mjs` 是**同一个决定**（§5 第 16 条）。**门禁逼着人对新模块说出"属于哪一类"，而不是让它悄悄可达或悄悄不可达** | `scripts/prt/reachability.mjs --diff` |
| 裁决项 | §5 清单 **1～29**（编号连续、无缺号、无重复；第 30 轮新增第 29 条） | `MULTI-AGENT-FEATURE-STATUS.md` §5 |
| 决策摘要 | `docs/DECISION-BRIEF.md` —— 条数由判据与 §5 裁决表双向核对 | `scripts/prt/intervention-coverage.mjs` |
| 文档表格 | 八份权威文档 **0 处**列错位；全仓棘轮 **87**（只许降） | 套件 `doc-table` |

★ 这一次全量 CI 也跑在一棵**冻住**的树上：`test` 阶段那 1089 秒里，
工作树的代码与文档**一个字节都没动过**——所以它是一次"提交即冻结"的读数。
（`.ci/r35b/summary.json` 的 `tree.fingerprint = 1648c0f71901ba2a` 就是这句话的凭据：
它是对 `git status --porcelain` 整份清单取的哈希，冻结意味着这个指纹在跑的过程中不变。）

★★ **"冻住"与"干净"是两件事，别看串**：
这一棵树**是脏的**（26 改 + 432 未跟踪），它只是**在跑的那 870 秒里没有继续变**。
第 24～27 轮的报告把这两件事混成了一件事——`git head` 只回答了"哪个提交"，
既没说清"哪棵树"，也没说清"跑的时候变没变"。
（第 22 轮我贴过一次**非冻结**的读数，被自己诊断出来了：*提交发生在 CI 期间
⇒ 那个读数不再描述任何一棵树*。这一轮没有重复那个错。）

★★ **2026-09-18 第 26 轮订正**：这句话原来写的是"那 **808** 秒里"。
808s 是**第 23 轮**那次 CI 的读数——第 22 轮写下这句话，
第 23 轮往上面那张表加行时**没有回头改句子**。于是同一份报告里，
表说当时那行的 825s、紧挨着的正文说 808s，**两者都长得像机器读数**。

> 一张表里已经写了三轮新读数，而紧挨着它的那句话还记着第一轮的那个数。

这类"散文里的数与表里的数不是同一个"今天有了判据：
`boundary-facts` 的 `handover-ci-prose-matches-table`（第 26 轮新增）。
**它当场就要求我把上面这个数一路改过来：825 → 869 → 938**
（每换一次交付 HEAD，散文就必须跟着表走——这就是这条判据存在的意义）。

★★ 第 23 轮那次 CI **红过两处**，两处都是**真门禁拦下的真遗漏**
（不是既有问题）：`config scan` 拦下 14 个未登记的具名码、
`prt-baseline` ④ 拦下平台契约漂移。详见第九节 9.7/9.8。
**这两处红本身就是"代码侧已做完"的一个旁证**：它们是我新加的代码触发的，
而不是旧债。

---

## 三、第 22 轮交付了什么

### 3.1 PRT-610 的**出站车道**（决策表第 19 条裁决的执行）

**缝在哪。** `team-hub/server.mjs:7296` 逐字写着「写侧只有一个入口：执行面调 `recordToolCall`」；
而执行面**调不了**它——`runtime` 进程的 `envNames` 里**故意没有** `TEAM_HUB_TOKEN`。
两条都是刻意的，合起来的结果是：

```text
toolCallLogEvidence().recorded  === false        ← 永远
release-gate.mjs 的 decisionSourceRecorded       ← 全仓没有任何产出者
```

★ 它**看起来不像坏了**：那个函数返回的是一句很谨慎、很正确的话
（"表在，但 0 条记录里没有一条带着决定来源"）。

**造了什么。** 两半，各自可单独跑绿：

| 文件 | 角色 | 套件 |
| --- | --- | --- |
| `runtime/toolcall/spool.mjs` | **写入侧**（执行面，无凭证）：逐 Run 落盘 | `toolcall-spool`，14 例 |
| `orchestrator/worker/toolcall-drain.mjs` | **收账侧**（有凭证）：收进 `tool_calls` | `toolcall-drain`，13 例 |

★★★ **环在真 SQLite 上走通了**：执行面（无 token）三次追加
（`decision` / `dispatched` / `result`，**全程无 hub 访问**）→ 收账侧调
`recordToolCall` / `markDispatched` / `recordResult` → 真库里出现带 `decisionSource` 的行
→ **`recorded` 由 `false` 翻成 `true`**。并有**否定对照**（只写不收则仍是 `false`）。

⚠️ **但生产里还没有人调这两半** ⇒ 第 15 条**没关**。缺的**不是代码**，见第四节。

### 3.2 一道新门禁：文档表格的**列数一致性**（`doc-table`，10 例）

修决策表第 23 条时发现那一行是 **6 列**（表头 5 列）：一个未转义的竖线躲在代码跨度里
（`` `string|null` ``），于是"**具体决定**"与"**不决定的后果**"各错位一格
——而那张表**就是承载裁决的表**。往下一查这不是一处笔误、是**一类**，于是变成读数。

- **八份权威文档必须 0 处**（硬断言）；本轮修掉 **13 处 / 5 个文件**；
- **全仓已跟踪 md 走棘轮**（基线 **87** 处 / 16 个文件，全在历史评审记录里），只许降；
- ★ 修法**不能**是把竖线转义：CommonMark 的代码跨度里反斜杠**不**转义。

---

## 四、★★★ 要您裁的那一件事（这是本轮最要紧的发现）

### 4.1 它不是"一个位置没定"，它是**一条通用契约**

第 22 轮原本只把第 28 条当成"spool 那个目录没定"。复核其余几项之后读到：
**它问的不是"spool 放哪"，而是"控制面的数据怎么到达执行面"。**

| 项 | 要的输入 | 今天的机制 | 也卡在这一处？ |
| --- | --- | --- | --- |
| PRT-610 落账（第 15 项） | 一次调用的决定来源 | 第 22 轮新建的落盘车道 | ★ **是**（就是本条） |
| **F-18 / F-19 执行面一半**（第 18 项） | validations / attempts / 岗位包内容 | **没有**（三个模块零生产调用方） | ★★ **是** |
| **Pack 账**（第 4 项） | 已安装的能力包账 | hub 有 `GET /api/packs/account`，**生产里没有任何消费方** | ★★ **是** |
| 范围表（第 14 项） | 读根/写根、命令/网络/MCP 授权 | **部署配置键**（已落地） | 不——缺口是人工项 **A** |
| 连接器声明（第 13 项） | 登记表 + 一次调用的成败 | **部署配置键**（已落地） | 不——同上 |

> ⇒ 这解释了一件本来会显得奇怪的事：**为什么第 18 项与 Pack 账两项，
> 代码侧各有进度，却都停在原地——它们不是缺实现，是缺同一个决定。**

### 4.2 要裁的只有一件事，而它有三条路

| | 做法 | 代价 | 与既有裁决的关系 |
| --- | --- | --- | --- |
| **甲** | 新登记一个部署配置键，两半各自从自己的 env 读 | 落在**同一个没修的数组**里——第 19 条那个人工项 **A** 今天已缺 4 把键，再添一把是**第五个受害者** | ★ 与范围表/连接器用的**同一机制** ⇒ 本条**并进 A**，**一起通或一起不通** |
| **乙** | 收账侧住进 hub 进程，路径从 hub 自己的 `dbFile` 同级目录派生 | 不新增键，但把车道位置**绑死在库位置上**，而这条隐式耦合**没有任何地方写着** | 不违反 PRT-253 §3（是从既有配置量派生），但引入一条静默耦合 |
| **丙** | 照第 19 条裁决的形状**按 Run** 交付目录（PRT-214 那五个性质） | 最贵：两侧各需一条按 Run 的缝；且 runId 必须在**按 Run 安装**的那处绑——`root-row.mjs:591` 是**进程级单例**，在那儿绑死会让整个进程只往**第一个** Run 的账本里写 | ★ **唯一与第 19 条逐字一致**的一条 |

★★ **本轮没有替您选。** 缺的是**一个决定**，而"猜一个两边都同意的路径"
正是 PRT-253 §3 明令禁止的"发明默认值"——它的后果是让"没接线"与"接好了"
在读数上**同形**，而那正是过去若干轮反复付代价的那一件事。

### 4.3 顺带说清：这一族里 Pack 账那一项**连形状都还没有**

⚠️ 不要把它们看成同一成熟度：**F-18 / F-19 的执行面一半确实建好了、有判据**
（`friction.test.mjs` 23 例、`graph.test.mjs` 15 例、`role-pack.test.mjs` 40 例）；
而 **Pack 账那一项 hub 有产出、无人接住**，消费方的形状都还没定。
两者的共同点只是**卡在同一处**。

---

## 五、需要您做的决定（按"不决定的后果"排序）

| 先看 | 决定 | 一句话 | 不决定的后果 |
| --- | --- | --- | --- |
| ★★★ 1 | **A**：把 4 把键加进 runtime 的 `envNames` | 那**一个数组**（`product/process-manifest.mjs`）缺 `LEGION_PATH_SCOPE` / `LEGION_CONNECTOR_DECLARATIONS` / `LEGION_EXECUTION_SCOPE` / `LEGION_EXTERNAL_API_SCOPE` | 三道范围检查与连接器判定在**真实部署里全都不生效**（而各自的用例全绿），且这个数**连续两轮在涨** |
| ★★★ 2 | **K**：岗位清单说的是「Legion 能力名」还是「执行面工具名」 | 两个词汇表**结构上不相交** ⇒ `whitelist`（PRT-603）**配了也接不上**，接上去是一个**全拒**的强制面 | 这一道**永远接不上**；而"接上去"的表现看起来像"岗位清单写错了" |
| ★★★ 3 | **L**（新，第 28 条）：控制面的数据怎么到达执行面 | 三条路见 §4.2；它同时挡住 PRT-610 落账、F-18/F-19 执行面一半、Pack 账**三项** | 三项**一起停**；`decisionSourceRecorded` 永远没有产出者，发布门禁永远判否 |
| ★★ 4 | **I**：两张 MCP 表谁是权威 | `execution-scope.mjs` 的 `mcp` 段 vs F-21 的连接器登记表 | 两道对同一工具给**两个**结论 |
| ★★ 5 | **J**：外部 API 授权表要不要管 scheme | `checkExternalApi` 不看 scheme ⇒ `ftp://…` 会被放行 | 今天不致命**只因为**那一格在真部署里是 `false`——而那正是第 1 条 |
| ★★ 6 | **B**：`persistRunRecord()` 传 `peakResource` | 记录层已能带、能校验、能落盘、能读回 | PRT-009 停在 ⏸ |
| ★★ 7 | **D**：`TEAM_HUB_TOKEN` 进 `envNames`，还是从 `fields` 拿掉 | "这个进程**能配**它"与"它**能拿到**它"必须有一处让步 | 一个**读不出、也不报错**的配置项 |
| ★ 8 | **C**：死代码处置 | 模块级 **9** 个不可达 / 函数级 **43** 条 | "死代码还有多少"这个数**只涨不跌** |
| ★ 9 | **第 29 条**（新，第 30 轮）：`RunRequest.env`（环境变量白名单）要不要有真消费者 | 两份规格都写"必须包含"，实测**不在必填字段里**、**被校验**、**缺席静默折成 `[]`**、而**读者一个都没有** | 这一条**永远不起作用**——而它在契约层**看起来是实现了的**（字段在、校验在、注释还引注了 spec §6.1），**任何字面核对都会通过** |
| — | **H**：政策门要不要从连接器声明里读「能力」 | MCP 公开名不在能力目录里 ⇒ 连接器层**只能更严、不能更松** | "配了 `allow`"与"没接上"的**最终判决相同** |

★ **前两条是同一类，也是最容易被漏掉的一类**：它们都不是"还没做"，
而是"**做了、而生产里没生效**"——而两条的读数在各自的套件里都是**绿的**。

---

## 六、需要外部环境的（本机做不了）

| 项 | 状态 | 阻塞 |
| --- | --- | --- |
| PRT-253 | ⏸ | 要一次**真的自动执行**。真额度**已花过且已验**（`dsh --profile headless` → exit=0、9.2s、`PONG`）；剩下的是平台边界：Windows 上不会有自动执行 |
| PRT-009 | ⏸ | 要一台**执行期外**的机器采样峰值资源（两个采样器**均已落地**，只差一次实跑读数） |
| PRT-256 | ⏸ | 需**真实外部用户** |
| PRT-910 | ⏸ | 需**真实用户项目** |
| PRT-316 | ⬜ | **日期闸门**：最早 **2026-09-24**（评审闸门已过，纯等待） |

---

## 七、诚实边界（这些不是"待办"，是已知的读数上限）

1. ⚠️ **那条出站车道在**生产里还没有人调**。** 写入侧与收账侧都建好了、环也在真库上走通了，
   但第 15 条**没关**，`decisionSourceRecorded` **依然没有任何产出者**。
   本轮口径是"**车道已建、环已证、位置未决**"，**不是**"模块全绿"。
2. ⚠️ **环的证明是在单进程内、用注入的临时目录走通的**；真实部署里写与收是**两个进程**，
   而"分开之后它们怎么找到同一个目录"正是第 28 条。
3. ⚠️ **没有给 spool 定目录**——不发明默认值。
4. ⚠️ **没有动 `team-hub/server.mjs`**（9097 行，且是别的会话的在制品风险区）。
5. ⚠️ **生产入参里仍然没有连接器声明表**；`decision-port.mjs` / `connector-feedback.mjs`
   只在**假 Context** 上挂过；**没有起过一个由 Launcher 完整启动的部署**
   （您正在跑的 DSH 在 3080 上）。
6. ⚠️ **`external-api-scope` 没有 scheme 规则**；`idempotencyKey` 恒为 `null`，
   于是 `IDEMPOTENCY_NOT_HONORED` 从不触发。
7. ⚠️ **文档表格棘轮还剩 87 处**没修——它们是**时点记录**（`docs/review/*`、
   `scratch/*evidence-backup/*`），记录的是当时发生的事、不再改动。

### 本轮**我自己造出来、被用例当场抓住**的四处（都留在注释与报告里）

| # | 缺陷 | 为什么它危险 |
| --- | --- | --- |
| 1 | 第一版 `safeRunId` **只挡路径分隔符** | 漏了冒号，又漏了**win32 会静默去掉的尾部点/空格**——`run-1.` 与 `run-1` 是同一个目录名 ⇒ 两个 Run 合流成一本账，而它**一个字符都没被替换**，连"消毒"这个能引起警觉的动作都没有 |
| 2 | 第一版 `toolCallDrainStatus` **用条数分桶** | "根本没跑过"与"跑了但执行面从没写过"是**同一个处置**（查接线），而"写了、一条都没落"是**另一个**（查数据）——两个桶会把它们混成一个 |
| 3 | 第一版表格检查器**用朴素 `split('\|')`** | 全仓报出 **934 处**、其中 **847 处是假红**（全部来自**已经正确转义**的写法）。*一个「到处都是假红」的检查，与一个「找不出真的那一处」的检查，在"它能不能挡住回归"上是同一个东西* |
| 4 | 第一版文件清单**没用 `git ls-files -z`** | `core.quotePath` 会把本仓 **8 个中文名 ADR** 转义加引号 ⇒ 它们**打不开**、各记一条 `missing`。*症状是总数偏高（像个坏消息），而真正危险的方向藏在里面：**它们自己的缺陷一处都数不到*** |

★ 还有两处**更小但同族**的：`spoolFileFor()` 只算路径不建目录；
以及**把指针写成 `§5 第 **28** 条`（数字加粗）让门禁的正则匹配不上**
——*一个"为了给人看而加粗"的指针，与一个"机器再也读不出来"的指针，是同一个字符串*。

---

## 八、复跑方法

```bash
# 全量门禁（约 14 分钟，test 阶段约 812 秒）
node scripts/ci/run-ci.mjs --out .ci/r22b

# 只看这一轮新增/受影响的面
node --test runtime/toolcall/spool.test.mjs
node --test orchestrator/worker/toolcall-drain.test.mjs
node --test scripts/prt/doc-table-integrity.test.mjs
node --test scripts/prt/reachability.test.mjs

# 台账与裁决清单
node scripts/spec-progress.mjs --check          # → 140/145，未完成 5
node scripts/prt/reachability.mjs --diff        # → 与基线比对（46 条，全部已定性）
```

---

## 附：第 22 轮提交（`9165836..208fbf8`，8 个）

| 提交 | 内容 |
| --- | --- |
| `e0010c4` | **代码**：出站车道两半（spool + drain）+ 两个套件 + run-ci 登记 |
| `201e1bc` | **文档**：PRT-610 台账行、`DECISION-…` §14、§5 第 28 条、会话/最终报告 |
| `6c99909` | **代码**：`doc-table` 检查器 + 10 例 + run-ci 登记 |
| `3205059` | **文档**：修 13 处表格错位 + 记录这道门禁的来历与两处自查缺陷 |
| `c9deadc` | **修正**：两个新模块定性为 `gap`（基线 44 → 46）+ 去掉指针数字的粗体 |
| `4a4a32c` | **文档**：记下可达性门禁按设计抓人 + 指针加粗把机器读指针弄瞎 |
| `466239d` | **文档**：第 28 条铺成三条路 + 指出它不是一条、是一族 |
| `208fbf8` | **文档**：冻结树上的全量 CI 读数（9/9 PASS） |

---

## 九、第 23 轮补记：F-15 的「告警 / 降级」——**spec 的一半此前没有任何实现**

### 9.1 一句话

`MULTI-AGENT-FEATURE-OPTIMIZATION.md` §4.4 对 F-15 逐字要求
「支持**告警、降级、暂停和硬阻止**」。量出来的落地情况是：
**暂停 / 硬阻止有**（`budget-ledger.mjs`），**按五维读出来有**（`usage-rollup.mjs`），
而**告警与降级一件都没有**——`budget-ledger.mjs` 里 `告警` / `alert` / `notify` /
`降级` / `degrade` 的出现次数**全部是 0**。

★ 更值得看的是它**为什么一直没被发现**：`usage-rollup.mjs` 的文件头**声称**
「硬阻止 / 暂停 / 降级 —— `budget-ledger.mjs` 已实现」——它把「降级」记在了账本名下。

> 一行写着"已实现"的覆盖声明，与一行真的已实现，
> 在排期表上是同一个东西——只不过前者会让下一轮的人**不去看它**。

### 9.2 本批交付

| 文件 | 内容 | 判据 |
| --- | --- | --- |
| `team-hub/budget-alert.mjs` | 求值：等级 / 可信度 / 动作 / 能不能放行 | 20 例，**7 处变异逐条咬住** |
| `GET /api/usage/alert` | 接到真库上的只读路由 | 9 例真 hub 进程 + 真 HTTP，**2 处变异咬住** |
| `team-hub/usage-rollup.mjs` | 订正文件头那句错误的归属声明 | —— |

★★★ **它的核心不是"比大小"，是"不许把不知道读成没事"：**

```text
已知花费 0 元 / 上限 100 元 / 7 条账目金额未知
  ⇒ spent / limit = 0  ⇒  "一切正常，离上限还远"     ← 而那 7 笔里可能有一笔早就超了
```

⇒ 求值把两个**正交**的量分开报（`level` 只由已知金额算、`confidence` 由
"不知道几条"算），再用**一条**规则接起来：
**`confidence` 不是 `exact` 时 `action` 永远不许是 `none`**；
对外只暴露一个 `allClear`，它要三件事**同时**成立。

### 9.3 ★★★ 接缝上抓到一个真缺陷（用例抓到的，不是复核出来的）

路由第一版只把**认识的那三个**阈值名从查询串里挑出来交给求值函数，于是
`?warning=0.5`（`warn` 拼错）**根本没进到**那条"不认识的阈值名 ⇒ 抛"的守卫里
——**守卫在，但它看不见这个键**，接口照旧返回 200。

> 一个把不合格的输入**过滤掉**、再交给守卫的适配层，
> 与一个根本没有守卫的实现，是同一个东西——
> 只不过前者的守卫**在源码里看起来是有的**。

★ 它与 9.1 那条**方向相反**：9.1 是"守卫太松"，这条是"守卫没看到输入"。
**两种都不会让任何一条既有用例变红。**

### 9.4 ★★ 一个刻意的接口设计：**不**拿 `perRunBudget` 当上限

`bindingStore` 里确实有 `perRunBudget: { maxCost, maxTokens, currency }`，
看起来正是"上限"。但它是**单次运行**的天花板，而本接口比的是
**一个时间窗内的累计花费**。那个比值算得出来、通常落在 0..1、看起来完全正常
——但它没有意义：窗里有 30 次运行时，"累计花费超过单次上限"是**必然**的。

> 一个"算得出来、落在合理区间、而且没有意义"的比值，与一个正确的比值，
> 在仪表盘上是同一个东西——只不过前者会在**每一次**告警里都显得很有道理。

⇒ 上限由调用方给。用例 ④c 用一条 `perRunBudget.maxCost = 0.01` 的夹具绑定钉住这条。

### 9.5 诚实边界（这一批**不**声称的）

1. ⚠️ **`degrade-model` 只是一个动作名，不是一次真的降级**——真的去换模型是执行面
   的事，而执行面今天**没有**读这个接口的地方（它没有 hub token，见第 19 条裁决）。
   ⇒ 与第 22 轮那条车道**卡在同一处**。口径是"告警这一半建好了、真 HTTP 上跑得通"，
   **不是**"生产里已经在降级"。
2. ⚠️ **预算上限本身还没有出处**（"每个 scope / employee 的预算是多少"没地方存）
   ——那是第 19 条人工项 **A** 的同一个缺口。
3. ⚠️ **没有做防抖**：`direction` 只报读数，滞回是有状态的事而本模块刻意是纯函数。
   在阈值上下反复跨线时会来回报——**这个代价是明说的，不是漏掉的**。

### 9.6 第 23 轮提交

| 提交 | 内容 |
| --- | --- |
| `8d1c1c0` 一类 | **代码**：`budget-alert.mjs` + 两个套件 + HTTP 路由 + run-ci 登记 + 订正 `usage-rollup` 那句归属 |
| 紧随其后 | **文档**：功能表 F-15 行、会话报告 §10.54、最终报告 §3.0h + §五 边界 31–33、本报告第九节 |

### 9.7 ★★ 全量 CI 在**冻结树**上跑出 2 处红 —— 都是本轮新码触发的，都已修

★ 这两处红的**价值大于它们造成的返工**：它们是**真门禁真的拦下了真遗漏**。

| 阶段 | 门禁 | 报的是什么 | 处置 |
| --- | --- | --- | --- |
| `env` | `config scan` | **14 项未在 schema 中处理**（`BUDGET_ALERT_CODES` / `_REASONS`） | 按既定做法登记进 `nonEnvLiterals` 并写明理由 |
| `test` | `prt-baseline` ④ | 平台契约已漂移：`+ 路由: GET /api/usage/alert` | `--record` 刷新基线（187 → 188 条路由） |

★ 第一处：我那 14 个具名码是 SCREAMING_SNAKE 的**对外可见字符串常量**，
扫描器按名字怀疑它们是环境变量。**它拦对了**——这类"新写的具名码没登记"
正是它存在的理由。

★ 第二处那次 diff **只有 2 行**（1 条新路由 + 1 个源文件哈希），
⇒ 它顺手证明了"这次的契约变化**只有**这一条"，没有意外的建表 / 状态机漂移。
**门禁的价值正是这个读数。**

### 9.8 ★★★ 由此抓到一处「设计说明与事实不符」（与 9.1 同族）

`scripts/prt/baseline-snapshot.mjs` 第 20–23 行写着：

```text
为什么**不**接进 CI 作为阻断门禁：
  ……把它做成硬门禁会让每个正常的功能提交都红，
  最后必然被人用 `--record` 无脑刷掉，反而失去意义。
```

**而它已经是一门阻断门禁了**：`baseline-snapshot.test.mjs` 的用例 ④ 做的正是
`assert.deepEqual(diffSnapshots(recorded, current), [])`，而那个文件在 CI 的
`test` 阶段里。

> 上面第 21–22 行**逐字预言了**这个结局。它没有防住——
> 因为写下那段话的人以为"没接进 CI"是**已经完成的事实**，
> 而实际上它是以另一个文件名接进去的。

⇒ 本轮**就是**那句预言的复现：加了 1 条只读路由 ⇒ CI 红在 ④ 上 ⇒
唯一的解除方式是 `--record` ⇒ 照做。

**这与 9.1 是同一族**：一句**没有被任何东西核对的断言**。
9.1 是"声称已实现"（而没实现），这里是"声称没有接 （而接了）。
两个方向的错都不响——直到有人撞上。

订正只改注释、不改行为；并顺手把一条**真实价值**写进去：
门禁的价值是"契约变化只有这一条"这个读数，而不是"拦住人"。

### 9.9 ★ 顺带：一次"系统性找同类问题"的尝试，结果是**阴性**

上面 9.1 是**撞上**的（读 spec 时顺手量了一下），所以我做了一次系统性检查：
把 spec 里点名的**具体能力词**逐个在**产品代码**（排除测试/文档/scripts）里数一遍，
零命中的列为嫌疑。

结果：除了 **F-15 那两个词**，其余全部有命中；而三个零命中
（`contextAssembler` / `skipOnOverlap` / `scheduleStore`）查下去**全是我自己猜的
驼峰名猜错了**——真实名字是 `assembleContext` / `overlapPolicy` / `automation-store.mjs`，
功能都在。

> 一个关键词探针**只能发现"我猜对了名字"的缺口**。
> 它找到的那一个（`告警` / `降级` 是**自然语言词**，spec 里逐字写着）之所以能被找到，
> 恰恰因为我不是在猜标识符，是在读那句话。

⇒ 所以这条探针**不**进 CI（它会按构造产生假阳性）；它的价值是这一次的阴性读数：
**同类问题在本仓没有第二个。**

---

## 十、第 24 轮：去**系统性**地找 9.1 那一族，找到了两个真缺陷

### 10.1 先说方法：我挑的是那一族里**最可自动核对**的一种

9.1 / 9.8 记的都是同一个形状——**一句没有任何东西核对的断言**
（`usage-rollup.mjs` 声称"降级已实现"；`baseline-snapshot.mjs` 声称"没接进 CI"）。
9.9 那次"系统性找同类"是**阴性**的，但它的做法是**关键词探测**——
只能发现"我猜对了名字"的缺口。

这一轮换了做法：找那一族里**不需要猜**的成员。**行号引用**就是：
被引文件在仓库里，行号对不对是**能算**的。于是扫了全仓 341 个产品文件
注释里的 **104** 处 `file.mjs:NNN`，另加台账与状态文档里的 **171** 处。

结果：**9 处**确定性坏引用（行号越界，或指向空行/纯收尾符）——
而它们**全部**在既有门禁 `scripts/prt/boundary-facts.mjs` 的射程之内。
顺藤摸下去，抓到的是**门禁自己**的两个缺陷。

### 10.2 缺陷 ①（重）：那个"范围引用检查器"**从来没读过范围终点**

`LINE_CITATION_RE` 只有 **3** 个捕获组，而 `scanLineCitations` 读的是 **`m[4]`**
（恒为 `undefined`）⇒ **范围终点永远等于起点**。实测：

| 引用 | 文件行数 | 修复前 | 单行对照 |
| --- | --- | --- | --- |
| `run-floor.mjs:600-9999` | 614 | **`broken=[]`（静默放过）** | `:9999` ⇒ 正确报出 |

⇒ 台账里 `run-floor.mjs:544-559` 与 `executor-binding.mjs:254-261`
两个范围引用**从来没有被当成范围查过**；`total` 也把范围当单行数。

> 一个"范围引用检查器"与一个"单行引用检查器"，
> 在**所有范围引用都恰好从合法行开始时**是同一个东西——
> 而这条判据的全部价值就在那个区间的**末端**。

### 10.3 缺陷 ②：只查"行在不在"，不查"那一行是不是空的"

`tool-request.mjs:731` 在台账与状态文档里共 **6** 处，**没有一次越界**——
而第 731 行是一个**空行**；真正那句话
（`if (pathScope === null) return undefined`）在 **780** 行（`scopeGuard` 内）。
`orchestrator/worker/executor.mjs:439` 同样是空行。

★ **边界**（写下来免得下一轮有人当成更强的判据）：它**不**检查"那一行的内容
与文档里说的对不对"——那要读懂语义，做不了。所以绿**不等于**引用正确，
只等于"它指向的地方**不是空的**"。

### 10.4 交付

- 修 `m[4]` → `m[3]`（两处）；读出**文件内容**（原来只留了 `.length` 这个**数字**）；
- 6 处 `tool-request.mjs:731` → **符号** `tool-request.mjs` 的 `scopeGuard`
  （行号会漂移：同一处被记过 `485-509` → `508-536` → `540/545`；符号不漂移）；
- `executor.mjs:439` 那段是**已标"已过期"的历史陈述**（同段已写明
  "缺口 ① 现在关着"并给出正确坐标）⇒ **去掉失效行号**，
  不让它读起来像一个活的指针；
- 新增 **⑪j / ⑪k** 两例（`boundary-facts.test.mjs` 34 → **36**），
  各带反向控制（⑪k 的第三条尤其关键：**跨空行的范围不许红**，
  否则这条判据会在真实台账上直接红、然后被人整体关掉）；
- 变异验证：两处逐条咬住，还原后**逐字相同**。

### 10.5 我在这**一次修复**里自己踩的两个坑（都留在注释与脚本里）

**坑 A —— 差点报出 132 条假发现。** 我照抄 `lines[i - 1]` 时没注意
`lines` 是一个**数字**（`.length`），于是 `undefined ?? ''` ⇒ 空串
⇒ **132 条引用全部被报成"指到空行"**，其中包含我刚亲手核过、
明明有内容的 `runtime-contract-server.mjs:599`。

> 一个"判据写错了"与一个"文档里有 132 处坏引用"，在输出里长得一模一样
> ——而且**后者看起来更像个发现**。处置却完全相反。

**坑 B —— 变异验证本身不完整。** 缺陷 ① 有**两处**（`key` 与 `to:`），
第一版变异只改了一处 ⇒ 守卫照样绿 ⇒ 被记成"⑪j 漏网"。
真因是**变异没做全**。*"变异没执行"与"守卫没咬住"在输出里同形，处置相反。*

★ 这两个坑与 9.1/9.8 是**同一族**：**一个读数，与一个坏掉的量具，
在只有那个读数的报告里长得一样。**

### 10.6 一处**故意没改**（归属别的会话）

`product/launcher/readiness.mjs:15` 的 `team-hub/server.mjs:4438` 也指错了
（`/api/config` 返回 `auth`/`db`/`port` 在 **9117** 行）。**没有提交这个修改**：
那个文件是**另一个会话的在制品**（工作树里 256+/66-），
提交它会把别人未复核的改动一起带走。⇒ 归属那份 WIP 的会话。
`product/process-manifest.mjs` 里同一条也留着，理由相同。

### 10.7 本轮的读数

| 项 | 读数 |
| --- | --- |
| 第 24 轮提交 | `9d0ddc4`（配置门禁的理由锚点）、`7a554e6`（坐标判据）、`3d921c9`（本节） |
| `boundary-facts` | **PASS**（14/14，红 0）；覆盖面 130 条（落到实处 122 / 后缀多候选 8） |
| 判据自己的套件 | **36/36**（34 → 36） |
| 变异 | 配置锚点 4/4；坐标判据 2/2；**还原均逐字相同** |
| 全仓项目文件里的坏引用 | 9 → **0**（另有 2 处归属别的会话，见 10.6） |
| 全量 CI | **9/9 PASS，exit 0**（HEAD `3d921c9`，`.ci/r24`，`test` 801677ms） |

★★ **这一轮最重要的结论不是"修了 9 处引用"**，而是：
**那 9 处全都在门禁的射程之内，而门禁对它自己的两个盲点一无所知。**
一个"坐标判据"如果只查行号范围，它给出的绿恰恰**覆盖**了它没查的那两样东西
——这与 9.1 的 `usage-rollup.mjs` 是同一个形状，只是层级更高：
**那次是一条注释说了假话，这次是一条判据说了假话。**

---

## 十一、第 25 轮：同一族的**第三种**断言 —— **计数**

### 11.1 这一族现在有三个成员，各有各的"烂法"

第 22–24 轮撞到的其实是同一个形状：**一句没有任何东西核对的断言**。

| 成员 | 长什么样 | 谁在核 | 什么时候做成判据 |
| --- | --- | --- | --- |
| **覆盖声明** | "降级已实现"（其实是零） | 没人 | 人工复核（第 23 轮） |
| **指针** | `file.mjs:731` 指的是一个空行 | 门禁，但只查行号范围 | **第 24 轮** |
| **计数** | "套件 X（N 例）"里那个 N 过期了 | **完全没人** | **第 25 轮（本轮）** |

★ 三者里**计数最可机器核对**：CI 每一行套件都带 `tests=N`（实测），
声明就写在文档里——两者本可以自动对上，而**今天没有人对**。

### 11.2 读数：主表 33 处计数声明，**30 处对、3 处错**

| 项 | 声明 | 实测 |
| --- | --- | --- |
| `runtime-contract`（F-01） | 13 | **64** |
| `dsh-adapter`（F-02） | 25 | **98** |
| `registry.test.mjs`（F-21） | 32 | **37** |

三个数**都不是**"写的时候算错了"，而是**写完之后用例还在长**——
而那三格描述的是**今天的状态**。

> 一个"写的时候数对了"的数字，与一个"昨天数对了"的数字，
> 在文档里长得一模一样——而后者是**借来的**权威。

### 11.3 ★★ 判据**只查主表** —— 这一条是设计里的重点，不是省事

全仓扫下来有 **106 处**与实测不符，而绝大多数是**历史读数**
（"本轮…5 例"、evidence 日志里当时的数）。**那些必须留着旧值**：
改它们就是篡改历史。

> 一条"当时的读数"与一条"现在的读数"，在文本里长得一样——
> 而前者**必须**冻结、后者**必须**跟着代码走。
> 所以一条"全仓 N 例都不许过期"的判据会**红在正确的地方**。

⇒ 测试 ④ 专门钉住这一点：拿一条非主表的历史读数去喂，它必须**不**报。

### 11.4 交付

- `scripts/prt/suite-counts.mjs`（判据）+ `suite-counts.test.mjs`（**10 例**），
  已登记进 `run-ci.mjs`（`stage` 的"套件清单完备"那条要求下，已 PASS）；
- 修掉上表三个数；
- `docs/MULTI-AGENT-FEATURE-STATUS.md` 加 **§5.9**；
- 变异（`scratch/_mutate-counts.mjs`）：把三个数**逐个改回去**再加一个
  "漂亮但错的数"，**4/4 逐条咬住**，还原后**逐字相同**。

### 11.5 ★★★ 写这道判据时，我让门禁**瞎掉了两次**，而两次都看起来是绿的

**坑 A：提取器只认到 20 条，而实际 33 条。**
行首正则写成 `/^\|\s*(F-\d+)\s*\|/`，于是 `| F-05 前半 |`、`| F-19 缺口① |`
这样的主表行**一条都没被认出来**（漏掉的 13 条里有 `run-events.test.mjs` 18 例、
`event-delivery.test.mjs` 31 例）。

> 一个"只认 20 条、而这 20 条全对"的门禁，与一个"33 条全对"的门禁，
> 在只有 `ok: true` 的输出里是同一个东西——而前者**看起来更可信**：
> 它还报了一个具体的条数。

**坑 B：在测试运行器里 spawn 测试运行器，子进程一个字都不输出。**
父进程带着 `NODE_TEST_CONTEXT` 时，子 node 认为自己是测试 worker：

```text
父进程在 test runner 里 + 不清 env   ⇒ len=0      ℹ tests 读不到
父进程在 test runner 里 + 清 env     ⇒ len=1169   读得到
父进程不在 test runner 里           ⇒ len=1166   读得到（所以直接跑 CLI 时看不出问题）
```

⇒ 32 条声明**全部**落进 `skipped`，返回体是 `{ ok: true, checked: 0, skipped: 32 }`。

> 一个**完全瞎掉**的门禁，与一个"32 条全对"的门禁，
> 在只看 `ok` 的输出里是同一个东西。

抓住它的是测试 ① 里那条 **`skipped === 0`** 的断言——
**这条断言不是为"数错了"写的，是为"门禁瞎了"写的。**
它与第 23 轮 F-15 的 `confidence` 是同一个设计：
**"跳过多少条"必须与"都对"分开报。**

### 11.6 诚实边界

- 它查的是**用例条数**，**不**查"这些用例测的东西对不对"。
  绿只等于条数一致，**不等于**那一格的证据够用。
- 它认不出**语义**上过期而**数字**恰好不变的声明（换了实现、条数没变）。
- 它**真跑文件**（32 个，串行 ≈ 18s）。复用 CI 的套件行计数是**错的**：
  一行聚合多个文件（`path-scope` = 4 个文件 ⇒ 67），而声明是**一个数**
  —— 我第一版就是拿聚合总数比单个数，得到 **135 处假发现**
  （与第 23 轮"拿窗口聚合比单次上限"是同一类错）。

### 11.7 本轮的读数

| 项 | 读数 |
| --- | --- |
| 第 25 轮提交 | `bacd407`（判据 + 10 例 + 登记 + 三个数 + §5.9 + 本节） |
| `suite-counts` 判据 | **32/32 已核、0 跳过、ok**（`node scripts/prt/suite-counts.mjs`，exit 0） |
| 判据自己的套件 | **10/10** |
| 变异 | **4/4 咬住**，还原**逐字相同** |
| 主表计数声明 | 33 处 → **0 处不符** |
| 全仓其余 ~106 处 | **故意不动**（历史读数，见 11.3） |
| 全量 CI | **9/9 PASS，exit 0**（HEAD `bacd407`，`.ci/r25`，`test` 824992ms） |
| 套件总数 | **373**（367 → 368 → 369 → 370 → 371 → 372：第 35 轮新增 `ledger-evidence`、`stage-scope`、`parse-suite-output`；**→ 373**：第 41 轮新增 `declaration-mirrors`） |

---

## 十二、第 26 轮：**这份报告自己**也在那一族里

### 12.1 出发点：§二 的标题是一句没人核过的断言

§二 的标题是「最终读数（**都是机器读数，可复跑**）」。这句话本身就是
**一句没有任何东西核对的断言**——而且它出现在一份**专门用来汇总
"哪些读数可信"的报告**里，位置比前面三种都更靠前：

> 读者正是**因为**它写着"可复跑"才不去跑。

所以这一轮我去跑了。逐行重算（`scratch/_verify-readings.mjs`）：
**大部分对，抓到一处真漂移。**

### 12.2 抓到的那一处：表说 825s，紧挨着的正文说 808s

§二 的表里已经写了**四轮** CI 的读数；而紧挨着表的正文说：

    "★ 这一次全量 CI 也跑在一棵冻住的树上：test 阶段那 808 秒里……"

**808s 是第 23 轮**那次 CI 的数。那句话是**第 22 轮**写的，
第 23 轮往上面加表行时**没有回头改句子**。

> 一张表里已经写了三轮新读数，而紧挨着它的那句话还记着第一轮的那个数。
> 两者在报告里长得都像"机器读数"。

★★ **写这一节时我被自己的判据拦下了**：上面这句我原来照抄了原文，
连 `test` 外面的**反引号**一起抄——于是锚点在报告里命中了**两处**
（真正的正文那处 + 这里引用的一处），`ANCHOR_AMBIGUOUS` 判红。

> 一段**解释这个锚点为什么危险**的文字，自己给这个锚点制造了歧义。
> 这与本模块 `D2` 那件事是同一个形状：*解释这个 bug 的注释复现了这个 bug*。
>
> ⇒ 这里改成**不带反引号**地引用（判据的锚点要求 `test` 前有反引号），
> 而不是去放宽锚点。**修文档，不修判据。**

### 12.3 做成了 5 条判据（加进既有的 `boundary-facts`，不另起一套）

| 判据 id | 核的是 |
| --- | --- |
| `handover-ledger-tallies` | 台账 **145 = 140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜**（**四个**数**都**核） |
| `handover-tracked-suites` | 套件总数 ↔ `git ls-files '*.test.mjs'` |
| `handover-unreachable-total` | 不可达条数 ↔ 基线 JSON |
| `handover-doc-ratchet` | 全仓棘轮 ↔ `REPO_WIDE_BASELINE` 常量 |
| `handover-ci-prose-matches-table` | **正文那句秒数 ↔ 表里最新一行的毫秒数** |

★ 加进 `boundary-facts` 而不是新写一套，是因为它已经解决了这条路的
**三个关键问题**：① 两侧必须**独立读出来**；② 锚点找不到要判红
（`ANCHOR_MISSING`——"判据静默失去检查对象"与"检查通过"在只有一个 ✅ 时同形）；
③ 锚点命中多于一处也判红（`ANCHOR_AMBIGUOUS`——不许靠"我是第一个匹配"）。

只有第 5 条是**新形状**：它的两侧**都在同一份文档里**
（散文 vs 表）。这不是"手抄件互核"——表里的毫秒数是每轮从
`summary.json` 抄进来的**实测**，而散文那句是**对表的概括**；
概括与表不一致本身就是缺陷，而且这个缺陷**真实发生过**（12.2）。

### 12.4 交付与复核

- 5 条事实 + 检查器 `tallyLedger` / `trackedTestFiles` / `tallyUnreachable`；
- 自身套件 **36 → 43 例**；
- 变异（打 **derive 侧**——测试 ③ 只打声称侧）：**5/5 咬住**，还原**逐字相同**；
- 真实仓库：**19/19，红 0**。

### 12.5 ★★★ 我又踩了同一个坑的**镜像**：变异全部"漏网"，而真相是变异没跑

第一版变异脚本在**同一个进程**里改源文件再 `checkFacts()`——
ESM 的**模块缓存**让它仍然用改之前那份 ⇒ **5 个变异全部报"漏网"**。

> "变异没执行"与"守卫没咬住"在输出里长得一模一样，处置却完全相反。
> 这是第 24 轮就写进报告的坑，第 26 轮又踩了一次。

改成 **spawn 一个新进程**跑检查（`scratch/_check-facts-once.mjs`）之后：
**5/5 咬住**。M1 的输出还顺手把那个 bug 的形状印了出来：

    claimed={"total":145,"done":140,"paused":4,"todo":1}
    actual ={"total":145,"done":0,  "paused":3,"todo":1}

——**`done: 0 / paused: 3` 是一个看起来完全合理的分布**，
而它来自"状态列差了一位"。这与本轮抓到的假发现是同一个形状。

### 12.6 诚实边界

- 这 5 条只钉**这一份报告里那几个承重的数**，不是"全仓所有散文里的数都核过了"。
- 第 5 条钉的是"散文 == 表里最新那一行"。若将来表里加了**多行**
  且散文想指**更早**那一轮，这条会红——那是对的：**得把那句话写清楚**。
- 它**不**核"表里那些毫秒数本身与 `summary.json` 一致"
  （那要读 `.ci/*/summary.json`，而且每次 CI 都换目录）。**这一条仍是人工的。**

### 12.7 本轮的读数

| 项 | 读数 |
| --- | --- |
| §二 抓到的真漂移 | **1 处**（正文 808s ↔ 当时表里 825s），已订正 |
| `boundary-facts` | **PASS**，参与比对 **19/19**，红 0 |
| 事实总数 | **14 → 19** |
| 自身套件 | **36 → 43 例** |
| 变异（derive 侧） | **5/5 咬住**，还原**逐字相同** |
| 全量 CI | **9/9 PASS，exit 0**（HEAD `dd6eb8f`，`.ci/r27b`，`test` 873171ms） |

### 12.8 ★ 这一轮把 `test` 阶段推高了，但**别把这个数当准的**

诚实记账。`test` 阶段的四次读数：

| 轮 | HEAD | `test` |
| --- | --- | --- |
| 第 24 轮 | `3d921c9` | 801677ms |
| 第 25 轮 | `bacd407` | 824992ms |
| 第 26 轮 | `f991d9d` | 868769ms |
| 第 26 轮收口 | `4ccdf86` | 937905ms |
| **第 27 轮** | `dd6eb8f` | **873171ms** ← **加了一整套路件，反而更快** |

★ **最后两行之间我只改了测试文件里三条控制的写法，调用次数一次都没多**
（既没加 `checkFacts()`、也没加套件），却涨了 **69136ms**；
而**第 27 轮我加了一整套判据（8 例 + 登记进 CI），反而降到 873171ms**
——比上一轮**低** 64734ms。
⇒ **这台机器上的 CI 读数有几十秒级的抖动**（另一会话有 440+ 条未提交路径，
很可能同时在跑测试）。

> 把 868769 − 824992 = **43777ms** 直接说成"我加的开销"，
> 与把 937905 − 868769 = **69136ms** 说成"什么都没干却慢了"，
> 与把 937905 − 873171 = **−64734ms** 说成"新判据让 CI 变快了"，
> 是同一类错：**拿两次读数之差当因果**。
> 第 27 轮这一行是这条告诫最好的反例——**它把因果方向整个翻了过来**。

**能站得住的证据是直接测出来的那一条**：一次 `checkFacts()` 实测 ≈**4.2s**
（其中走全树的 `generatedArtifacts()` 589ms、`lineCitations()` 600ms），
而本轮**确实**多了 **10 次**调用：

- 测试 ③ 逐条对带锚点的事实做反面控制，带锚点的事实 **5 → 9** ⇒ +5 次；
- 新增 ⑭c/⑭d/⑭e/⑭e2/⑭f ⇒ +5 次。

**10 × 4.2s ≈ 42s** —— 这是**算式**，不是"两次 CI 相减"。
该套件今天约 **128s**（直接计时：`node --test scripts/prt/boundary-facts.test.mjs`），
其中约 **86s 是它本来就有的**。

★ **没有为了这几十秒去动缓存**（"每个 context 只扫一次树"能省四成）：
`defaultContext()` 那几个口是 `checkFacts` 与整套喂毒控制**共用的**，
在轮末改缓存语义、又没有等价的控制面覆盖，风险大于这点时间。
记为**有实测数字的**后续候选。

---

## 十三、第 27 轮：那一族的**第五种形态** —— 同一张表的两个格子互相矛盾

### 13.1 这一族的形态表（把它一次列全，因为下一个人还会撞到）

| # | 形态 | 长什么样 | 谁在核 | 轮次 |
| --- | --- | --- | --- | --- |
| ① | 覆盖声明 | "降级已实现"（其实是零） | 没人 | 第 23 轮（人工） |
| ② | 指针 | `file.mjs:731` 指的是空行 | 门禁，但只查范围 | 第 24 轮 |
| ③ | 计数 | "套件 X（N 例）"过期 | 没人 | 第 25 轮 |
| ④ | 报告标题 | "都是机器读数，可复跑" | 没人 | 第 26 轮 |
| ⑤ | **表内两个格子** | 🟡（没做完）配 `—`（不差什么） | 没人 | **第 27 轮** |

> 共同形状：**一句没有任何东西核对的断言**。

### 13.2 读数：29 行功能表里 2 行自相矛盾

| 行 | 状态 | 「还差什么」 |
| --- | --- | --- |
| **F-22 后端与工作区** | 🟡 | **`—`** |
| **F-24 ACL 与安全姿态** | 🟡 | **`—`** |

> 一行 🟡 配一个 `—`，与一行 ✅ 在表里长得一模一样——而读者只会看「状态」那一列。

★ F-24 那句缺口**原本就在表里**，只是**写在「依据」格里**——
同一份信息放错格子，按「还差什么」读表的人与机器都读不到。

### 13.3 两行都保持 🟡，**没有**升级成 ✅

写清缺口是**补上缺失的信息**；把状态改成 ✅ 是**对"做完了"下断言**。
前者我不需要谁批准，**后者需要**。（F-15 是表里的先例：核心已落地 +
一条子项未做 ⇒ 🟡，且缺口写出来。）

订正后：
- **F-22** 核心已交付（`workspace` 24 例 + `workspace-wiring` 9 例、PRT-306 ✅、可达）；
  未做的是 Docker/SSH/remote 扩展 —— spec 第 138 行「**后续**扩展」+ 第 218 行「远程后端」。
- **F-24** 读面已交付；未做的是多用户**写**面 ACL —— 第 218 行「多用户」。

### 13.4 判据 `feature-table-status`（8 例）

> 状态格的**终点**既不是 ✅ 也不是 ⏸ ⇒ 「还差什么」格**不许为空**。

★ **✅ 与 ⏸ 必须豁免**：表中 **21 行 ✅ + 2 行 ⏸ 正是用 `—` 写的**，
那是正确写法。规则放宽一寸（任何状态都不许 `—`）就会红在 **23 个正确的地方**，
然后被人整体关掉 —— 与 §5.9「全仓 N 例都不许过期」是同一种错。

### 13.5 变异：**两个方向都要打**（5/5 咬住，还原逐字相同）

| 方向 | 变异 | 谁捉住 |
| --- | --- | --- |
| 文档 | F-22 / F-24 缺口格退回 `—` | 门禁在真仓库上（exit 1，点名到行） |
| 代码 | `isEmptyGap` 恒 false（弄瞎） | **套件自己** |
| 代码 | 把 🟡 也当豁免（架空） | **套件自己** |
| 代码 | 把 ✅ 也算受约束（过宽） | **套件自己** |

★ 只打文档会漏掉"规则被架空了却仍然绿"这一整类。
★ 全部在**新进程**里跑：ESM 缓存会让同进程内的变异"看起来没生效"，
而*"变异没执行"与"守卫没咬住"在输出里一模一样*（第 24/26 轮的坑）。

### 13.6 诚实边界

- 只查两个格子**是否相容**，**不查**缺口那句话**对不对**。
- 发现不了"状态与缺口都自洽、但都是错的"。
- 16 行被跳过是因为**它们不是功能表**（§2 缺口投影表 3 列、§5.1 边界表 3 列，
  都没有状态列）。跳过数**报出来**——*"跳过 16 行"与"16 行都合规"在只报 `ok`
  的输出里是同一个东西*。

### 13.7 ★★★ 本轮的 CI **红过一次**，而红的是**第 26 轮那道判据在捉我**

这是本轮最值得记的一条。第 27 轮第一次全量 CI：

    test  FAIL (861222ms)
      FAIL boundary-facts: exit=1 tests=43 pass=40 fail=3 skipped=0

红的三条全是 §12 新加的那组，根因只有一句：

    ✖ handover-tracked-suites
      ✖ 文档说 360，产物是 361

**我加了 `feature-table-status.test.mjs` 这一个套件，于是总数 360 → 361，
而 §二 那张表还写着 360。**

> 第 26 轮我写下这条判据的理由是"读者正是因为表里写着'可复跑'才不去跑"。
> 第 27 轮它当场把我的账翻了出来——**只用了一轮就自证了价值**。

★ 这条红**不是**"CI 又坏了"，而是**新判据第一次实战**：
它拦下的是一个**真实存在的、我自己刚制造出来的**读数过期，
而不是既有旧账。修法是改那个数（360 → 361），**不是关掉判据**。

★ 顺带：这也说明**判据覆盖的是"结果"，不是"意图"**——
我不是故意写错 360，我只是**忘了**每加一套就要改一处数字。

---

## 十四、第 28 轮：那一族的**第六种形态** —— 读数的**适用范围**

### 14.1 我这一天跑的**每一次**全量 CI 都跑在一棵脏树上

第 24～27 轮共 5 次全量 CI，我逐条写成：

    全量 CI（**交付 HEAD**）| 9/9 PASS，exit 0（HEAD `dd6eb8f`，`.ci/r27b`）

而 `summary.json` 里唯一能回答"跑的哪棵树"的字段是 `git head=<sha>`——
**那是提交的名字，不是树的状态**。实测（`git status --porcelain`）：那一整天
工作树里都躺着**另一个会话的 13 个已改文件 + 441 个未跟踪文件**。

> `git head` 回答"哪个提交"，读者读成"哪棵树"。
> 在一份 9/9 PASS 的报告里，这两个问题**长得一模一样**。

★ 这一次读数**大部分是真的**（另一个会话的在制品与 `scripts/prt/` 无关），
但"是不是真的"当时**没有任何东西**回答得了——这就是它也算一个成员的理由。

### 14.2 ★ 顺带订正我自己的一个推断：脏树**没有**污染条目 A 的读数

我曾担心"脏树"会让条目 A（4 把键不在 `runtime.envNames` 里）的读数不可信。
**实测不必担心**：把 `HEAD` 版与工作树版的 `process-manifest.mjs` 的
5 个 `envNames` 数组逐字比过，**完全一致**（`scratch/_extract-envnames.mjs`）
⇒ 条目 A 的读数在两棵树上都成立。

    runtime.envNames（13）: DSH_HOME, LEGION_ACTOR, LEGION_APPROVAL_POLICY, LEGION_ATTENDED,
                           LEGION_CWD, LEGION_DATA_DIR, LEGION_ENFORCEMENT_ACTION, LEGION_LOG_DIR,
                           LEGION_PERMISSION_PRESET, LEGION_RUNTIME_TOKEN, LEGION_SCOPE,
                           LEGION_TASK_ID, TEAM_HUB_URL
    → 四把键（PATH_SCOPE / CONNECTOR_DECLARATIONS / EXECUTION_SCOPE / EXTERNAL_API_SCOPE）**都不在**

### 14.3 修法：产物侧 + 判据侧

**产物侧**——`run-ci.mjs` 新增 `readTreeState()`，把 `head`/`dirty`/
`modifiedCount`/`untrackedCount`/`fingerprint` 写进 `summary.json`，
**脏树时红字警告**（下面是 `.ci/r28` 的真实输出）：

    ⚠⚠ 本次 CI 跑在一棵**脏树**上：已改 14 个文件 + 未跟踪 450 个（指纹 1648c0f71901ba2a）。
       ⇒ 这次读数证明的是「**这棵树**是绿的」，**不是**「提交 caf24e7 是绿的」。

★ 读不出来时报 `known:false` 并说"读不出来"——**"没法判断"不等于"干净"**。

**判据侧**——新套件 `ci-reading-integrity`（**12 例**），两层**刻意分开**：

| 层 | 查什么 | 能不能进 CI |
| --- | --- | --- |
| 文档层 | 「交付 HEAD」那一行必须用**封闭词表**写树 | **能**（纯文本） |
| 产物层 | 真读 `.ci/*/summary.json` 交叉核对 | **不能** |

⚠️ 产物层**不能**当 CI 门禁：`.ci/` 在 `.gitignore:43`、tracked 文件数 **0**
⇒ 全新检出里没有那份产物，它在 CI 里会永远"没有可核对的产物"，
**而那种绿是假的**。边界写在模块头注释里。

★ 词表**刻意封闭**：自由文本（"已确认工作树"）与真的提了树，
在正则下的区别是**猜**出来的。

### 14.4 它当场红在**我自己的报告**上

    ✖ 第 39 行是「交付 HEAD」的 CI 读数，但**没说跑在哪棵树上**。

### 14.5 变异 6/6 咬住，还原逐字相同

    文档 M1 删掉「脏树 14 改 + 441 未跟踪」
    文档 M2 换成自由文本「已确认工作树」
    文档 M3 把「交付 HEAD」这个标志词改掉（扫到 0 行必须红）
    代码 M4 封闭词表放宽成"任何非空都行"
    代码 M5 去掉"扫到 0 行也是失败"
    代码 M6 readSummaryTree 把"缺字段"当成干净

### 14.6 ★★★ 第 26 轮那道判据**又**把我抓住了（连着两轮）

改完这一轮的表（交付 HEAD 从 `873171ms` 变成 `870000ms`）之后，
`boundary-facts` 当场红：

    ✖ handover-ci-prose-matches-table
      ✖ 文档说 873，产物是 870
      文档原句：test 阶段那 873 秒里

（★ 上面这句**故意不带反引号**：判据的锚点正是带反引号的 `test` 阶段那 N 秒里，
照抄会造出**第二个匹配** ⇒ `ANCHOR_AMBIGUOUS`。这是这个坑第三次咬到我，
见 §12.5 与 §14.3 的表。）

**与第 27 轮同一个形状**：我更新了表，忘了跟紧挨着它的那句正文。

★ 结论不是"这个数真难维护"，而是：**它已经连着两轮在我身上生效了**。
一条判据的价值不在写下来的那天，在于**它此后每次都咬得住**——
`handover-ci-prose-matches-table` 两轮 2/2。

★ 另外，那次红也暴露了那句正文的**第二层歧义**：它写着
"工作树的代码与文档**一个字节都没动过**"，而我这一轮刚给表加上
"脏树 26 改 + 432 未跟踪"。两句都真，但放在一起读者会把
**"跑的时候没变"**读成**"树是干净的"**。⇒ 已把两者**分开写明**，
并用 `tree.fingerprint` 作为"冻住"的凭据（它是对整份 `git status` 清单取的哈希）。

### 14.7 诚实边界

- 查"**有没有说清楚跑在哪棵树上**"，**不查**"那次 CI 到底绿不绿"。
- **不**判断"脏树上的绿算不算数"——这次**可以**接受（在制品与判据目录无关），
  但"可以接受"是**我读出来的**，不是门禁判出来的。
- 文档层只约束「交付 HEAD」那一行；历史轮次的行不受约束，因为那些是
  **当年的快照**，要求它们补写树的状态等于**篡改历史**。



