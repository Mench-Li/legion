# PRT 人工介入汇总（2026-09-20）

> 目标：`docs/MULTI-AGENT-FEATURE-OPTIMIZATION.md` 的全部功能实现 + 最终报告。
> 本文件只回答一件事：**剩下的、本机做不动的，具体是哪几件、为什么、需要谁做什么。**
>
> 口径：HEAD `8cd812e`（树含 `eeedd21`）；`.ci/r43` 全量 CI **9/9 PASS，exit 0**。
> 台账 `docs/superpowers/prt/PRT-PROGRESS.md`：**144 行 = 140 ✅ / 4 ⏸ / 0 ⬜**。

---

## 一、★ 4 条 ⏸ 里有 2 条是**同一个根因**

| 行 | 台账里的阻塞原话 | 归到哪一类 |
| --- | --- | --- |
| **PRT-009** | `peak-resource` 缺**实跑读数**；"卡在 `docs/STATUS.md` §4 第 15 条" | ⚠ **本机不存在那个读数** |
| **PRT-253** | "剩下的是**一次真的自动执行**（worker → Runtime 契约 → DSH → 模型），而那个前提在本机不成立" | ⚠ **同上一条** |
| **PRT-256** | "需真实外部用户" | 👤 需要**真人** |
| **PRT-910** | "需真实用户项目" | 👤 需要**真人** |
| **PRT-316**（⬜） | 日期门：加固提交 `0db37af` = 2026-09-10 + **14 天**一个发布周期 ⇒ 最早 **2026-09-24** | 🕐 **需要时间**（今天 2026-09-20，**还差 4 天**） |

**PRT-009 与 PRT-253 的根因是同一条**（两行的台账文字自己都指向 `docs/STATUS.md` §4 第 15 条）：
**这台机器（Windows）上不会发生"一次真的自动执行"**，而那两行各要一次**外部环境里**的读数。
本机上没有能替代它的读数——这不是"还没做"，是"这台机器上做不出来"。

> 一次读数覆盖到的范围，与一条链路覆盖到的范围，
> 在"验证过了"这句话里是同一个东西——只不过前者的绿只覆盖它自己那一段。

★ 需要谁做什么：**给一个能跑真实自动执行的环境**（非 Windows，或把 §4 第 15 条那条边界解除）。
拿到之后，PRT-009 与 PRT-253 两条可以**一起**收口。

---

## 二、★★ 已经解除的（不要再当成"等人介入"）

我上一批汇总里列过三项。对着**今天的树**复核，三项**都变了**：

| 原列项 | 今天的实际状态 | 证据 |
| --- | --- | --- |
| **0B · 那 5 把键进 runtime `envNames`** | ✅ **已交付** | `6ff673d` feat(§0B)："那一个数组补上了 —— 四道范围检查第一次真的能到执行面" |
| **0B · PRT-009 的 `peakResource` 那一行** | ✅ **已交付** | `product/launcher/launcher.mjs:1270` `peakResource: x.peakResource ?? null,`（提交 `896fcc8`） |
| **§5 第 14 条**（那条两天前的证据） | ✅ **已收回并更正** | `1f5e2a6` docs(§5 第 14 条)：收回我自己那条证据 |

> 一条"等它提交"的阻塞，与一条"它已经提交了"的阻塞，
> 在**只看我上次记的那句话**的时候是同一个东西。

⇒ 那三项的阻塞理由是"在制品不许动"，而**那个在制品现在是已交付的提交**。
**从"需要你介入"的清单里撤下。**

★ 另：PRT-009 的 `estimated-cost` 也已结清（实测 **$0.045086 USD**，
数字**不抄进代码**，由 `scripts/prt/baseline-measure.mjs --pending` 从证据文件 × 记录的价目表重算）。

---

## 二之二、⚠️ 第 44 轮新量出来的：另一个会话的 `2f5a4b3` **引入了 4 个套件回归**

这不是"要你裁决"，是**要你知道有一批红不在我这边**，而且我**没有**去改它（会撞车）。

**先量，不先信。** 第 44 轮第三次用 `git worktree add --detach <commit>` 把**只有某一个提交**的树
单独签出来跑，于是把"哪一族红是谁造成的"分开了：

| 套件 | `d470687`（`2f5a4b3` **之前**） | `1ce5b90`（现在，**净树**） | 判定 |
| --- | --- | --- | --- |
| `runtime-host-row-dsh-process` | **10/10** | **4/10** | ✖ 回归 |
| `runtime-host-binding-unblocked-dsh-process` | **7/7** | **3/7** | ✖ 回归 |
| `runtime-host-registrar-row-dsh-process` | **6/6** | **4/6** | ✖ 回归 |
| `runtime-contract-cross-process` | **19/19** | **10/19** | ✖ 回归 |
| `model-api` | 15/15 | **15/15** ✅ | ★ **不是这个提交** |

⇒ 前四族共 **57 → 21**，是 `2f5a4b3`（§5 第 20 条 · 甲那条断链挂上去）引入的，**在净树上复现**。

★★ **`model-api` 是这一格最值钱的地方**：CI 日志里五族**同时**红、看着像同一个原因，
**按日志写就会把它也栽给那个提交**。分树复跑才发现它只在**脏树**上红——
是某个会话**未提交的在制品**造成的。**"同时红"不等于"同一个原因"。**

失败机理（`runtime-host-row-dsh-process` A 的原文）：

```text
service "legionRuntimeHostBinding" has been registered a...
补丁层 v1 未完全生效：legion-enforcement-runtime-host-registrar:
  行已挂载但未激活（等待依赖服务），不产生任何强制效果
```

即：那一行**挂上了**（`PATCH_LAYER_ROWS` 5 → **7** 行确实生效），但**没激活**
⇒ 补丁层没完全生效 ⇒ 真 DSH 进程里的读数与"没挂"仍不同形。

**为什么不我去修**：那是另一个会话**正在做的同一件事**（他们此刻还有
`product/process-manifest.mjs`、`product/launcher/*` 等十余个文件在改），
我动同一个面会撞车；而且这四族本来就是**回归判据**——它们红着，正是判据在说
"这一行挂上了但没真的生效"。⇒ **记进度、留给那个会话收口。**
（★ 也正因为如此，本轮**不能**给出"全量 CI 9/9 PASS"的读数：`test` 阶段红。）

---

## 三、留给你的三项决定（都不阻塞其他工作）

这三项从第 20～30 轮起一直挂着，是**裁决**而不是实现：

1. **§5 第 20 条** —— §9 那条链上**唯一一处硬断点**。它要的不是代码，是"这条链到底算不算完成"的判决。
2. **§5 第 16 条** —— 阶段 9 的产品级动作**由谁触发**（含 §7 那几个度量模块）。
3. **§5 第 13/15/18 条** —— 需要你本人给的事实（第 14 条我今天已经自己收回并更正了）。

---

## 四、这 8 轮（第 36～43 轮）我自己做了什么

主线只有一条：**把"声明表 + 手写复述"这个缺陷形状机械化**，然后让它自己去找缺陷。

| 轮 | 交付 |
| --- | --- |
| 37 / 38 / 39 | `spec-tests-7`、`design-boundaries`、`metrics-spec7` 三道新门禁 |
| 40 | `RUN_RECORD_OPTIONAL_FIELDS` 声明了却**没有消费者** ⇒ 补 `recordWiring()` |
| 41 | 新建 `declaration-mirrors` 门禁（扫全仓 238 张冻结表）+ 第 3 个真缺陷 |
| 42 | 收回**我自己上一轮发的**豁免：`WRITABLE_ROLES` 是两条静默失效路径 |
| 43 | 收回剩下两条豁免 ⇒ **豁免表空了**；`BPE_ARTIFACT_FIELDS` 少一个字段、`PREFLIGHT_VERDICTS` 汇总里被手写复述 |

**第 43 轮结束时的读数**：

    declaration-mirrors   238 张冻结表 / 判为装饰 0 张 / 豁免 0 条
    paths                 26/26      破验 10/10 咬住 0 漏网
    preflight             16 → 21/21
    bpe                   27 → 32/32
    全量 CI               .ci/r43  9/9 PASS, exit 0

★ **豁免表空**这件事本身值得记一句：三次豁免，三次都以"修好代码"收场——
**每一处边界情况最后都发现是代码的问题，不是判据的问题。**
（表没有删掉：它是"人判断"的留痕位，将来真遇到分不开的形状，理由写在那里，过期会被自动提醒。）

★ 第 42、43 两轮各遇到一次同一个性质：**两版实现行为等价 ⇒ 破验分不开**。
两次的出路都不是"加断言"，而是**把被跟随的那张声明做成可注入的参数**
（`writableDirsInsideInstall({ writableRoles, readers })`、
`classifyPreflightChecks(checks, { kinds })`、`parseTokenizerArtifact(raw, { fields })`）。

---

## 五、结论

- **可实现的部分：已完成。** 台账 145 行里 **140 行 ✅**，且每行都有可复跑证据。
- **剩下的 5 行不是"还没做"，是"本机做不动"**：2 条要外部环境（同一根因）、2 条要真人、1 条要等 4 天。
- **需要你做的**：上面第三节那三项裁决 + 第一节那个"能给一次真实自动执行的环境"。
- 目标**保持 active**：还有 4 ⏸ / 1 ⬜，按既定口径不标完成。
