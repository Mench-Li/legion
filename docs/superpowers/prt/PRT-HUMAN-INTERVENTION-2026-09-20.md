# PRT 人工介入汇总（2026-09-20）

> 目标：`docs/MULTI-AGENT-FEATURE-OPTIMIZATION.md` 的全部功能实现 + 最终报告。
> 本文件只回答一件事：**剩下的、本机做不动的，具体是哪几件、为什么、需要谁做什么。**
>
> 口径：HEAD `062130e`（树含 `2f5a4b3`、`5288a14`）；`.ci/r45c` 全量 CI **7/9，exit 1**
> （8 个红族**逐条取证**全在另一会话，见 §二之二）；最近一次 **9/9 PASS** 是 `.ci/r43`。
> 台账 `docs/superpowers/prt/PRT-PROGRESS.md`：**145 行 = 140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜**。
>
> ★ 本文件已**复核到第 45 轮**。§二、§三 各有一次"撤下"，理由都是同一句话：
> **一条"等它提交"的阻塞，与一条"它已经提交了"的阻塞，
> 在只看我上次记的那句话的时候是同一个东西。**

---

## 一、★ 4 条 ⏸ 里有 2 条是**同一个根因**

| 行 | 台账里的阻塞原话 | 归到哪一类 |
| --- | --- | --- |
| **PRT-009** | `peak-resource` 缺**实跑读数**；"卡在 `docs/STATUS.md` §4 第 15 条" | ⚠ **本机不存在那个读数** |
| **PRT-253** | "剩下的是**一次真的自动执行**（worker → Runtime 契约 → DSH → 模型），而那个前提在本机不成立" | ⚠ **同上一条** |
| **PRT-256** | "需真实外部用户" | 👤 需要**真人** |
| **PRT-910** | "需真实用户项目" | 👤 需要**真人** |
| **PRT-316**（🟡） | 日期门：加固提交 `0db37af` = 2026-09-10 + **14 天**一个发布周期 ⇒ 最早 **2026-09-24** | 🕐 **需要时间**（今天 2026-09-20，**还差 4 天**） |

**PRT-009 与 PRT-253 的根因是同一条**（两行的台账文字自己都指向 `docs/STATUS.md` §4 第 15 条）：
**这台机器（Windows）上不会发生"一次真的自动执行"**，而那两行各要一次**外部环境里**的读数。
本机上没有能替代它的读数——这不是"还没做"，是"这台机器上做不出来"。

> 一次读数覆盖到的范围，与一条链路覆盖到的范围，
> 在"验证过了"这句话里是同一个东西——只不过前者的绿只覆盖它自己那一段。

★ 需要谁做什么：**给一个能跑真实自动执行的环境**（非 Windows，或把 §4 第 15 条那条边界解除）。
拿到之后，PRT-009 与 PRT-253 两条可以**一起**收口。

---

## 二、★★ 已经解除的（不要再当成"等人介入"）

我上一批汇总里列过三项。对着**今天的树**复核，三项**都变了**：

| 原列项 | 今天的实际状态 | 证据 |
| --- | --- | --- |
| **0B · 那 5 把键进 runtime `envNames`** | ✅ **已交付** | `6ff673d` feat(§0B)："那一个数组补上了 —— 四道范围检查第一次真的能到执行面" |
| **0B · PRT-009 的 `peakResource` 那一行** | ✅ **已交付** | `product/launcher/launcher.mjs:1270` `peakResource: x.peakResource ?? null,`（提交 `896fcc8`） |
| **§5 第 14 条**（那条两天前的证据） | ✅ **已收回并更正** | `1f5e2a6` docs(§5 第 14 条)：收回我自己那条证据 |

> 一条"等它提交"的阻塞，与一条"它已经提交了"的阻塞，
> 在**只看我上次记的那句话**的时候是同一个东西。

⇒ 那三项的阻塞理由是"在制品不许动"，而**那个在制品现在是已交付的提交**。
**从"需要你介入"的清单里撤下。**

★ 另：PRT-009 的 `estimated-cost` 也已结清（实测 **$0.045086 USD**，
数字**不抄进代码**，由 `scripts/prt/baseline-measure.mjs --pending` 从证据文件 × 记录的价目表重算）。

---

## 二之二、⚠️ 第 44 轮新量出来的：另一个会话的 `2f5a4b3` **引入了 4 个套件回归**

这不是"要你裁决"，是**要你知道有一批红不在我这边**，而且我**没有**去改它（会撞车）。

**先量，不先信。** 第 44 轮第三次用 `git worktree add --detach <commit>` 把**只有某一个提交**的树
单独签出来跑，于是把"哪一族红是谁造成的"分开了：

| 套件 | `d470687`（`2f5a4b3` **之前**） | `1ce5b90`（现在，**净树**） | 判定 |
| --- | --- | --- | --- |
| `runtime-host-row-dsh-process` | **10/10** | **4/10** | ✖ 回归 |
| `runtime-host-binding-unblocked-dsh-process` | **7/7** | **3/7** | ✖ 回归 |
| `runtime-host-registrar-row-dsh-process` | **6/6** | **4/6** | ✖ 回归 |
| `runtime-contract-cross-process` | **19/19** | **10/19** | ✖ 回归 |
| `model-api` | 15/15 | **15/15** ✅ | ★ **不是这个提交** |

⇒ 前四族共 **57 → 21**，是 `2f5a4b3`（§5 第 20 条 · 甲那条断链挂上去）引入的，**在净树上复现**。

★★ **`model-api` 是这一格最值钱的地方**：CI 日志里五族**同时**红、看着像同一个原因，
**按日志写就会把它也栽给那个提交**。分树复跑才发现它只在**脏树**上红——
是某个会话**未提交的在制品**造成的。**"同时红"不等于"同一个原因"。**

失败机理（`runtime-host-row-dsh-process` A 的原文）：

```text
service "legionRuntimeHostBinding" has been registered a...
补丁层 v1 未完全生效：legion-enforcement-runtime-host-registrar:
  行已挂载但未激活（等待依赖服务），不产生任何强制效果
```

即：那一行**挂上了**（`PATCH_LAYER_ROWS` 5 → **7** 行确实生效），但**没激活**
⇒ 补丁层没完全生效 ⇒ 真 DSH 进程里的读数与"没挂"仍不同形。

**为什么不我去修**：那是另一个会话**正在做的同一件事**（他们此刻还有
`product/process-manifest.mjs`、`product/launcher/*` 等十余个文件在改），
我动同一个面会撞车；而且这四族本来就是**回归判据**——它们红着，正是判据在说
"这一行挂上了但没真的生效"。⇒ **记进度、留给那个会话收口。**
（★ 也正因为如此，本轮**不能**给出"全量 CI 9/9 PASS"的读数：`test` 阶段红。）

---

## 三、留给你的三项决定（都不阻塞其他工作）

这三项从第 20～45 轮起一直挂着，是**裁决**而不是实现
（★ 第 45 轮把原第 1 项 §5 第 20 条撤下、换上 §5 第 28 条，**仍然是三项**）：

1. **§5 第 16 条** —— 阶段 9 的产品级动作**由谁触发**（含 §7 那几个度量模块）。
2. **§5 第 13/15/18 条** —— 需要你本人给的事实（第 14 条已自己收回并更正）。
3. **§5 第 28 条** —— 那条"出站车道"的**文件落点**（`甲/乙/丙` 三个选项）。

---

### 三之一、★★ 第 55 轮对账：上面的"**三项**"是一份**精选**，不是 §5 的全部开口

本轮给 §5 决策表补了一张**机器可读的状态索引**（`MULTI-AGENT-FEATURE-STATUS.md` §5.0.1），
它逐行抄出每一条**自己写下的**显式状态标记。读数：

    已裁决 **4**（#1 #14 #19 #20）· 待施工 **0** · 待裁决 **5**（#9 #16 #18 #27 #28）· **未标注 20**（共 29 行）

★ 本文件 §三 列的三项（#16 · #13/#15/#18 · #28）**全部落在这 25 条"未显式裁决"里**
（#16、#18、**#28** 是"待裁决"；#13、#15 是"未标注"）—— 所以**它们没有过期**。

★★ **但"三项"不等于"只有三项"**：§5 那 29 行里有 **20 行没有写明自己还需要谁**。
★★★ 而"未标注"的准确含义是"**机器判不出**"，**不是**"还开着" —— 详见 §5.0.1 里那张
"四种假阳性"表（提及 / 否定 / 子项 / 交叉）。那个词表是**故意窄**的：
一次错误的"已裁决"会把一行**从你的队列里悄悄抹掉**，比多留一行坏得多。

> 一张"状态写在散文里"的清单，与一张"状态写在列里"的清单，
> 在**读者从头读到尾**的时候是同一个东西 ——
> 而清单存在的理由，正是**没有人会从头读到尾**。

⇒ 请把 §5.0.1 那张索引表当作**候选队列**（29 行），本文件 §三 是**其中确定要你裁的三项**。
★ 我**没有**替你把那 20 行判成"已裁决"或"还开着" —— 那是裁决，不是抄写。
索引表由 `scripts/prt/reachability.mjs` 的 `decisionStateRows()` 派生，
判据 `reachability.test.mjs` ⑭/⑭b/⑭c 盯着"索引 = 今天派生出来的值"（陈旧即红）。

---

### 三之三、★★★★★ 第 63 轮新查出：**端到端门禁是红的，而执行器今天就拒绝自动执行**

★ **这是本文件里分量最重的一条，请先看它。**

前 6 轮我都在核"文档说的与产物是否一致"。这一轮去读**真正的端到端门禁**怎么说的 ——
`.ci/r51`（`acffc8d`）9 阶段里 **`test` 与 `smoke` 两阶段 FAIL**，逐套件 **13 条套件级 FAIL**。

★★ 我没停在存档上，**在当前 HEAD 上直接复跑**，确认它**今天仍然红**：

| 套件 | 读数 |
|---|---|
| `runtime-contract-cross-process` | **9 红** |
| `dsh-composition-runtime-host-row-dsh-process` 等**三个** | 合计 **11 红** |
| `model-api` | **1 红** |

⇒ 实测 **21 条红**。

★★★ 根因（已钉到证据，`scratch/_probe-r63-selfcheck.mjs`）：执行器自检报
`EXECUTOR_SELF_CHECK_INCOMPATIBLE`，`reasons` 逐字是

```
composition-patch-layer: legion-enforcement-runtime-host-registrar:
    行已挂载但未激活（等待依赖服务），不产生任何强制效果
composition-patch-layer: legion-enforcement-runtime-contract-server: 同形
```

- `patch-layer.mjs:319-333` 的 `isRuntimeOnlyRow` 判据是「`module === null` 且有 `runtimeModule`」；
- 这两行的 `module` **都不是 null** ⇒ **不在** `RUNTIME_ONLY_ROW_IDS` 里 ⇒ 被按"**强制面行**"对账；
- 而 `runtime-host-row.mjs:587` 写着 `inject: [ENFORCEMENT_ROOT_SERVICE]` —— 它**在等一个依赖服务**。

★★★★ **这是 `2f5a4b3`（§5 第 20 条 · 您选甲，2026-09-20 另一会话落地）的落地后果。**
而那两行**自己的注释**（`patch-layer.mjs:267-304`）**明说这是设计**：

> 「真实部署里四项必需能力里三项**如实报未确认** ⇒ 自检不兼容 ⇒ `autoExecutionForbidden: true`」
> 「挂与不挂的**行为等价**，区别只在拒绝从"没人挂"变成"查过、且拒了"」

⇒ **按产品设计，执行器今天在真部署上就是拒绝自动执行的。**
这也正对上台账里 **PRT-009 / PRT-253 两条 ⏸** 的原因（"要一次**真的自动执行**"）。

#### 要您裁的：三条路，各有代价

| # | 路 | 代价 |
|---|---|---|
| **甲** | 让这两行走"**非强制面**"豁免（不进强制面对账） | ★ 等于**放宽一个 fail-closed 的自检**。这个项目一路在防的就是这类改动 —— 我不自己动 |
| **乙** | 改那 21 条用例的**期望**（承认"自检不兼容"是当前正确行为） | ★ 等于把"执行器不可用"写成预期。门禁会变绿，而**产品仍然不能自动执行** |
| **丙** | 在组合里**真的 provide** `legionEnforcementRoot`，让两行激活 | ★ 需要那个服务的**真实现**；而"三项能力如实报未确认"里 `usage-reporting` 的理由是**硬的**（引擎的 `SubagentResult` 契约**没有**用量字段） |

★ **我选的是"不动、报上来"**：让这两行豁免，是在我不知道的地方把一个自检变松；
而"变绿"与"变可用"是两件事 —— 这条路我判不了，也不该替您判。

#### ★ 第 64 轮把上面那个"13 条"**收窄成一簇** —— 请以这里为准

第 63 轮我报的是"逐套件 **13 条** FAIL"。第 64 轮把每一条**单独复跑**，结果差别很大：

| 存档里的 FAIL | 单独复跑 | 判 |
|---|---|---|
| `boundary-facts`（`pass=48 fail=3`） | **61/61 绿** | 已自愈 |
| `suite-counts`（被杀） | 绿 | **假红** |
| `run-plane`（被杀） | **20/20 绿** | **假红** |
| `event-delivery`（被杀） | **7/7 绿**（1.4s） | **假红** |
| `context-tokenizer-wiring`（被杀） | 绿 | **假红** |
| `chat-l1-smoke`（输出为空） | **35/35 绿，exit=0** | **假红** |
| `product-launcher`（`413/1`） | `launcher.test.mjs` **35/35 绿** | 那条未复现 |
| `p13-host-injection`（被杀） | 需 `@dsh-external/dsh-team-hub` **构建产物**，未测 | 未测 |
| **4 个 PRT-253 执行面套件** | **21 红 · 每次都复现** | **★ 真的** |

⇒ **13 条里 9 条是资源争用的假红；真的只有 1 簇，就在自动执行那条路上。**

> 一个把「因资源争用被杀」与「真的挂住」记成同一个读数的阶段，
> 会让**八条假红把一条真红淹掉** —— 而报告的人只会说"CI 是红的"。

★ 这一条**修正**了第 63 轮的措辞：不是"CI 大面积红"，而是
**"恰好一簇红，而它正是自动执行那条路"** —— 后者比前者更严重，因为它精确。
★ 一条顺带的好消息：存档里 `boundary-facts` 那时是 `pass=48 fail=3`，
而**现在 HEAD 上 61/61 全绿** —— 那一处已自愈，不用管。

★ 另一件要说清的：`.ci` 下**几百个**存档里，最后 5 个**没有一个是 9 阶段全绿**的。

> "台账 140/145 ✅"与"端到端门禁全绿"是**两支不同的账** ——
> 前 6 轮我一直在核对前一支的内部一致性，而**没人去看后一支**。

---

### 三之二、★ 第 45 轮复核：撤下 §5 第 20 条（你已裁决、且已落地）

它原来在这里写的是「§9 那条链上**唯一一处硬断点**」。
对着**今天的树**复核，那句话**已经过期了**：

| 原列项 | 今天的实际状态 | 证据 |
| --- | --- | --- |
| **§5 第 20 条**（§9 链唯一硬断） | ✅ **已裁决、已落地** | 你 **2026-09-20 裁决「甲」**；`2f5a4b3` feat(§5 第 20 条 · 甲)："那条断链挂上了" |

`node scripts/prt/alpha-chain-trace.mjs` 今天的读数：

    ✔ L1…L6、L8 —— 有活实现
    △ L7  —— 软缺口  ⇒ 归属 §5 第 28 条
    △ L9  —— 软缺口  ⇒ 归属 §5 第 16 条
    最先硬断的一节：**（没有）**
    断点归属：**全部有归属**

⇒ **"§9 链上唯一的硬断"已经不存在了。** 剩下的两处是**软缺口**
（支撑模块没人挂），且都**有归属**、都挂在别人的决定上。

> 一条"等它提交"的阻塞，与一条"它已经提交了"的阻塞，
> 在**只看我上次记的那句话**的时候是同一个东西。
> （§二 已经因为同一件事撤下过三项；这是第四项。）

★★ **而它换出来一件新的、真的需要你看一眼的事**：那次落地**引入了 4 个套件回归**
（净树复现）—— 见 §二之二。所以第 20 条**不再需要裁决**，
但它**留下的那 4 个红套件**是新的账。

---

## 四、这些轮（第 36～45 轮）我自己做了什么

主线只有一条：**把"声明表 + 手写复述"这个缺陷形状机械化**，然后让它自己去找缺陷。

| 轮 | 交付 |
| --- | --- |
| 37 / 38 / 39 | `spec-tests-7`、`design-boundaries`、`metrics-spec7` 三道新门禁 |
| 40 | `RUN_RECORD_OPTIONAL_FIELDS` 声明了却**没有消费者** ⇒ 补 `recordWiring()` |
| 41 | 新建 `declaration-mirrors` 门禁（扫全仓 238 张冻结表）+ 第 3 个真缺陷 |
| 42 | 收回**我自己上一轮发的**豁免：`WRITABLE_ROLES` 是两条静默失效路径 |
| 43 | 收回剩下两条豁免 ⇒ **豁免表空了**；`BPE_ARTIFACT_FIELDS` 少一个字段、`PREFLIGHT_VERDICTS` 汇总里被手写复述 |

**第 43 轮结束时的读数**：

    declaration-mirrors   238 张冻结表 / 判为装饰 0 张 / 豁免 0 条
    paths                 26/26      破验 10/10 咬住 0 漏网
    preflight             16 → 21/21
    bpe                   27 → 32/32
    全量 CI               .ci/r43  9/9 PASS, exit 0

★ **豁免表空**这件事本身值得记一句：三次豁免，三次都以"修好代码"收场——
**每一处边界情况最后都发现是代码的问题，不是判据的问题。**
（表没有删掉：它是"人判断"的留痕位，将来真遇到分不开的形状，理由写在那里，过期会被自动提醒。）

★ 第 42、43 两轮各遇到一次同一个性质：**两版实现行为等价 ⇒ 破验分不开**。
两次的出路都不是"加断言"，而是**把被跟随的那张声明做成可注入的参数**
（`writableDirsInsideInstall({ writableRoles, readers })`、
`classifyPreflightChecks(checks, { kinds })`、`parseTokenizerArtifact(raw, { fields })`）。

| 44 | ① PRT-009 `peak-resource` **端到端真读数第一次走通**（真子进程 140MB → 真采样器 → 真落盘 → 真读回，逐字段相等）；② 抓到"**只在失败路径才印**的读数是反的"（主动停止 **0** 条日志 vs 非主动退出 1 条）⇒ 修后 A 1 / B 1（`supervisor` 22 → **25/25**，破验 5/5）；③ §5 第 20 条落地 ⇒ **§9 链上唯一的硬断消失**（`alpha-chain` 12/14 → **14/14**）|
| 45 | ★★★ 把"**认不出的标记就跳过**"做成**可证伪**的（往合成台账放一个今天不存在的第 5 个标记 🔵）⇒ 量出**两个**解析器静默丢行 ⇒ 台账状态词表收敛到**一处所有**；★★★ 并用**假变异检验（M3）**证明那条判据**有分辨力**（"永远通过"与"实现真的对"在输出上都是 ✔）。★ 同一形状在**第二份文档**（功能对照表）上又遇到一次：**13 种**所有者许可的形状被静默丢掉 ⇒ 同一条修法。★ 又查出**我自己**上一段"只改了一半"的注释（`NON_DONE_STATUSES` 还是手写的）与"**普查用大网、执法用小网**"（生成物词表 6/5 项 vs 普查 9 项）|
| 46 | ★★★ **同一个问题在本仓被回答了两遍** —— `tallyLedger` 是台账行规则的**第二个所有者**（自己判行、自己分格、自己写四个 `if` 分档）。真台账上两个所有者**今天一致**（145 = 140/1/4/0），但 ① 四种脏格子里 **3 处**接受规则分歧；② 词表加第 5 个标记 ⇒ `total=5` 而四档之和 **4**。⇒ 判定权交回所有者 + 分档改成派生（`tallyKey`）+ 平衡自检 + `marks` 可注入。★★ 顺手挖出**闸门自身**的潜在缺陷：`handover-ledger-tallies` 两侧用 `JSON.stringify` ⇒ **对象的插入顺序**成了一条没人打算立的规矩（改动后当场报红而**五个数一个都没变**）⇒ 加 `canonicalJson()`。★★ T3 被强杀 ⇒ 源文件留在变异形态（第 44 轮后第二次）⇒ 给变异脚本加了**哨兵** |
| 47 | ★★★ 同一张功能对照表的**第三个**手写解析器（`spec-status-calibration.parseStatusTable`）。真文档上两个解析器**一致**，但 ① 状态格 `🔵` ⇒ 它静默算成 **🟡**（`aggregate` 的 `其余 ⇒ 🟡`），而所有者**抛**；② 4 格 / 7 格的状态行 ⇒ 它**静默跳过**。★ 旁证**就在同一文件里**：`parseCalibration()` 用 `< 4` 而它用 `≠5 且 ≠6`。⇒ 判定权交回所有者。★★ **而本轮把我自己第 45 轮的断言拦住了** —— 它钉的是**写法**（要求本地别名）而新消费者**直接**用那个正则 ⇒ 改成钉**性质**，并复跑 F1 确认仍咬住 |
| 48 | ★★★ 同一张功能表的**第四个**手写解析器（`feature-landing-paths.parseLandingCells`）。真文档上**四个**解析器一致（都是 29 行），但 4 格 / 7 格的功能行被它**静默跳过**。★★ **后果比第 47 轮更重**：该模块管的是"声明的代码落点必须指向存在的文件"，跳过一行 ⇒ **它声明的路径一个都不会被核**，而门禁报"全部通过"。⇒ 把**行识别器**也放进叶所有者（`featureTableRow` + `FEATURE_ROW_MIN_CELLS`，与台账的 `ledgerTaskRow` **对称**），四个消费者全部转手；其中 `spec-status-calibration` 是**同一文件两处**各自手写。★★ **续**：由一次**全仓扫**又找出**第五份**（`feature-table-status.parseFeatureRows`）—— 而这一份**做对**了"报出跳过"（第 23 轮 F-15 的成果），缺陷是**归错类**（把 4/7 格的真功能行说成"另一张表"）⇒ "一份'跳过并报出来'的清单比'静默跳过'好得多，但它仍然可能把**自己看不懂的东西**说成**别人的东西**"。★★ 而**"全仓扫"这个动作本身**也记一笔：前四份靠"逐个文件找"（依赖我**想得到**那个文件），第五份是一次模式匹配找出来的 |
| 49 | ★★★ **一张声明表里 21 条理由靠"位置"说话** —— `prt-reachability-baseline.json` 的 `reason`（44 条里 21 条用"同上"/"上面那条"），而那份 reason 正是**本清单写给你看的依据**。★★ **有一处已经指错了**：`runtime/toolcall/spool.mjs` 写着「它的消费者就是**上面那条**收账侧」，而"上面那条"今天是 `runtime/packs/store.mjs`（裁决处 **§5 第 19 条**），真正的收账侧在下标 **2** ⇒ "一条相对指称在表里插进一行无关条目之后，会**改变另一行说的话**，而不改变那一行的任何一个字"。量出 **5 条违规**（3 处"同上"想指的是**同组但不紧邻**的条目）⇒ 新增 `positionalReasonViolations()` + 8 处改为**指名引用**。★★ **变异抓出我自己的一条假控制**（`X2` 没咬住：用例里测"裁决处不同"的那条被"类不同"抢先覆盖）★ 探针第一版也报过一个**假问题**。★ 已知残留：相邻同类且都无裁决处的两条看不出"同上"是否同组 |
| 50 | ★★★ **第 49 轮那条规则的"已知残留"当天就兑现了** —— 我写下的残留是"两条相邻、同类、都不声明裁决处的条目看不出来"，而 `product/release/checklist.mjs` 的"同上"紧挨着 `product/metrics-spec7.mjs`（一个**指标口径**模块），仅因为"都是 gap、都写 §5 第 16 条"被放行 ⇒ "一条判据的'已知残留'如果**当天就能兑现**，那它不是残留，是它**允许**的一种错"。⇒ 改成**严格**：**一条位置指称都不许**，**21 → 0 条**、无残留；14 条全部改成**指名 + 量出来的事实**。★ 而"量出来"逼出第二课：探针第一版走进 `.worktrees/`（兄弟工作树）且按**文件名**匹配（`from './context-retention.mjs'` 被算成 `retention.mjs` 的 import 者）—— "一个按**子串**匹配的探针，报出的是'某个名字里含这个名字的东西'，而它和'这个名字本身'在输出里长得一模一样"；★ 同坑第 49 轮已踩一次 ⇒ 这版把"路径分段匹配"与"跳过兄弟工作树"写成显式并**断言**其生效。★ 本轮先读全 §5 第 28 条，确认 L7/L9 是产品+架构的**三选一决定**（甲/乙/丙），**不是缺实现** ⇒ 保持不动 |
| 51 | ★★★ **唯一一份写给人照做的文档，此前一条判据都没有** —— `git grep -l PRT-HUMAN-INTERVENTION -- '*.mjs'` **零命中**，而 `PRT-PROGRESS`/`PRT-HANDOVER`/`MULTI-AGENT-FEATURE-STATUS`/`prt-reachability-baseline` 四份都有人核 ⇒ "一份**要人照着做**的清单，如果它自己没有人核，它错的时候只有**读它的人**会发现 —— 而那个人正是**因为不知道答案才来读它**的"。★ 而它**已经错了**：标题停在"第 45～47 轮"（家族表已到 50）、总数停在 201（真值 **216**）⇒ `boundary-facts` 补第 20/21 条（标题轮次 = 家族表最大轮次；总数 = 块内各项之和）。★★ **三次被机制抓住我自己**：① 新判据**当场**抓到我（改了 49→51 却把总数留在旧值 214）；② 框架**通用反面控制**抓出 claim **只钉住一端**（区间标题的左端纯装饰，改成 `第 46～50` 判据不红 —— "一条钉住'两端说法'其中一端的判据，与一条钉住整句的判据，在改动**另一端**之前是同一个读数"）；③ `ANCHOR_AMBIGUOUS` 抓出文档里有**两块**同名读数块 ⇒ 用独有后缀 `（全部可复跑）` 钉死。★ 而**我自己的用例**犯了同一个歧义错 ⇒ 报的是「判据测不出过期」，**而坏的是控制不是判据**。★ 顺带修掉一处不属于我的漂移（`handover-tracked-suites` 373 vs 374）|
| 52 | ★★★ **交付物自己的目录也犯了同一族** —— 最终报告 §三 是**逐轮留档**（第 16 轮起，34 个小节按轮次编号），而其中的 **39～43 是倒着放的**（43 / 42 / 41 / 40 / 39），44、45 又跳到了 39 前面；顶层标题还写着"第 15 轮做了什么"。⇒ "一份**看起来按编号排好了**的文档，与一份真的排好了的文档，在**只看前几节**的时候是同一个读数 —— 读者从 16 一路看到 38 都是升序，**合理地**假设后面也是"。★ 改成按**块**搬（带"行数守恒 + 多重集相等"断言：只允许搬动，一个字符都没改），标题改成"截至第 51 轮"。★★ 补 `boundary-facts` 第 22 条（标题里的轮次 = §三 小节轮次最大值）+ 三条用例（真仓库全序递增 / 反转真实小节必须报出倒序 / **区间写法不许被静默跳过**）。★ 而探针**第一版**用 `第 (\d+) 轮` ⇒ `3.0j 第 24～29 轮` **被静默跳过**，输出里看起来"该查的都查了"（同一个坑第 51 轮刚踩过）。★ 另：用 PowerShell 改脚本把 CJK 编坏（`Set-Content -Encoding utf8`）——`node --check` 拦下了，报告**没有**被写坏。 |
| 53 | ★★ **一条判据盯着的数是个"移动靶"** —— `handover-tracked-suites` 把交接报告里的"套件清单完备 N 个"与 `git ls-files` 绑在一起，而**任何人加一个测试文件它就会过期**（第 53～54 两轮共 **6** 次跟随：374→375→376→378→379→**380**）。★ 查清后确认**这是设计意图、不是缺陷**：那个 N 是 `run-ci.mjs` 每次 CI 在 `stage` 阶段**逐字打印**的真值（`.ci/r51/ci.log` L50 `PASS 套件清单完备（373 个 *.test.mjs 全部有归属）`），报告里是**手抄**。⇒ 保留严格比对、继续跟随（"真实 + churn"胜过"安静 + 过期"）。★★ 而这一轮**真正**的教训是我的：`suite-counts` 报 10 条过期声明，**我的提取窗口设成了 dump 的第 30～130 行**，于是一份 10 条的清单我只看到 8 条，还据此怀疑"判据漏了第 175 行"。⇒ 第二版全文档替换又报"命中 2 次"，我一度以为文档变了；真因是**非主表里那句散文是历史读数、必须冻结**，只能**按行**改。> 一条**自己只扫了一半**的探针，报出来的是"对方漏了"，而对方是完整的。 |
| 54 | ★★★ **交付物自己的「验证与门禁」有一份没人盯的副本，于是它飘了 30 轮** ——最终报告 §四 有四行读起来像"当前值"（第一行本来就写着"（第 22 轮收口）"，**§四 自己是冻结读数表**，问题只出在这四行**没写轮次**）：套件数 **350**（真值 379）、`boundary-facts` **34/34**（真值 54）、五个套件 **331/331**（真值 82）、不可达 **46**（真值 **44**，连数都错）。⇒ "一个被两处声明的数，与一个被一处声明的数，在**两处恰好还相等**的那些天里是同一个读数"—— 而抄的那一份**没人盯**。★ 修法**不是再抄一遍新数字**（30 轮后同样飘），而是**删掉重复、给留下的补上轮次、把"当前值"指给唯一持有者**。★★ 顺手补 `sectionFourBareCurrentReadings()` + 三条用例：⑲ 真报告 0 条、⑲b 拿掉冻结标记必须报、⑲c **标了**轮次的同一行**不许**报（判据不许惩罚合法历史）+ 对称控制"§四 **之外**的同名行不许被扫"。★ 订正**我自己**刚写错的一处：说明句把"套件数"也算给了人工清单，而它的持有者是 `PRT-HANDOVER` §二。★ 套件数同日第 **6** 次跟随（→**380**）——"移动靶"是设计意图已在第 53 行记明，而本轮把它**当读数**看的结论升级了：一轮红一次、每次都不是缺陷，产出的不是信号而是**训练人忽略它** ⇒ 下一轮该把它改成"指名某一次 CI 运行"的忠实转抄。 |
| 55 | ★★★ **写给人照做的那张清单，没有状态列** —— §5 决策表有 **29 行、5 列**，而**没有一列是状态** ⇒ "某一行还开不开着"此前只能靠通读散文判出来；而交付里说"需要人工介入的只有**三件**"。⇒ "一张**状态写在散文里**的清单，与一张**状态写在列里**的清单，在**读者从头读到尾**的时候是同一个东西 ——而清单存在的理由，正是**没有人会从头读到尾**"。★ 补 §5.0.1 **状态索引**（由 `reachability.mjs` 的 `decisionStateRows()` 派生，判据 ⑭/⑭b/⑭c 盯陈旧），只抄每行**自己写下的**显式标记，**不替业主判定**：已裁决 **4** · 待裁决 **4** · **未标注 21**（共 29）。★★ **读数会随"扫哪一格"在 20 与 26 之间变动**（实测三次：扫 3 格 26、扫前 4 格 26、扫整行 20）⇒ "一个状态读数会随扫描面变动，说明它当时还不是一个读数，只是一个印象"；定为**整行**（标记位置本身不一致：`已裁决` 有时在"需要谁"格，而"不决定则…"只出现在最后一格；掐掉最后一格会让 4 条"待裁决"全部消失 ——**那是把读数改小，不是把表变准**）。★★★ 而判据**当场抓到我自己的假阳性**：#28 被判成"已裁决"，真因是那一行里写的是"各自与**已裁决事项**的关系"——`已裁决` 是该行**提到**的词（指**别的行**），不是对**这一行**的裁决 ⇒ 加否定前瞻 `已裁决(?!事项|的)`。★ 与 `suite-counts` 那个坑同形（他们代码里记着 `peakResource` 是**字段名**、"判据 47 例"说的是**另一套**判据，却被提取成计数声明）。> 一个认不出"这一行**提到**了已裁决"与"这一行**是**已裁决"的模式，> 报出来的是"这一行已裁决"——而它读起来跟真的一样。 |
| 56 | ★★★ **审计我自己上一轮那个识别器的词表 —— 结论是"不加词"** ——第 55 轮它报"未标注 21"，本轮把"收尾说法"放宽，结果 **7 条命中里 4 条是假阳性**，四种形状各不相同：**提及**（#21「台账里每一条非 ✅ 的行」）、**否定**（#28「没有把第 15 条记成已关」）、**子项**（#6「B1 已关」——关的是 B1，本行讲 C 系列）、**交叉**（#13「剩下的并进第 19 条」）。⇒ "散文里的关闭与**提到**关闭在正则眼里长得一样，而它们的后果相反：一个把这一行移出人的队列，一个不该"。★ 于是**不加词**：一次错误的"已裁决"会把一行**悄悄抹掉**，比多留一行坏得多；并把"未标注 = **机器判不出**，不是还开着"写进 §5.0.1 的表头。★ 钉成 ⑮/⑮b/⑮c（三种假阳性）+ ⑮d（正向对照：显式裁决/开口径必须认出来）+ ⑮e（#28 回归）。★★★ **而写 ⑮e 的夹子时抓到一个真的洞**：真文档里那两个词被 markdown 从中间劈开了 ——#28 逐字是「**不决定**则…」与「各自与**已裁决事项**的关系」，于是 `/不决定则/` **匹配不到**、`/已裁决(?!事项)/` 也判不出来。> 一个被 markdown 加粗从中间劈开的短语，与一个**不存在**的短语，> 在任何一条正则眼里都是同一件事。⇒ 不在词表里堆 `\*{0,2}`（写 N 处、漏一处就静默失效），而在**匹配之前**统一去掉 `**` 与反引号。★ 归一化后 **#28 从"未标注"变成"待裁决"** —— 与人工清单说它还开着**一致**；读数随之 已裁决 4 · 待裁决 **5** · 未标注 **20**。★ 判据**当场报陈旧**（索引没跟上派生值），重生成即绿。 |
| 57 | ★★★ **两处错误互相抵消，总数看起来是对的 —— 而总数正是所有人核对的那一格** ——交付物 §一「结论」那张表把 🟡 与 ⬜ 写成 `0 / 1`，而台账真值是 `1 / 0`。★★ **两个数正好互换 ⇒ 总数照样是 145**，任何只核「总数」或只核「✅」的检查都看不见它 ——而「总数 145 / 已完成 140」正是所有人会去核对的那两格。它旁边那句散文「**本批把最后一个 🟡 收掉了**」也因此是假的：🟡 就是 PRT-316（日期未到），在那里已经很多轮了。★ 为什么此前没有判据：`handover-ledger-tallies` 只盯**交接报告**（那一侧写的是对的）；`spec-progress` 只报 `140/145`（不含分档）；`ledger-evidence` 只盯**单行**证据 ——**没有任何一条判据核过交付物 §一 那张表**。★ 新增事实 `report-section-one-ledger-tallies`（`boundary-facts` **22 → 23 条**），逐格核**五个数**、键序无关、与交接报告同一条派生源；`boundary-facts` 那个**通用反面控制**（从真实值造一个错值）自动覆盖了它。★ 真文档破验 **5/5**：把 🟡/⬜ 改回互换的错值 ⇒ 红；并**顺带证出**被改坏的版本里**总数仍是 145** —— 那正是它此前藏住自己的原因；还原后逐字节相同。★★ 而这一轮我自己**两次**踩同一个 CRLF 坑：`b.indexOf("\n  }),\n")` 在 CRLF 文件上找不到收尾（插入器报"找不到收尾"），计数正则 `Object\.freeze\(\{\n\s+id:` 数出 **0** 条（把"插入成功"报成失败）——> 一个按 `\n` 找东西的探针，在 CRLF 文件上会把"没找到"报成"它不存在"。 |
| 58 | ★★★ **同一个数被抄了 10 处，只有 2 处有判据** —— 台账分档 `140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜` 在仓库 5 份文档里被抄 **10 处**：交付物 L5/L1747、交接报告 L15/L36/L697、人工清单 L8、状态文档 L2055/L3759/L4004，而只有 2 处有判据（`handover-ledger-tallies` 管交接报告那一行、`report-section-one-ledger-tallies` 管交付物 §一 那张表）。★★ 而"哪处现行、哪处历史"**没有本地信号**：先按 ±2 行找轮次/提交号/`.ci`，结果把交付物**第 5 行**（开头那句现行摘要）判成了历史留档 ——"第 45 轮"恰好出现在它附近，而它离真正的历史段落还有 40 行。> 一个按"附近有没有轮次标记"分类的探针，会把文档**开头那句现行摘要**判成历史留档。⇒ 改用**文档自己声明的结构边界**：交付物有 `## 三、逐轮留档`；标题**之前** = 现行（必须等于台账），**之后** = 历史（免检）。★ 并 **fail closed**：留档区里若有抄写**不在** `### 3.0*` 小节内，也报红—— 否则"把现行读数挪到标题后面"就成了绕过判据的办法。★★ 写这条判据时又抓到一个形状：**同一个数在同一份文档里用两种形状写着** ——开头是「四档连写」（`140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜`），§一 那张表是**每格一个数**；> 而任何**一条**正则只认其中一种。（第一版断言写 `>= 2`，实测只有 1 —— 就是被这个两种形状绊的。）★ 破验 **4/4**：改错开头那句 ⇒ ⑯ 红；把错的抄写塞进留档区 ⇒ fail closed 红；还原逐字节相同。★★★ 而破验脚本第一版把**三次全量 58 例**跑进一个 590s 窗口 ⇒ 第 4 次被砍死，而**被砍死的进程不会跑 `exit` 处理器** ⇒ 破验 B 的改动**留在了文档里**，靠哨兵才抓回来。> 一个把改动写进磁盘、再靠 `exit` 处理器还原的脚本，在被强制杀死时**什么都不会还原** ——> 而"被杀死"和"跑完了"在退出码上都是非零。⇒ 破验改用 `--test-name-pattern` **只跑那一条**（4 次跑完，远低于窗口）。 |
| 59 | ★★★ **把第 58 轮的"多盯 2 处"改成"分类必须完整"** —— 第 58 轮只覆盖了交付物自己的 2 处（靠它自己声明的 `## 三、逐轮留档` 边界），剩下 **8 处**仍然没人管。★ 而"哪处现行、哪处历史"在别的文档里没有现成边界 —— 逐个看上下文才发现：现行的那几处都在**声明为读数的段落**里（交接报告 `## 一、一句话结论`、`## 二、最终读数（都是机器读数，可复跑）`、人工清单抬头），历史的那几处都在**描述"某轮做过什么"的叙述段**里。★★ 做法改成**登记**：`TALLY_LIVE`（4 处，必须等于台账）+ `TALLY_FROZEN`（5 处，写明出处），判据 ⑰ 要求两份清单**一处不落地**对上全部抄写。> "10 处里只有 2 处被盯"这件事，不能靠"多盯 2 处"来修 ——> 只要还剩下一处没人登记，同一个缺陷下次就换个地方复现。★★★ 而它第一次跑就报"有 1 处没有登记"，抓到的正是**我在第 58 轮写下的家族表第 58 行** ——那一段正文**把台账分档又引了一遍**。> 记录"同一个数被抄了 10 遍"这件事，本身又抄了第 11 遍 ——> 而抓住它的，正是那条**刚刚为这件事写的判据**。⇒ 家族表的编号行给它一条**规则**（= 历史），不逐条登记（逐条登记会每轮补一次，"补一下"正是这类缺陷的来源）。★ 破验 **5/5**：改**交接报告 §一**那句 ⇒ 红；改**人工清单抬头**那句 ⇒ 红（**这两处此前完全没有判据**）；**新增第 11 处**抄写 ⇒ 报"没有登记";还原逐字节相同。★ 顺带：`declared.size` 与"两份清单长度相加"在这条判据里**不相等**（家族行走规则、不在清单里），用相加就会少算它 —— 断言消息里已把三个数都打出来。★ 同时复核了时间敏感项：今天 **2026-09-20**，PRT-316 最早 **2026-09-24**（还差 4 天），台账仍 140/145。 |
| 60 | ★★★ **改了一节，不等于改掉了那个错信念** —— 第 57 轮我修了交付物 §一 那张表里的 🟡/⬜ 混淆，**没修 §2**。而 §2 是「**这是需要您裁决的部分**」——人真正照着做的那一节。实测它在那里还写着两处：§2.0 那张一页纸表 `| — | **PRT-316**（时间） | … | **台账里唯一的 ⬜** |`（★ 而 PRT-316 是台账里唯一的 **🟡**），以及 §2.1 的注 `标题里的"5 项"是**台账**的非 ✅ 行数（**1 ⬜ + 4 ⏸**）`。> 同一个错信念写进两节，改了一节不等于改掉了它。★★ 而前几轮的判据**只认「四档连写」**那一种形状（`140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜`）—— 这两处是**散文形状**，一条都没被盯。⇒ 新增判据 ⑱：现行区里 ① `N <标记>` 的 N 必须等于台账那一档；② `唯一的 <标记>` 的标记必须是台账里**恰好为 1** 的那一档。★ 破验 4/4：把「唯一的 🟡」改回「唯一的 ⬜」⇒ 红；把「1 🟡 + 4 ⏸」改回「1 ⬜ + 4 ⏸」⇒ 红；还原逐字节相同。★ §2.0 那一行还带一个**按构造就会过期**的写法：「最早 2026-09-24（**今天还差 5 天**）」——今天（2026-09-20）其实是 **4 天**。⇒ 改成只写**绝对日期**，不写"还差几天"。> 一个"今天还差 N 天"的写法，在它被写下的第二天就开始错。 |
| 61 | ★★★ **同一节里「第 N 条」指两个东西** —— 交付物 §2 现行区里，**相邻两行**用同一个短语指两个编号空间：L131「…而那正是**第 1 条**」= §2.0 表 `先看` 那一列的**排名 1**；L132「解掉它同时关掉**第 15 条**（PRT-610）」= **§5 第 15 条**。★ 而两者都不带限定词。> 同一节里"第 N 条"指两个东西时，读者分不出自己读到的是哪一个 ——> 而分不出的地方，恰好是"先看哪一格"这句话最要紧的地方。★ 文档自己早写对了：L118「（**§5** 第 20 条 · 甲）」、L130「（★ **§5** 第 **25** 条）」；而 L122 讲同一件事时用的是**「行」**。⇒ ① 排名那两处改用「行」（与 L122 自己的写法一致）；② 现行区里**其余 14 处** §5 引用一律补上限定词（原来全不带）。★ 新增判据 ⑲：现行区里 `第 N 条` 必须带 `§5`/`决策表` 限定词，**且**那个 N 必须是 §5 里真有的条号。★ 破验 4/4：去掉 `§5` 限定词 ⇒ 红；引用 `§5 第 99 条`（不存在）⇒ 红；还原逐字节相同。★ 顺带：判据自己也要**证明它扫到了东西**（`valid.size >= 29`）——否则"§5 一条都没读进来"与"每一条都对"是同一种绿。 |
| 62 | ★★★ **一个否定结果：我打算做的那个改动，测量不支持它** —— 第 61 轮发现台账里 `server.mjs` 的引用 9 轮内衰减两次（`2351 → 2363 → 2375`），都是**别人改行数**造成的，于是我提出"把引用从**行号**改成**符号**"。真要动手前先量规模：台账里 `path:line` 引用 **117 处**（去重 110），本地可解析 34 处。★ 然后写了个探针：引用**后面**出现的标识符 = 被引的符号，再看符号离被引行多远 ——读数一度是"**7 处里有 4 处漂移**，d=73 / 7 / 5 / 4"。★★★ **逐条按文件里的真实位置复核，四处全是假阳性，台账的引用是对的：**`authority.mjs:759` 那一句讲的是 **`narrowToGrant`**（759 正是 `permit = narrowToGrant({…})`），而我的探针抓的是**下一句**提到的 `normalizeManifest`；`composition.test.mjs:710` 就是被引的那个用例本身（L710 = `test('自检：六项全过 → enforcement-effective…')`），而探针抓的是被**断言的值** `incompatible`；`dsh/index.mjs:129/144` 同理。> 一个把引用**后面**出现的标识符当成"被引的符号"的探针，> 会把**四处处处正确**的引用报成"全部漂移"。⇒ **不发那个判据。** 现有 `ledger-line-citations-resolve` 核的是"行号存在且非空"，那正是它**打算**核的性质；我没有量出它不够。> "我本来打算做的那个改动，测量不支持它"也是一个结果 ——> 而不写下来的否定结果，会被下一轮**重新做一遍**。★ 这是本批**第三次**同一种失效（第 54 轮只读了半段 dump、第 56 轮词表扫面、本轮符号归属）：**"从邻近文本推断指代"的探针，推错了会把正确的东西报成错的 —— 而"报错"和"报对"在输出里长得一样。**★ 追加一条纪律：**推断指代的探针，在报"发现"之前必须先按**被指对象的真实位置**复核至少三条。**★ 探针文件保留，但头部写明 `UNSOUND — DO NOT BUILD ON THIS`。 |
| 63 | ★★★★★ **端到端门禁（整套 CI）是红的 —— 而台账说 140/145 已完成** —— 前 6 轮我都在核"文档说的与产物是否一致"，这一轮去读**真正的端到端门禁**怎么说的。`.ci/r51`（`acffc8d`，我第 51 轮那次）9 阶段里 **`test` 与 `smoke` 两阶段 FAIL**，逐套件 **13 条套件级 FAIL** + 1 条 smoke FAIL。★★ 我没停在存档上：**在当前的 HEAD 上直接复跑**，确认它**今天仍然红**：`runtime-contract-cross-process` **9 红**、三个 `dsh-composition-runtime-host-*` 合计 **11 红**、`model-api` **1 红** ⇒ 实测 **21 条红**。★★★ 根因钉到证据上（`scratch/_probe-r63-selfcheck.mjs`）：执行器自检报 `EXECUTOR_SELF_CHECK_INCOMPATIBLE`，`reasons` 逐字是「`composition-patch-layer: legion-enforcement-runtime-host-registrar: 行已挂载但未激活（等待依赖服务），不产生任何强制效果`」和 `…runtime-contract-server…` 同形。而 `patch-layer.mjs:319-333` 的 `isRuntimeOnlyRow` 判据是「`module === null` 且有 `runtimeModule`」，这两行的 `module` **都不是 null**（`./plugins/runtime-host-registrar-row.mjs` / `./plugins/runtime-contract-server-row.mjs`）⇒ **不在** `RUNTIME_ONLY_ROW_IDS` 里 ⇒ 被按"强制面行"对账。而 `runtime-host-row.mjs:587` 写着 `inject: [ENFORCEMENT_ROOT_SERVICE]` —— 它**在等一个依赖服务**。★★★★ **这是 `2f5a4b3`（§5 第 20 条 · 业主选甲，2026-09-20 另一会话落地）的落地后果。**而那两行自己的注释（`patch-layer.mjs:267-304`）**明说这是设计**：「真实部署里四项必需能力里三项**如实报未确认** ⇒ 自检不兼容 ⇒ `autoExecutionForbidden: true`」、「挂与不挂的**行为等价**，区别只在拒绝从"没人挂"变成"查过、且拒了"」。⇒ **按产品设计，执行器今天在真部署上就是拒绝自动执行的。**★ 所以这**不是**一个我该自己"修"的缺陷：让这两行走"非强制面"的豁免，等于**把一个 fail-closed 的自检放宽** —— 那正是这个项目一直在防的那类改动。**⇒ 列为裁决项（见 §三之三），我不动它。**★ 一条顺带的好消息：存档里 `boundary-facts` 那时是 `pass=48 fail=3`，而**现在 HEAD 上 61/61 全绿** —— 那一处已经自愈，不用管。★ 另一件要说清的：`.ci` 下**几百个**存档里，最后 5 个**没有一个是 9 阶段全绿**的。> "台账 140/145 ✅"与"端到端门禁全绿"是**两支不同的账** ——> 前 6 轮我一直在核对前一支的内部一致性，而没人去看后一支。 |
| 64 | ★★★ **把"13 条红"收窄成"1 簇真红"** —— 第 63 轮报的是"逐套件 13 条 FAIL"。本轮把每一条**单独复跑**：`event-delivery` **7/7 绿（1.4s）**、`run-plane` **20/20 绿**、`context-tokenizer-wiring` 绿、`chat-l1-smoke` **35/35 绿 exit=0**、`suite-counts` 绿、`boundary-facts` **61/61 绿**（已自愈）、`launcher.test.mjs` **35/35 绿**；`p13-host-injection` 需 `@dsh-external/dsh-team-hub` 构建产物（未测）。⇒ **13 条里 9 条是资源争用的假红；真的只有 1 簇（4 套件 21 红），就在自动执行那条路上。**★ 那些"被杀"的条目在存档里写的是「套件超过 300s 被杀（已连后代进程一起清理）：可能存在泄漏句柄或死锁」——**它把"因资源争用被杀"与"真的挂住"记成同一句话**，于是 8 条假红把 1 条真红淹掉了。> 一个把「因资源争用被杀」与「真的挂住」记成同一个读数的阶段，> 会让**八条假红把一条真红淹掉** —— 而报告的人只会说"CI 是红的"。★ 这一条**修正**了第 63 轮的措辞：不是"CI 大面积红"，而是**"恰好一簇红，而它正是自动执行那条路"** —— 后者比前者**更严重**，因为它精确。★ 同时提醒：`test` 阶段那一次跑了 **2486 秒**（>40 分钟）——在那个负载下"300s 被杀"是**预期的**，把它当产品缺陷读会指向错的方向。 |

**第 64 轮结束时的读数**（全部可复跑）：

    declaration-mirrors   238 张冻结表 / 判为装饰 0 张 / 豁免 0 条
    progress-check        20 → **29/29**      intervention-coverage 16 → **18/18**
    boundary-facts        44 → **61/61** + CLI 23/23 红 0
    feature-evidence      10 → **12/12**      feature-table  17 → **19/19**
    spec-status-calibration 13 → **15/15**    ledger-evidence **11/11**
    feature-landing-paths  → **15/15**        feature-table-status 8 → **9/9**
    alpha-chain-trace **14/14**               doc-table-integrity **10/10**
    reachability          10 → **21/21**      suite-counts 11 → **13/13**
    ⇒ 十三个套件合计 **247 通过 / 0 失败**
    破验                   M1/M2/M3 · F1/F2/F3 · D1/D2/D3 · T1/T2/T3 · V1/V2 · W1/W2/W3/W4
                           · X1/X2/X3 · Y1/Y2 · Z1/Z2
                           —— **全部咬住，全部逐字节还原**
    全仓手写解析扫描        `!= 5 && != 6` ⇒ **无输出**（五份手写全部清除）
    全量 CI                `.ci/r45c` 7/9、`.ci/r47` 7/9、`.ci/r51` 6/9（★ r51 跑在**边跑边改**的树上，读数见交接报告那一行的限定）

★★ **第 44～48 轮是同一个缺陷家族的五次显形**，而仅第 48 轮一张表就有**五份**手写解析：

| 轮 | 份数 | 显形的样子 |
| --- | --- | --- |
| 44 | — | 词表**漏了一个标记** ⇒ 报 144 而台账 145，**而 144 正好能过门禁** |
| 45 | 2 | **同一个词表被抄成两份**（台账一处 + 功能表一处），13 种合法形状被静默丢掉 |
| 46 | 2 | **同一个问题被回答了两遍**（行怎么认、标记归哪一档），答案各自正确却互相不一致 |
| 47 | 3 | 认不出就归成"部分完成" |
| 48 | **5** | 跳过的行**连它声明的代码落点都不核了**；最后一份把"看不懂的"说成"别人的" |

> 这五次的共同点不是"某处写错了"，而是
> **一个已经有答案的问题，被重新回答了一遍，而第二次的答案更弱**。
> 更弱的那个不会报错——它只是**安静地少算一点 / 少读几行**。

★★ 结构上，第 45 与第 48 轮落地的是**同一个动作**，只是各做了一边：

    第 45 轮：`progress-check.ledgerTaskRow`    ← 台账那边
    第 48 轮：`progress-check.featureTableRow`   ← 功能表这边（对称的那一个）
    两者都是：**叶所有者导出识别器，消费者只转手、不再自己认**

★★ **第 45 轮最值得留下来的一句**（它适用于本批**每一个**判据）：

> "🔵 会抛"**区分不了**这两种实现：
> ① 它查的是那张词表（**派生**）；② 它自己写死一份（**抄了一份**）。
> 两者在**今天**行为完全一样——
> 只不过前者加第 5 个状态只改一处，后者要改 N 处，而漏掉的那几处**不报错**。
>
> ⇒ 所以真正的工作不是"再写一条断言"，而是**把被跟随的那张表做成可注入的参数**，
>   然后**故意把缺陷放回去**（假变异检验），看判据是不是真的红。

★ 这已经是**第六次**遇到"两版实现行为等价 ⇒ 破验分不开"（第 42、43、45、46、47、48 轮）。
**六次的出路完全相同**：把被跟随的那张声明做成**可注入的参数**
（`marks` / `minCells` / `nonDoneStatuses(marks)`），
然后**故意把缺陷放回去**（变异），看判据是不是真的红。

★ 而第 45 轮还两次踩到"**判据先扫到了别的东西**"：
① 一条扫**源码**的判据先扫到了**记录这个缺陷的注释**（⇒ 假红）——
而那个文件的开头**自己就写着**"这正是不该让判据去扫源码的原因"，我读到了还是先写了一个；
② 名单里**照抄真路径**，被另外两条判据读成"清单声明"、当成**生产入口**。
⇒ 两次的处置都不是加豁免，而是**删掉那张名单、改为扫目录**。

---

## 五、结论

- **可实现的部分：已完成。** 台账 145 行里 **140 行 ✅**，且每行都有可复跑证据。
- **剩下的 5 行不是"还没做"，是"本机做不动"**：2 条要外部环境（同一根因）、2 条要真人、1 条要等 4 天。
- **这 5 行每一行都有 §5 归属**（`intervention-coverage` 读数：**0 条无归属**）——
  "没有任何人在等它"这件事**已经发生过一次**，现在不会再发生。
- **需要你做的**：上面第三节那三项裁决 + 第一节那个"能给一次真实自动执行的环境"。
- ★★ **第 45 轮新增一件需要你知道的事**：另一会话的 `2f5a4b3`（就是你裁决「甲」的那条落地）
  **引入了 4 个套件回归**、**净树复现** ⇒ 那条链虽然通了，
  它**留下的账**（4 个红族）还没销，见 §二之二。
- 目标**保持 active**：还有 4 ⏸ / 1 🟡，按既定口径不标完成。

---

## 六、★ 一句话给到你的

**能做的都做完了**（140/145，每行有可复跑证据；§9 商业 Alpha 九节链**没有硬断**）。
**剩下的 5 行，每一行都卡在"本机不会发生的事"上**：

1. **给一台能跑真实自动执行的环境** ⇒ 一次收口 **PRT-009 + PRT-253**（同一个根因）。
2. **三名真实用户 / 一个真实项目** ⇒ 收口 **PRT-256 + PRT-910**。
3. **等到 2026-09-24**（还差 4 天）⇒ 收口 **PRT-316**。

外加**三项裁决**（§三）：第 16 条（阶段 9 由谁触发）、第 13/15/18 条（需要你给的事实）、
第 28 条（出站车道的文件落点，甲/乙/丙）。

★ 以及一件**不需要你决定、但需要你知道**的：另一会话的 `2f5a4b3`
引入了 **4 个套件回归**（净树复现）——那是账，不是我这边的活。
