# 手动磨刀器（SKU-6 / TS-2S-200）· import 就绪包（T-011, scope=shop）

- 产出角色：军团士兵「上架参谋」（soldier-listing）
- 任务：T-011（scope=shop）—— 磨刀器 import 前就绪包：5 项待闭环清单 + 载荷 schema 自检
- 状态结论：**NOT READY（不可导入）** —— 5 项待闭环未完成；字段级发现 8 处（其中需处置 7 处），全部处置完成后才可执行 `/v3/product/import`
- 依据文件（均为只读实读，未改动）：
  1. `listing-draft-sharpener.md`（223 行）—— 上架草案（类目、属性、红线、定价）
  2. `import-payload-sharpener.json`（46 行）—— 待自检的 import 载荷模板
  3. `ozon-api/products.md`（36 行）—— Ozon Seller API 上传/更新商品官方文档
- 证据等级沿用草案约定：A=官方/平台 API；B=权威机构；C=平台页面样本；D=媒体/经验

---

## 1. 字段级 schema 自检发现表

> 对照基准说明：`ozon-api/products.md` 为 /v3/product/import 的**流程级**文档（图片须为云存储**直链**、请求设置主价格、返回 `task_id`、经 `/v1/product/import/info` 查状态、审核 <1 天、上架需有库存），**未包含** vat/old_price 等字段枚举；因此对文档未覆盖的字段，处置项标注「对照官方 OpenAPI schema 终核」，不臆造枚举值。

| # | 字段（载荷路径） | 当前值 | 官方要求（products.md / 草案） | 风险等级 / 处置 |
|---|---|---|---|---|
| 1 | `items[0].images` | `[]`（空数组） | products.md：图片以**云存储直链**形式随 import 上传；草案 §6：主图白底、建议 4–8 张 | **高**：空 images 无法建立商品卡/过审。处置：闭环图片真实 URL（待闭环项 3）后再导入 |
| 2 | `items[0].weight` | `200`（g）占位 | 草案 §5/§9：weight 为推断占位；利润模型按 0.25 kg 毛重测算 | **中高**：重量影响履约费/仓储费与申报，占位值禁止导入。处置：净重实测替换（待闭环项 1） |
| 3 | `items[0].width / height / depth` | `200 / 50 / 60`（mm）占位 | 草案 §5：`200x50x60` 为示例占位，**待实测** | **中**：尺寸影响 FBO 体积重/仓储计费。处置：实测 ДхШхВ 替换（待闭环项 1） |
| 4 | `items[0].attributes[4389]`（Страна-изготовитель） | `{"value":"Китай"}`，**缺 dictionary_value_id** | 草案 §1.2：4389 为字典属性（dictionary_id=1935，is_collection=true）；Ozon 字典属性须带真实 `dictionary_value_id` | **高**：缺 id 导入会被拒或取不到值；且 «Китай» 为推断值。处置：确认原产国后从字典 1935 拉取真实 id + value（待闭环项 2） |
| 5 | `items[0].old_price` | `"0"` | products.md 未覆盖；官方 schema 中 old_price 为可选划线价 | **中**：`"0"` 语义待确认（等于现价或 0 可能被判无效/被忽略）。处置：对照 /v3/product/import OpenAPI 确认；建议置空或填真实划线价 |
| 6 | `items[0].vat` | `"0"` | products.md 未覆盖；vat 为枚举字符串，取值与主体（跨境 vs 本土）税率相关 | **中**：`"0"` 是否合法取决于 schema 版本与跨境主体适用税率。处置：对照官方 schema + Ozon Global 招商确认（关联待闭环项 5） |
| 7 | `items[0].attributes[85]`（Бренд） | `{"value":"Нет бренда"}`，**缺 dictionary_value_id** | 草案 §1.1：85 为字典属性（dictionary_id=28732849）；草案 §4 品牌决策已审批为白牌 | **中**：字典属性缺 id 可能导入失败；需从品牌字典 28732849 拉取 «Нет бренда» 的真实 dictionary_value_id。**注：此为本次自检新增发现，不在 `_meta.pending_before_import` 原 4 项内，须一并闭环** |
| 8 | `_meta.pending_before_import` | 4 项未闭环（净重、尺寸、原产国 id、图片 URL） | 模板自声明；与草案 §10 待补证清单一致（草案另有品牌决策=已审批、履约费复核、白名单确认） | **高**：模板自声明未闭环，与字段实际状态（发现 1–4）互相印证。处置：按 §2 五项待闭环清单逐项闭环后方可导入 |

### 1.1 已核验通过项（对照草案，非发现）

| 检查项 | 结论 | 依据 |
|---|---|---|
| `offer_id` 一致性 | ✅ `_meta.offer_id` = `items[0].offer_id` = `TS-2S-200` | 载荷行 5/18；注：草案 §4 提示型号码「具体以最终货源 SKU 编号为准」，货源确认后可回填 |
| 类目/类型一致性 | ✅ `category_id=17027907`、`type_id=94462`，与草案 §0/§1 一致 | 载荷行 21–22 |
| 必填属性齐全 | ✅ 3 个必填属性（8229 Тип / 85 Бренд / 9048 Название модели）均已填 | 草案 §1.1；载荷行 29–31 |
| 标题长度 | ✅ 86 字符 ≤ 100 | 草案 §0/§2；载荷行 19 |
| 价格字段类型 | ✅ `price="899"` 为字符串，符合官方 price 字段类型；已审批 899 ₽ | 草案 §0/§7；载荷行 23 |
| 单位字段 | ✅ `weight_unit="g"`、`dimension_unit="mm"` 已带单位 | 载荷行 39/43 |

---

## 2. 5 项待闭环清单（import 前必须闭环）

| # | 待闭环项 | 负责人 | 所需输入 | 完成标准 | 验收方式 |
|---|---|---|---|---|---|
| 1 | **净重与尺寸实测** | 采购/寻源参谋 | 货源实测数据（称重/量尺记录） | `weight`（净重 g）与 `width/height/depth`（ДхШхВ mm）替换占位值 200 与 200x50x60；`weight_unit`/`dimension_unit` 保持 g/mm | 与货源称重/量测记录一致；实测记录存档备查，抽样复核 |
| 2 | **原产国确认** | 寻源参谋 | 货源产地信息（1688/供应商页面、产地证明） | 属性 4389 填真实 `dictionary_value_id`（字典 1935 内对应国家 id），`value` 文本同步 | 与 1688/供应商页面一致；dictionary_value_id 为字典真实返回，非臆造 |
| 3 | **图片真实 URL** | 上架参谋/用户 | 白底主图 + 细节图 4–8 张托管 URL（云存储直链） | `images` 数组填满（主图 ≥1 必需，草案 §6 建议 4–8 张）；符合主图规范（白底、商品占画面 ≥50%、≥500×500 px） | URL 可访问（HTTP 200）；无盗图/水印/Logo/促销文字；图面与实物/描述一致 |
| 4 | **报关预归类终核** | 报关行 + 合规参谋 | ЕТТ ЕАЭС 官方归类结论 | HS 6804（14%）vs 8205（3%）二选一终核，并据此复核利润模型与 899 ₽ 定价 | 报关行书面结论（邮件/文档）存档；利润测算（草案 §7）同步更新 |
| 5 | **跨境/Global 主体类目白名单书面确认** | 合规参谋 + Ozon Global 招商 | 招商书面确认 | 确认 `disabled=false`（类目 17027907 / type 94462）适用于**跨境主体**上架 | 官方/招商回复存档（工单/邮件/截图）；结论回填草案 §9 |

> 闭环顺序建议：2（原产国）→ 1（实测）→ 3（图片）可并行；4、5 为独立链路；全部闭环后由执行参谋生成最终载荷并送将军审批执行。

---

## 3. 纪律声明

1. **结论全部基于文件实读**：本报告对 `listing-draft-sharpener.md`、`import-payload-sharpener.json`、`ozon-api/products.md` 三份文件逐行读取后成文；未读取的内容不作断言。
2. **未实测值保持「待闭环」**：净重、尺寸、原产国、图片 URL、dictionary_value_id 均未臆造任何数字或字典值；发现 5/6（old_price/vat）因官方文档未覆盖枚举，明确标注「待官方 schema 终核」，未替其假定合法值。
3. **`import-payload-sharpener.json` 未改动**：本任务仅产出 `import-readiness.md` 一个文件，未修改/删除任何其他文件。
4. **无写接口调用**：未调用 taskctl、看板写接口或任何 HTTP 写操作；状态迁移由将军负责；未联网下载。
5. 载荷的最终修改（替换实测值、补 dictionary_value_id、填 images 等）不属于本任务范围，须在 5 项闭环完成后由执行参谋另建产出并经将军批准。

---

## 4. 结论

- **就绪状态：NOT READY** —— 字段级发现 8 处（高/中高风险 7 处 + 模板自声明 1 处），5 项待闭环全部未完成，`/v3/product/import` **不得执行**。
- **新增发现**：属性 85（Бренд）为字典属性但缺 dictionary_value_id（发现 7），不在载荷 `_meta.pending_before_import` 原 4 项内，需纳入闭环。
- **阻塞点**：工具链路无阻塞（read/write 均可用）；业务阻塞 = 5 项待闭环所需外部输入（实测数据、产地信息、图片托管、报关行结论、招商确认）尚未到位，等待对应负责人/外部方提供。
