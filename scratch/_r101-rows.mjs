// scratch/_r101-rows.mjs —— 第 101 轮：同类的洞再找一处，★ 而"把宽度从 2 改成 3"只是把墙挪位置
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 100 轮结束时的读数**（全部可复跑）：', '**第 101 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 100 轮**', '本文件已**复核到第 101 轮**')

const ANCHOR = '## ★★★★★ 第 100 轮：里程碑'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 100 轮里程碑那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 101 轮：顺着第 100 轮那个洞，**在同一天里又找到一处同类的** —— 而修法差点又是"把墙挪一格"',
    '',
    '第 100 轮修的是 `/^\\| (\\d{2}) \\|/`（**恰好两位**）—— 家族表跨到第 100 行时它静默失明。',
    '★ 本轮顺着这个形状在同一个文件里再搜，找到**两处**：',
    '',
    '| 位置 | 写法 | 后果 |',
    '|---|---|---|',
    '| `boundary-facts.test.mjs`（`isFamilyHistoryRow`） | `/^\\|\\s*\\d{1,2}\\s*\\|/` | ★★★ **第 100 行不再被"历史行"豁免** |',
    '| `boundary-facts.test.mjs`（`第 N 条` 引用扫描） | `/第\\s*(\\d{1,2})\\s*条/g` | 条号过 99 之后，那处引用**看不见** ⇒ 越界检查**不报**（静默漏检） |',
    '',
    '#### ★★★ 而第一处的病，比"直接红"更贵',
    '',
    '实测（`scratch/_probe-r101-width.mjs`）：',
    '',
    '```',
    'L1474  | 100 | ★★★★★ **里程碑…**',
    '       匹配 \\d{1,2} 豁免？ ★ 否（会被当成"抄写"）',
    '       正文含台账分档串？ 否 ⇒ 今天侥幸不红',
    '```',
    '',
    '⇒ 第 100 行**今天不红**，只是因为它正文里没引那句分档串。',
    '★ 而**下一行**只要引一次 `140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜`，本判据就会报「有一处**没有登记**」——',
    '**而真正的病根是"豁免规则假定了编号宽度"**。',
    '',
    '> ★★★★ 一个会给出**误导性诊断**的洞，比一个直接红的洞更贵：',
    '> 它把人引去**补登记**，而补完那一行，下个月再来一行同样会红。',
    '',
    '#### ★★★★★ 而本轮差点又踩一次同样的坑 —— **我先把 `\\d{1,2}` 改成了 `\\d{2,3}`**',
    '',
    '★ 改完跑套件，**我自己刚加的回归用例当场把它打红**：',
    '',
    '```',
    'AssertionError: 家族行的豁免规则认不出编号 1000 的行',
    '```',
    '',
    '⇒ `\\d{2,3}` 只是**把墙从 100 挪到 1000** —— **同一个洞换个位置**。',
    '',
    '> 一个"把宽度从两位改到三位"的修法，与一个"不再假定宽度"的修法，',
    '> 在"下一次语料长过那个宽度"这件事上是同一个东西。',
    '',
    '★★ 所以我把它改成 **`\\d+`（不设宽度）**，并把那条回归用例的输入扩到 `9 / 99 / 100 / 101 / 1000 / 10000` ——',
    '让它**永远不可能靠"再挪一格"通过**。',
    '',
    '#### ★ 一条诚实的边界（不假装覆盖）',
    '',
    '那条豁免规则**只看行首那个编号**，所以它也会豁免本文档里**别的** `| N |` 表',
    '（例如第 100 轮那张"九个阶段"表）。那些行今天不含分档串、不进 `copies` ⇒ 无害。',
    '★ 而**真要区分"哪张表是家族表"，靠的不是行首编号** —— 本轮不假装解决它，只把它写下来。',
    '',
    '#### ★★★★★ 附：本轮负控时**真的出了事故，而它按设计被接住了**',
    '',
    '跑第二个变异（`\\d{2,3}`）时，命令**超时被杀**（590s 上限），于是**脚本的还原那行没跑到** ⇒',
    '`scripts/prt/boundary-facts.test.mjs` **被留在变异状态**、`scratch/.mutant-in-progress.json` **哨兵还在**。',
    '',
    '★ 我下一步做的第一件事就是**读哨兵** —— 它还在，于是立即还原并清哨兵，随后核对：',
    '',
    '- 活规则回到 `/^\\|\\s*\\d+\\s*\\|/` ✔',
    '- 哨兵已清 ✔',
    '- `git diff` 里剩下的 `\\d{2,3}` 只出现在**我有意写的注释与用例文案**里 ✔',
    '- 套件回到 **62 通过 / 2 失败**（那 2 条是台账引用，非本轮）✔',
    '',
    '> ★★ 一个"**变异脚本会在结束时还原**"的设计，与一个"**它可能跑不到那一行**"的事实，',
    '> 在**它被杀掉的那一刻**是不同的东西 —— 而**哨兵就是为这一刻准备的**。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK 第 101 轮小节已插入（在里程碑那节之前）')
}

if (!/^\| 101 \|/m.test(t)) {
  const ROW = '| 101 | ★★★★★ **顺着第 100 轮那个洞在同一文件里又找到两处同类的** —— 而修法差点又是"把墙挪一格"。'
    + '★ 两处：① `isFamilyHistoryRow` 的 `/^\\|\\s*\\d{1,2}\\s*\\|/` ⇒ **第 100 行不再被"历史行"豁免**；'
    + '② `第\\s*(\\d{1,2})\\s*条` ⇒ 条号过 99 后那处引用**看不见**、越界检查**不报**（静默漏检）。'
    + '★★★ 而①的病**比直接红更贵**：实测第 100 行**今天侥幸不红**（正文没引分档串），'
    + '而**下一行**只要引一次 `140 ✅ / 1 🟡 / 4 ⏸ / 0 ⬜`，判据就报「有一处**没有登记**」——'
    + '**而病根是"豁免规则假定了编号宽度"** ⇒ 一个**误导性诊断**会把下一班人引去补登记，补完下个月再来一行照样红。'
    + '★★★★★ 而本轮差点又踩同一个坑：**我先把 `\\d{1,2}` 改成了 `\\d{2,3}`**，'
    + '结果**我自己刚加的回归用例当场把它打红**（`认不出编号 1000 的行`）⇒ `\\d{2,3}` 只是**把墙从 100 挪到 1000**。'
    + '> 一个"把宽度从两位改到三位"的修法，与一个"不再假定宽度"的修法，'
    + '> 在"下一次语料长过那个宽度"这件事上是同一个东西。'
    + '★★ 已改成 **`\\d+`（不设宽度）**，且回归用例的输入扩到 `9/99/100/101/1000/10000` ⇒ **不可能靠"再挪一格"通过**。'
    + '★ 诚实边界：那条规则**只看行首编号**，因此也会豁免别的 `| N |` 表（第 100 轮那张"九个阶段"表）——'
    + '那些行今天不含分档串、不进 `copies` ⇒ 无害；而**真要区分"哪张表是家族表"靠的不是行首编号**，本轮不假装解决。'
    + '★★★★★ 附：本轮负控时**真的出了事故，而它按设计被接住了** —— 跑第二个变异时命令**超时被杀**（590s），'
    + '**脚本的还原那行没跑到** ⇒ 文件被留在变异状态、**哨兵还在**；我下一步第一件事就是读哨兵，立即还原并核对'
    + '（活规则回到 `/^\\|\\s*\\d+\\s*\\|/` · 哨兵已清 · `git diff` 里剩的 `\\d{2,3}` 只在我有意写的注释与用例文案里 · 套件回到 **62/2**）。'
    + '> 一个"**变异脚本会在结束时还原**"的设计，与一个"**它可能跑不到那一行**"的事实，'
    + '> 在**它被杀掉的那一刻**是不同的东西 —— 而**哨兵就是为这一刻准备的**。 |'
  const lines = t.split('\n')
  const i100 = lines.findIndex((l) => /^\| 100 \|/.test(l))
  if (i100 < 0) bad.push('  x 找不到 | 100 |')
  else { lines.splice(i100 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 101 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
