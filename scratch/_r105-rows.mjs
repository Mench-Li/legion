// scratch/_r105-rows.mjs —— 第 105 轮：把第 104 轮那个洞**修在机制上**（给源码「原文」引用立判据）
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 104 轮结束时的读数**（全部可复跑）：', '**第 105 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 104 轮**', '本文件已**复核到第 105 轮**')

const ANCHOR = '#### ★★★★★ 第 104 轮：一条**漂了 414 行**的引用'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 104 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 105 轮：把第 104 轮那个洞**修在机制上**，而不是只修那一处实例',
    '',
    '第 104 轮我修了 `port.mjs` 的指针（`2241 → 1827`），并在文档里写下：',
    '',
    '> ★ 本轮只修了**一处实例**，没修**产生它的机制**。',
    '',
    '★ 本轮把那句话兑现了 —— 给这一类**立判据**。',
    '',
    '#### 一、为什么这一层能做机械判定，而台账那层不能',
    '',
    '| 层 | 读什么 | 判什么 |',
    '|---|---|---|',
    '| `ledger-line-citations-resolve`（已有） | **台账** `PRT-PROGRESS.md` | 只判"落点**存在且在范围内、不是空行**" |',
    '| ★ `source-original-citations-on-line`（本轮新增） | **源码注释** | 判"**那句引文在不在那个行号上**" |',
    '',
    '★★ 差别在**注释里抄没抄原句**：抄了（`…原文：「…」`）⇒ "引文在不在这行"是**可机械判定**的；',
    '没抄（只给坐标）⇒ 要判它必须读语义，做不了。',
    '',
    '> ★ 所以这一层比台账那层**强**，但也**更窄** —— 它只覆盖"同时写了行号又抄了原文"的那种。',
    '> **绿 ≠ 所有引用都对。** 这句边界已写进判据的 `why` 里。',
    '',
    '#### 二、立完之后立刻量到：**3 条引用，只有 1 条对**',
    '',
    '```',
    'total=3  ok=1',
    'broken:',
    '  product/launcher/legacy-data-adoption.mjs:130      → launcher.mjs:108  引文不在这一行',
    '  product/launcher/legacy-data-adoption.test.mjs:77  → launcher.mjs:108  引文不在这一行',
    '```',
    '',
    '★★★ 而这两处**不是**并行会话这次编辑造成的 —— 我逐字核过：',
    '',
    '| 那句话在哪 | 行号 |',
    '|---|---|',
    '| **HEAD** 的 `launcher.mjs` | **`:113`** |',
    '| 工作区（该文件此刻 ` M `） | **`:118`** |',
    '| 引用写的 | **`:108`** ← **两处都不是它** |',
    '',
    '⇒ `108` 处是**另一段**注释（`PORT_ENV_KEYS` 那张表的「每个键都必须在进程清单的 `envNames` 里声明过」）',
    '⇒ **这条引用在 HEAD 上就已经错了**（差 5 行），**不是**这次编辑造成的。',
    '',
    '#### ★★★ 三、那两处**本轮不动**，但写进读数 —— 而不是让判据一直红',
    '',
    '引用它们的两个文件（`legacy-data-adoption.mjs` 与它的 `.test.mjs`）**都是 ` M `**',
    '（并行会话正在编辑）⇒ 此刻去改会与他们的在制品撞车。',
    '',
    '★ 于是我把这两处**登记进 `FACTS` 的 `expect`**（与 `TALLY_LIVE` / `TALLY_FROZEN` 同一手法）：',
    '',
    '> ★★ 一条"把已知坏引用写进读数"的判据，与一条"因为它坏所以一直红"的判据，',
    '> 区别在于**下一处新漂移**能不能被看见：**后者会被人习惯性忽略（一直是红的），而前者一有新东西就变。**',
    '',
    '★ 并且**在单元测试里再钉一遍**（用例 ㉓ 的 ②）：那两处必须在、且**总数必须恰好是 2** ——',
    '所以"有人顺手清空 `expect` 让它绿"与"新漂移发生了"**都会红**。',
    '',
    '#### ★★★★★ 四、验证（三步，缺一不可）',
    '',
    '| 步 | 动作 | 读数 |',
    '|---|---|---|',
    '| ① | 立判据后跑 CLI | ✔ **绿**（`expect` 登记了那两处） |',
    '| ② | **变异**：把 `port.mjs` 的指针**改回 `2241`**（漂 414 行那个原样），另起进程 | ★ **✔ → ✖**，判据咬住；哨兵已清、文件已还原 |',
    '| ③ | 单元测试 ㉓：真仓解析面 + 登记面 + **纯判定函数的正反两向** | **62 → 63 通过** |',
    '',
    '★★ 第 ③ 步里我把判定抽成了一个纯函数（`originalQuoteOnLine(lines, lineNo, quote)`）**专门为了能被测试直接喂输入** ——',
    '否则那层"扫目录"的逻辑只能靠**改真文件**来测，而那种测法**改坏了会留下残骸**',
    '（第 101 轮**真的发生过一次**：变异脚本被超时杀掉，还原那行没跑到，文件被留在变异状态）。',
    '',
    '#### ★ 五、一处**必须自己先破的假象**',
    '',
    '我第一版探针报了 **3 个假阳性**（第 104 轮已记）：把 Legion 自己的插件当成 DSH 文件、逐行扫漏掉跨行引文、',
    '按仓库根解析同目录相对引用。',
    '',
    '> ★★ 所以立这条判据之前，我先把那三个假阳性**逐个破掉**，才敢让它进 `boundary` 阶段 ——',
    '> **一条会误判的判据，比没有判据更贵**：它会逼人去改一条其实是对的引用。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK 第 105 轮小节已插入')
}

if (!/^\| 105 \|/m.test(t)) {
  const ROW = '| 105 | ★★★★★ **把第 104 轮那个洞修在机制上**（兑现上一轮写的"只修了一处实例，没修产生它的机制"）—— '
    + '新增判据 `source-original-citations-on-line`（`boundary-facts` 第 **26** 条事实）：'
    + '**源码注释**里每条 `路径:行 …原文：「…」` 的引文必须落在它写的那个行号上。'
    + '★ 与同类判据的分工：`ledger-line-citations-resolve` 读**台账**、只判"落点存在且在范围内不是空行"；'
    + '这一层读**源码**、判"**那句引文在不在那行**"。'
    + '★★ 差别在**注释里抄没抄原句**：抄了就可机械判定，只给坐标的要读语义、做不了 ⇒ '
    + '**这一层更强也更窄（绿 ≠ 所有引用都对）**，边界已写进 `why`。'
    + '★★★ 立完立刻量到 **total=3 / ok=1**：两处坏引用都指向 `launcher.mjs:108`，而那句引文'
    + '在 **HEAD 是 `:113`**、在工作区是 **`:118`** ⇒ **`:108` 两处都不是它**（那是 `PORT_ENV_KEYS` 那段注释）'
    + '⇒ **这条引用在 HEAD 上就已经错了（差 5 行），不是并行会话这次编辑造成的**。'
    + '★ 那两处**本轮不动但写进读数**：引用它们的两个 launcher 文件**都是 ` M `**（会撞车），'
    + '于是登记进 `FACTS` 的 `expect`（与 `TALLY_LIVE`/`TALLY_FROZEN` 同一手法）——'
    + '> ★★ 一条"把已知坏引用写进读数"的判据，与一条"因为它坏所以一直红"的判据，区别在于**下一处新漂移**能不能被看见：'
    + '> **后者会被人习惯性忽略（一直是红的），而前者一有新东西就变。**'
    + '★ 并在单元测试 ㉓ 的 ② 里再钉一遍（那两处必须在、总数必须恰好 2）⇒ "顺手清空 expect 让它绿"与"新漂移"**都会红**。'
    + '★★★★★ 验证三步：① 立判据后 CLI ✔ 绿；② **变异**（把 `port.mjs` 指针改回 `2241`）⇒ **✔ → ✖**、判据咬住、哨兵已清；'
    + '③ 单元测试 **62 → 63 通过**（其中把判定抽成纯函数 `originalQuoteOnLine` 专为可测 —— '
    + '否则只能靠改真文件来测，而第 101 轮**真的**因超时被杀留下了变异残骸）。'
    + '★ 立据之前先破掉第一版探针的 **3 个假阳性**（把 Legion 自己的插件当 DSH 文件、逐行扫漏跨行引文、按仓库根解析同目录相对引用）——'
    + '**一条会误判的判据比没有判据更贵**：它会逼人去改一条其实是对的引用。 |'
  const lines = t.split('\n')
  const i104 = lines.findIndex((l) => /^\| 104 \|/.test(l))
  if (i104 < 0) bad.push('  x 找不到 | 104 |')
  else { lines.splice(i104 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 105 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
