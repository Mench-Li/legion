// scratch/_r106-rows.mjs —— 第 106 轮：交付物**开头那一段**同时烂了三处，而它是这份报告的门面
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 105 轮结束时的读数**（全部可复跑）：', '**第 106 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 105 轮**', '本文件已**复核到第 106 轮**')

const ANCHOR = '#### ★★★★★ 第 105 轮：把第 104 轮那个洞**修在机制上**'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 105 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 106 轮：交付物**开头那一段**同时烂了三处 —— 而它是这份报告的**门面**',
    '',
    '★ 产品侧此刻**全部被并行会话占着**（`config-schema.mjs` · `process-manifest.mjs` · `launcher.mjs` · ',
    '`legacy-data-adoption.mjs` · 它的 `.test.mjs` —— 五个都是 ` M `），所以本轮转去核**交付物自己**。',
    '',
    '★★ 一读就发现：**报告开头那段（L22～L46）同时烂了三处**，而三处的性质各不相同。',
    '',
    '| # | 烂在哪 | 原文写的 | 真值 |',
    '|---|---|---|---|',
    '| ① | **最近一轮 CI 的目录名** | `.ci/r84-full` | 最新**全量**存档是 **`r100-full`**（09-21 04:24，**8/9 PASS**） |',
    '| ② | **那 21 条红的"簇"数** | 「收窄后是**真的 1 簇**」 | ★★★ **第 91～93 轮已撤回**：逐条读后是**三簇** |',
    '| ③ | **那 21 条红的条数** | 「**21** 条端到端用例红」 | ★★★ **第 103 轮实测 22 条 / 6 个套件**（漏了 `model-config`） |',
    '',
    '#### ★★★★★ 而第 ① 处是**第三次**烂 —— 这才是值钱的那一点',
    '',
    '```',
    '第 80 轮修过一次：  .ci/r45c → .ci/r51',
    '第 97 轮修过一次：  .ci/r51  → .ci/r84-full',
    '第 106 轮（本轮）： .ci/r84-full → 又是错的',
    '```',
    '',
    '★★ 而第 97 轮那次修的时候，我**已经把取法写进去了** —— 原话是',
    '「`.ci` 下**按时间最新**的那个存档」。',
    '',
    '> ★★★ 一句"描述怎么找"的话，与"那个名字本身"是两件事：',
    '> **前者不会烂，后者每跑一次 CI 就烂一次** —— 而它们**写在同一行上**，',
    '> 于是读的人以为那句描述保证了那个名字。',
    '',
    '★★★★★ ⇒ 所以本轮**不是再改一次那个名字**（那会烂第四次），而是**把名字删掉**：',
    '',
    '```',
    '改后：最近一轮**全量**（九阶段）CI：**8/9 PASS**（2026-09-21 04:24）',
    '      ⚠️ 唯一 FAIL 的是 `test`，而它 FAIL 的理由**不是产品红**，是**套件登记闸门**',
    '      ★ 存档在哪：本机 `.ci/` 下、按修改时间取最新的那个**全量**存档（`.ci/` 被 gitignore、不进仓）',
    '```',
    '',
    '⇒ 这一行现在**结构上不可能再过期**：它写的是**结果**与**取法**，两者都不随新的一次 CI 变化。',
    '',
    '#### ★★★★★ 而第 ②③ 两处是**同一个病**：一个结论在别处被推翻了，**而它没有回来**',
    '',
    '第 91～93 轮的结论（三簇、各有处置）与第 103 轮的读数（22 条 / 6 套件）',
    '**都写在人工介入清单里**，而交付物开头那三行**至今还是第 77 轮的说法**。',
    '',
    '> ★★★ 这正是这一整批里反复出现的形状：**"改了那一处"不等于"改掉了那个说法"**',
    '> —— 而**交付物的门面**是最不能留着旧说法的地方，因为多数读者**只读开头**。',
    '',
    '★ 处置：那三行**留在原处**（它们是**留档**，记录第 63～77 轮当时怎么读的），',
    '但在它们**上面**加一段显式声明「**该结论已被第 91～93 轮撤回、条数已被第 103 轮更正**」，并指向现行读法。',
    '',
    '★★ 为什么不直接改写那三行：**留档的价值就在于它没有被改写** ——',
    '如果每轮都把旧结论抹掉，就再也答不出"我当时是怎么读的、后来为什么改"。',
    '',
    '#### ★★ 顺带核到一条**被独立证实**的旧结论',
    '',
    '报告 L44 写着「四项必需能力里**三项是写死的 `false`**」。',
    '★ 而第 104 轮我**逐个读过** `runtimeCapabilityEvidence()` 的实现：',
    '`structured-result` 是**算出来的**，另外三项（`tool-permission-enforcement` · `cancel-and-timeout` · `usage-reporting`）',
    '**确实是写死的 `false`**，且各自还带 `code` / `source` / `reason`。',
    '',
    '⇒ **第 77 轮那个说法是对的**，本轮这次独立核对**没有推翻它**。',
    '★★★ 而这也顺带说明：那"三项写死"**不是**一个笼统的设计常数，',
    '**三条各有各的理由，且三条都指向仓里真实存在的东西**（补丁层判定 / 引擎契约的现场故障 / 引擎结果契约的两侧形状）——',
    '这一点第 104 轮已逐条核过，写在这里免得下一个人又把它们当成"同一个原因"。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK 第 106 轮小节已插入')
}

if (!/^\| 106 \|/m.test(t)) {
  const ROW = '| 106 | ★★★★★ **交付物开头那一段同时烂了三处 —— 而它是这份报告的"门面"**（产品侧此刻全部被并行会话占着，转去核交付物自己）。'
    + '三处性质各不相同：① **最近一轮 CI 的目录名**写 `.ci/r84-full`，而最新全量存档是 **`r100-full`**（09-21 04:24，**8/9 PASS**）；'
    + '② 那 21 条红写「收窄后是**真的 1 簇**」，而**第 91～93 轮已撤回**（逐条读后是**三簇**）；'
    + '③ 条数写「**21** 条」，而**第 103 轮实测 22 条 / 6 个套件**（漏了 `model-config`）。'
    + '★★★★★ 而第 ① 处是**第三次**烂：第 80 轮修过（`.ci/r45c → .ci/r51`）、第 97 轮修过（`→ .ci/r84-full`）——'
    + '★★ 而第 97 轮那次**已经把取法写进去了**（「`.ci` 下按时间最新的那个存档」）。'
    + '> ★★★ 一句"描述怎么找"的话，与"那个名字本身"是两件事：**前者不会烂，后者每跑一次 CI 就烂一次** ——'
    + '> 而它们**写在同一行上**，于是读的人以为那句描述保证了那个名字。'
    + '⇒ 本轮**不是再改一次那个名字**（那会烂第四次），而是**把名字删掉**：改成只写**结果 8/9 PASS** + **取法** ⇒ '
    + '这一行**结构上不可能再过期**。'
    + '★★★★★ 而第 ②③ 是**同一个病**：结论在别处被推翻了、**而它没有回来**——'
    + '第 91～93 轮（三簇）与第 103 轮（22 条/6 套件）**都写在人工介入清单里**，而交付物开头那三行**至今还是第 77 轮的说法**。'
    + '> **"改了那一处"不等于"改掉了那个说法"** —— 而**门面**是最不能留旧说法的地方，因为多数读者**只读开头**。'
    + '★ 处置：那三行**留在原处**（留档的价值就在于它没有被改写），但在**上面**加显式声明「已被第 91～93 轮撤回、条数已被第 103 轮更正」并指向现行读法。'
    + '★★ 顺带**独立证实**了一条旧结论：报告 L44 写「四项必需能力里**三项是写死的 `false`**」——'
    + '第 104 轮逐个读过 `runtimeCapabilityEvidence()`：`structured-result` 是算出来的，另三项（`tool-permission-enforcement` · `cancel-and-timeout` · `usage-reporting`）**确实写死 `false`**，'
    + '且各带 `code`/`source`/`reason` ⇒ **第 77 轮那个说法是对的**；'
    + '★★★ 且那"三项写死"**三条各有各的理由、各指向仓里真实存在的东西**，不是笼统的"同一个设计常数"。 |'
  const lines = t.split('\n')
  const i105 = lines.findIndex((l) => /^\| 105 \|/.test(l))
  if (i105 < 0) bad.push('  x 找不到 | 105 |')
  else { lines.splice(i105 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 106 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
