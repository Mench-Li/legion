// scratch/_r107-rows.mjs —— 第 107 轮：交付物 §一 与 §四 里那四个**扫描规模**，四个里三个已过期
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 106 轮结束时的读数**（全部可复跑）：', '**第 107 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 106 轮**', '本文件已**复核到第 107 轮**')

const ANCHOR = '#### ★★★★★ 第 106 轮：交付物**开头那一段**同时烂了三处'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 106 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 107 轮：四条边界"扫了多少文件"，**四个里三个已经过期**',
    '',
    '★ 接着上一轮的办法核**交付物自己的数**（产品侧仍全部被并行会话占着）。',
    '这一轮核的是 §1.1 那张投影表与 §四那张边界表里的一共**四个扫描规模**：',
    '',
    '| | 报告写的 | 第 107 轮实测 | 差 |',
    '|---|---|---|---|',
    '| ① 不自研第二套 Agent Loop | 296 | **381** | +85 |',
    '| ② 不复制任务/审批/审计数据库 | 38 | **88** | +50 |',
    '| ④ 不多 Harness | 15 | **15** | —— **唯一没过的** |',
    '| ⑤ 不单独装 DSH | 162 | **164** | +2 |',
    '',
    '#### ★★ 而"过期"在这里**根本不是缺陷**',
    '',
    '那个扫描是**全仓按 glob 数文件**的，而**另一个会话在持续落新文件**',
    '（实测那段时间 `team-hub/routes/*.mjs` 一项就有 **49** 个）。',
    '文件变多 ⇒ 扫描量变多 ⇒ **这份文档里的数必然越来越旧**。',
    '',
    '#### ★★★★★ 同一个形状，本仓**已经栽过、也已经定过处置** —— 本轮只是沿用',
    '',
    '`boundary-facts.mjs` 的 `handover-tracked-suites` 那条判据里，白纸黑字记着：',
    '',
    '```',
    '实测两轮内它红了 7 次、真缺陷 0 次（374→375→376→378→379→380→381，',
    '最后一次只隔几分钟 —— 红得比提交还快）。',
    '⇒ 第 54 轮把它从"等于当前值"改成**下限**（relation: atLeast）',
    '```',
    '',
    '> ★★★ **"红得比提交还快"** —— 这六个字就是"等于"式判据在这种数上的判决书：',
    '> 它红得**有道理**，所以每一次都要有人去看；而它红的**原因永远是同一件事**，',
    '> 所以**看了几次之后就没有人再看了**。',
    '',
    '★★★★★ ⇒ 本轮**不另发明一套处置**，直接沿用：',
    '',
    '```',
    '改前：| ① 不自研第二套 Agent Loop | 机械 | 296 个文件 |',
    '改后：| ① 不自研第二套 Agent Loop | 机械 | ★ ≥ 381 个文件 |',
    '      （四条都改，另加一段说明为什么是"下限"）',
    '```',
    '',
    '★ 这个数的**真实作用**是"证明扫描面不是空的"：`design-boundaries.mjs` 里本来就有一条空转守卫',
    '（`scanned > 0` 否则报 `check-scanned-nothing`）—— 下限守的是**同一个方向**，',
    '只是把"不是 0"收紧到"不少于第 107 轮实测的那个规模"。',
    '⇒ **"文件被删到下限以下"才是会真正破坏那句说法的方向。**',
    '',
    '#### ★ 顺带：§1.1 那张表里重复的那四个数**去掉了**，只留 §四 一处',
    '',
    '§1.1 原来也抄了一遍 `296/38/15/162`，与 §四 **同一个数写两处**。',
    '★★ 这是第 96～97 轮反复踩过的形状：**同一个数散在两处 ⇒ 改一处漏一处**。',
    '⇒ 本轮让 §1.1 只写"4 机械 / 1 设计"，把四个数**指到 §四**（不重复抄）。',
    '',
    '#### ★★★★★ 立了四条判据（第 27～30 条事实）',
    '',
    '★ 为什么是**四条**而不是一条：`relation: \'atLeast\'` 的比较是**标量对标注**',
    '（`actual < claimed`）—— 四个数打包成一个字符串就**没法比大小**了。',
    '',
    '★ 验证（两步，都在另起进程里做）：',
    '',
    '| 步 | 动作 | 读数 |',
    '|---|---|---|',
    '| ① | 四个下限**各 +1000**（把文档改大） | 绿 **4/4 → 0/4**、红 **0 → 4** ⇒ 四条全咬住 |',
    '| ② | **反向对照**：只把 ① 改大 | 红 **恰好 1 条，且正是 ①** ⇒ 四条**各判各的**，不是一红俱红 |',
    '',
    '★★ 第 ② 步是关键：如果四条事实其实读的是同一个量（比如都读了 `scanned` 的第一个键），',
    '那"改一条红四条"与"改四条红四条"**在只数红的总数的输出里长得一样**。',
    '',
    '★★ 单元测试 ㉔ 另加两个**方向**断言（不依赖 `checkFacts` 的实现）：',
    '下限改成"实际值 +1"就**必须**会红，改成"实际值 −1"**必须不**红 ——',
    '**这两条一起才定义了"它是下限"**；少任何一条，把 `atLeast` 换回 `equal` 都不会被发现。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK 第 107 轮小节已插入')
}

if (!/^\| 107 \|/m.test(t)) {
  const ROW = '| 107 | ★★★★★ **四条边界"扫了多少文件"，四个里三个已经过期**（接着上一轮核交付物自己的数）。'
    + '实测：① 不自研第二套 Agent Loop **296→381**、② 不复制账本 **38→88**、④ 不多 Harness **15→15（唯一没过期的）**、⑤ 不单独装 DSH **162→164**。'
    + '★★ 而"过期"在这里**根本不是缺陷**：扫描是**全仓按 glob 数文件**、另一个会话在持续落新文件'
    + '（那段时间 `team-hub/routes/*.mjs` 一项就有 **49** 个）⇒ 文件变多则这个数必然越来越旧。'
    + '★★★★★ 而同一个形状**本仓已经栽过也定过处置**：`handover-tracked-suites` 里白纸黑字写着'
    + '「实测两轮内它红了 **7** 次、真缺陷 **0** 次（374→375→376→378→379→380→381，**红得比提交还快**）⇒ 第 54 轮把它从"等于当前值"改成**下限**」。'
    + '> ★★★ **"红得比提交还快"** 就是"等于"式判据在这种数上的判决书：它红得**有道理**（每次都要有人看），'
    + '> 而红的**原因永远是同一件事**（看几次之后就没有人再看了）。'
    + '⇒ 本轮**不另发明一套处置**，直接沿用：改成 `★ ≥ 381` 等四条下限，并写明真实作用是"证明扫描面不是空的"'
    + '（`design-boundaries.mjs` 本就有空转守卫 `scanned > 0` ⇒ 同一个方向）——**"文件被删到下限以下"才是会真正破坏该说法的方向**。'
    + '★ 顺带把 §1.1 里重复抄的那四个数**去掉**、只指到 §四（同一数写两处 ⇒ 改一处漏一处，第 96～97 轮踩过）。'
    + '★★★★★ 立了**四条**判据（第 27～30 条事实）：为什么不是一条 —— `atLeast` 的比较是**标量对标注**，四个数打包成字符串就**没法比大小**。'
    + '★ 验证：① 四个下限各 +1000 ⇒ 绿 **4/4 → 0/4**、红 **0 → 4**；② **反向对照**只改 ① ⇒ 红**恰好 1 条且正是 ①**（⇒ 四条**各判各的**，不是一红俱红）。'
    + '★★ 第 ② 步是关键：若四条其实读同一个量，"改一条红四条"与"改四条红四条"**在只数红的总数的输出里长得一样**。'
    + '★★ 测试 ㉔ 另加两个方向断言（不依赖 `checkFacts` 实现）：下限改成"实际+1"**必须**红、改成"实际−1"**必须不**红 ——'
    + '**两条一起才定义了"它是下限"**；少任何一条，把 `atLeast` 换回 `equal` 都不会被发现。 |'
  const lines = t.split('\n')
  const i106 = lines.findIndex((l) => /^\| 106 \|/.test(l))
  if (i106 < 0) bad.push('  x 找不到 | 106 |')
  else { lines.splice(i106 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 107 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
