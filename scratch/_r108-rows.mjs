// scratch/_r108-rows.mjs —— 第 108 轮：交付物的**决策面漏掉了它最重的那一条**
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 107 轮结束时的读数**（全部可复跑）：', '**第 108 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 107 轮**', '本文件已**复核到第 108 轮**')

const ANCHOR = '#### ★★★★★ 第 107 轮：四条边界"扫了多少文件"'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 107 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 108 轮：交付物的**决策面**漏掉了它**最重的那一条**',
    '',
    '★ 接着核交付物。这一轮核的是 **§2.0「一页纸：要您做的决定」** 与 **§2.1「需要您决策的」** 那两张表。',
    '',
    '#### 一、量到的事实',
    '',
    '§2.0 那张表的"先看"列一共这些行：',
    '',
    '```',
    '0(✅已落地)  1(✅已落地)  2=K  3=H  4=I  5=J  6=L  7(✅已落地)  8=D  9=C  E/F/G  10=§5第29条  PRT-316',
    '```',
    '',
    '§2.1 那张表的格子：**A · B · C · D · E · F · G · H · I · J · K · §5 第 29 条 · L**。',
    '',
    '★★★★★ **两张表上都没有**「启动自检恒判不兼容 ⇒ `autoExecutionForbidden` 恒为 true」这一条。',
    '它只出现在**别人的正文里**：',
    '',
    '| 在哪 | 以什么身份出现 |',
    '|---|---|',
    '| §2.0 第 **0** 行的正文 | 作为那个"已落地"决定的**代价**被补记 |',
    '| §2.0 第 **E/F/G** 行的正文 | 作为"**另加**一条更靠前的"被塞进去 |',
    '| 报告开头 L37 / L44 | 作为**历史留档** |',
    '',
    '> ★★★ 而 §2.0 自己的表头写着：「它存在的理由只有一个：**您需要先知道先看哪一格**」。',
    '> ⇒ **一条只存在于别的行的正文里的决定，是没法"先看"的** ——',
    '> 而它是这份报告里**后果最重**的一格：**产品永远不能自动执行**。',
    '',
    '#### ★★★★★ 二、为什么它最重',
    '',
    '`runtime/dsh-composition/plugins/runtime-host-registrar-row.mjs` 的 `runtimeCapabilityEvidence()`（**L579**）',
    '给出四项必需能力，其中 **3 项的 `satisfied` 是写死的 `false`**：',
    '',
    '| 行 | 能力 | 判据码 |',
    '|---|---|---|',
    '| **L584** | `tool-permission-enforcement` | `ENFORCEMENT_PLANE_MEASURED_ELSEWHERE` |',
    '| **L592** | `cancel-and-timeout` | `CANCEL_NOT_GUARANTEED_BY_ENGINE` |',
    '| **L600** | `usage-reporting` | `RESULT_CONTRACT_HAS_NO_USAGE` |',
    '',
    '第 4 项 `structured-result` 是**真的算出来的**（`structuredResultEvidence()`）。',
    '★ 而 `probe.mjs` 只认**显式 true** ⇒ **`autoExecutionForbidden` 恒为 true**。',
    '',
    '★★★ 三条 `reason` **各不相同、各指向仓里真实存在的东西**（第 104 轮逐条读过，本轮再核一遍）：',
    '① 强制面**在别处**被判定（探针"**不把同一件事再判一遍**，两份判定会漂移"）；',
    '② `runtime/adapters/dsh/port.mjs` 文件头记的**已发生的生产故障**「abort 不保证杀死子代理」；',
    '③ DSH 的 `SubagentResult` 契约里**没有任何用量字段**，而 `usage.mjs` 的 `collectUsage()` 读 `result.usage`。',
    '',
    '★★★★ 它的**代价**是 **22 条端到端用例红 / 6 个套件** —— 而 §2.0 的第 0 行与 §三之三 L972 那处',
    '**都还写着第 63～77 轮的"21 条"**。★ 本轮一并更正为 22 条 / 6 个套件，并写明漏掉的 `model-config` 那 1 条',
    '**是因为第 102 轮之前的 `test` 一个用例都没跑**。',
    '',
    '#### ★★★★★ 三、这一格真正要您决定的，是什么',
    '',
    '★ **不是**"去把那三个 `false` 改成 `true`" —— 那三条各有各的理由，改掉就是把它们当初拒绝的东西种回去。',
    '要决定的是**下面两条路走哪条**：',
    '',
    '| | 做法 | 代价 |',
    '|---|---|---|',
    '| **甲** | 承认「本设计下自动执行**永不开**」，把它写成**设计事实** | §9 链的 L2/L5 **不是"红的"而是"按设计关着"** ⇒ 红名单据此**重定基线**；★ 而**产品仍然不能自动执行**，只是这件事从"没人知道"变成"白纸黑字" |',
    '| **乙** | 给自检一个**第二来源**（跨进程 / 跨平面的证据通道），让"在别处被判定"能**被本进程验证** | 门禁变绿；★ 而**要定义"在别处"怎么被核** —— 否则就是把我第 91～93 轮警告过的"**两份判定会漂移**"重新种回去，而那正是这三项当初选择"不报 true"的**理由** |',
    '',
    '★★ 我**没有**替您选，也从第 98 轮起**故意不动手** ——',
    '乙的已知代价恰恰是「**门禁会变绿，而产品仍然不能自动执行**」，而那正是这份工作**通篇在批评的那种绿**。',
    '',
    '#### ★★★ 四、这个漏是怎么产生的（值得记下来）',
    '',
    '那两张表是**第 16～33 轮**那批写的；而这一条是**第 63～79 轮**才查出来的。',
    '此后每一轮的发现都进了 §三之三的**留档** —— 而**没有任何一步是"把它提升进决策表"**。',
    '',
    '> ★★★ **「写进了日志」与「写进了决策表」不是同一件事**，',
    '> 而**只有后者能让您"先看哪一格"**。',
    '',
    '★★ 这与第 106 轮那个发现是**同一个形状**（"改了那一处"≠"改掉了那个说法"），',
    '只是换了一层：**"记下了这件事" ≠ "把它放到了读者会先看的地方"。**',
    '',
    '#### ★ 五、一处**我自己造的、又被自己抓住的**破坏',
    '',
    '★ 插入 §2.1 那一格时，我的 `edit` 把 **L 那一行的行首**（`| **L** | ★★★ 那条出站车道的文件该放在哪个目录…`）**吃掉了**，',
    '于是 L 的内容被**接在 M 那一行后面**，两张表之间多出一行**只有 5 列内容的残段**。',
    '',
    '★★ 我是**读回去核列数**时发现的：把 §2.1 每行的管道符数出来，发现那一行**只有 6 个管道却混着两行的内容**。',
    '⇒ 已补回 L 的行首，并**逐行复核：A～L 与 M 每一行都是 6 个管道（5 列）**。',
    '',
    '> ★★★ 而这件事的教训不是"小心一点"：**是我先做了一次"改一处"，而没有先做一次"读回去数一遍"。**',
    '> 表格的破坏**不会让任何判据变红**（`doc-table-integrity` 管的是**列数相等**，而那一行的管道数**恰好还是 6**）——',
    '> 它就是**安静地把两行并成一行**。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK 第 108 轮小节已插入')
}

if (!/^\| 108 \|/m.test(t)) {
  const ROW = '| 108 | ★★★★★ **交付物的决策面漏掉了它最重的那一条**（核 §2.0「一页纸：要您做的决定」与 §2.1「需要您决策的」两张表）。'
    + '§2.0 的"先看"列是 `0,1,2=K,3=H,4=I,5=J,6=L,7,8=D,9=C,E/F/G,10=§5第29条,PRT-316`；§2.1 的格子是 **A～L + §5 第 29 条** ——'
    + '**两张表上都没有**「启动自检恒判不兼容 ⇒ `autoExecutionForbidden` 恒为 true」。它只出现在**别人的正文里**：'
    + '§2.0 第 0 行的正文（作为那个"已落地"决定的**代价**补记）、第 E/F/G 行的正文（作为"**另加**一条更靠前的"塞进去）、以及报告开头的历史留档。'
    + '> ★★★ 而 §2.0 自己的表头写着「它存在的理由只有一个：**您需要先知道先看哪一格**」⇒ '
    + '> **一条只存在于别的行的正文里的决定，是没法"先看"的** —— 而它是这份报告里**后果最重**的一格：**产品永远不能自动执行**。'
    + '★★★★★ 核到确切的源码：`runtime/dsh-composition/plugins/runtime-host-registrar-row.mjs` 的 `runtimeCapabilityEvidence()`（**L579**），'
    + '3 项写死的 `satisfied: false` 在 **L584**（`tool-permission-enforcement` / `ENFORCEMENT_PLANE_MEASURED_ELSEWHERE`）、'
    + '**L592**（`cancel-and-timeout` / `CANCEL_NOT_GUARANTEED_BY_ENGINE`）、**L600**（`usage-reporting` / `RESULT_CONTRACT_HAS_NO_USAGE`）；'
    + '第 4 项 `structured-result` 是**真的算出来的**。三条 `reason` **各不相同、各指向仓里真实存在的东西**（强制面在别处被判定"不把同一件事再判一遍"／引擎契约的**已发生生产故障**／引擎结果契约**没有用量字段**）。'
    + '★ 而 `probe.mjs` 只认**显式 true** ⇒ **恒为 true，与本机平台无关**。'
    + '★ 本轮把两处「**21 条**」一并更正为 **22 条 / 6 个套件**（§2.0 第 0 行、§三之三 L972），'
    + '并写明漏掉的 `model-config` 那 1 条**是因为第 102 轮之前的 `test` 一个用例都没跑**。'
    + '★★★★★ 而要决定的**不是**"把那三个 `false` 改成 `true`"（那三条各有理由，改掉就是把它们当初拒绝的东西种回去），是两条路：'
    + '**甲**＝承认「本设计下自动执行**永不开**」并写成**设计事实**（§9 链的 L2/L5 从此**不是"红的"而是"按设计关着"**、红名单重定基线；代价是**产品仍不能自动执行**）；'
    + '**乙**＝给自检一个**第二来源**让"在别处被判定"能被本进程验证（门禁变绿；代价是**要定义"在别处"怎么被核**，否则就是把我第 91～93 轮警告过的"**两份判定会漂移**"重新种回去）。'
    + '★ 我**没有**替您选，也从第 98 轮起**故意不动手**——乙的已知代价恰恰是「**门禁会变绿，而产品仍然不能自动执行**」，而那正是这份工作**通篇在批评的那种绿**。'
    + '★ 漏的成因：那两张表是**第 16～33 轮**写的，而这一条是**第 63～79 轮**才查出来的，此后每轮都进了 §三之三的**留档**、'
    + '**而没有任何一步是"把它提升进决策表"** ⇒ **「写进了日志」与「写进了决策表」不是同一件事**，只有后者能让您"先看哪一格"。'
    + '★★ 这一轮我还**自己造了一处破坏又自己抓住**：插入 §2.1 那格时 `edit` 把 **L 那一行的行首**吃掉了，L 的内容被接到 M 后面；'
    + '★ 我是**读回去数列数**时发现的（那一行 6 个管道却混着两行内容）⇒ 已补回 L 的行首，并逐行复核 A～L 与 M **每行都是 6 个管道（5 列）**。'
    + '> ★★★ 教训不是"小心一点"：**是我先做了一次"改一处"，而没有先做一次"读回去数一遍"** ——'
    + '> 表格的破坏**不会让任何判据变红**（`doc-table-integrity` 管**列数相等**，而那一行管道数**恰好还是 6**），它就是**安静地把两行并成一行**。 |'
  const lines = t.split('\n')
  const i107 = lines.findIndex((l) => /^\| 107 \|/.test(l))
  if (i107 < 0) bad.push('  x 找不到 | 107 |')
  else { lines.splice(i107 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 108 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
