// scratch/_r109-rows.mjs —— 第 109/110 轮：一条**超时**的判据与一条**没跑**的判据，产物长得一模一样
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 108 轮结束时的读数**（全部可复跑）：', '**第 110 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 108 轮**', '本文件已**复核到第 110 轮**')

const ANCHOR = '#### ★★★★★ 第 108 轮：交付物的**决策面**漏掉了它**最重的那一条**'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 108 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 109/110 轮：一条**超时**的判据，与一条**没跑**的判据，在产物里长得一模一样',
    '',
    '★ 第 108 轮我起了一个后台作业跑**全量**九阶段 CI（`test` 现在要 18.5 分钟，超出工具单次上限）。',
    '这一轮取读数：**8/9 PASS，唯一 FAIL 的是 `test`**。',
    '',
    '#### 一、而我第一次数那 7 份套件日志时，少数掉了一个',
    '',
    '★ 我的枚举脚本判的是 `ℹ fail (\\d+)`。⇒ **凡是日志里没有这一行的，我一条都没看见。**',
    '',
    '| 套件 | 读数 |',
    '|---|---|',
    '| `runtime-contract-cross-process` | 10 通过 / **9 失败** |',
    '| `runtime-host-row-dsh-process` | 5 / **5** |',
    '| `runtime-host-binding-unblocked` | 3 / **4** |',
    '| `runtime-host-registrar-dsh-process` | 4 / **2** |',
    '| `model-api` | 14 / **1** |',
    '| `model-config` | 17 / **1** |',
    '| ★ **`boundary-facts`** | **没有读数 —— 它【超时被杀】** |',
    '',
    '★★★★★ **第 7 个套件我就是这样漏掉的**：它那份日志里**只有 `[超时现场]`**，',
    '**既没有 `ℹ pass` 也没有 `ℹ fail`** ⇒ 我的正则**根本匹配不到它**。',
    '',
    '> ★★★★★ **一条超时的判据，与一条"没跑"的判据，在产物里长得一模一样** ——',
    '> 而这条套件存在的**全部意义**就是"**别让一个数字静默失去检查对象**"。',
    '> ★ 而杀死它的那次超时，**是我自己造成的**。',
    '',
    '#### ★★★★★ 二、那个超时确实是我造成的，而且能算出来',
    '',
    '第 107 轮我给 `boundary-facts` 加了**四条**事实（四条机械边界各"扫了多少文件"），',
    '**每条各调一次 `checkRepo()`**。实测：',
    '',
    '```',
    'checkRepo()     单次 = 372ms',
    '四条事实        每次 checkFacts() = +1488ms（+19%）',
    '该套件用例③（"逐条做反面控制"）会调 checkFacts() 每条事实一次 ≈ 30 次',
    '⇒ 我那次改动给它加了约 45 秒',
    '```',
    '',
    '★ 而 `run-ci.mjs:130` 写着 `TEST_SUITE_TIMEOUT_MS = 300000`（**5 分钟**），该套件实测 **347 秒** ⇒ **被杀**。',
    '',
    '#### ★★★★★ 三、修法：给 `checkRepo()` 加**按进程缓存**',
    '',
    '```',
    '修前  347s（超时被杀）',
    '修后  281s   ⇒ 省 66 秒，且 64 → 65 通过 / 2 失败（测试条数没少、红数没多）',
    '```',
    '',
    '★ 并加了单元测试 ㉕ 钉住缓存的**两个方向**：',
    '① 两次 `checkRepo()` 必须是**同一个对象**（否则那 45 秒还在）；',
    '② `clearCheckRepoMemo()` 之后必须是**新对象**（反向控制）——',
    '> ★★ 少了第 ② 条，"**缓存没清**"与"**判据咬不住**"在输出里长得一样，而处置相反。',
    '',
    '★★ 缓存的**边界**我写进了代码注释，免得下一班人把它当成"`checkRepo()` 是纯函数"：',
    '**它其实会读目标文档** ⇒ 一趟进程里改了那份文档再调它，会拿到改动前的结果。',
    '★ 而**本套件不受影响**：它 mutate 的是交付物 / 人工清单那两份，**不是**目标文档；',
    '而这四条事实派生的是**扫描到的文件数**，与任何文档无关。',
    '',
    '#### ★★★★★ 四、而"修好了"这句话只能说到这里 —— 残余风险仍在',
    '',
    '**281 秒离 300 秒只有 19 秒。** ⇒ 缓存只还回了**我加的那 45 秒**，',
    '**没有解决"这个套件本来就贴着上限"这件事**。',
    '',
    '> ★★★ 而一个贴着上限的套件，坏起来**不是变红、是变成没有判决** ——',
    '> 而恰恰是这门判据**最不该**处于的状态。',
    '⇒ 这条**记在这里、不假装解决**：要真的关上它，需要把套件的**成本**降下来',
    '（用例③"逐条做反面控制"是 `O(事实数 × 全仓扫描)`），而不是再靠一次缓存。',
    '',
    '#### ★★★★★ 五、而这一轮还**推翻了我第 106 轮自己写下的那句话**',
    '',
    '第 106 轮我在交付物开头写：`test` FAIL 的理由「**不是产品红**，是**套件登记闸门**」。',
    '★★ 那句对 `r100-full` 成立（那次闸门提前 `return`，套件一个都没跑）；',
    '**对今天这一轮不成立** —— 今天的 `test` FAIL 有**两个互不相干的理由**：',
    '',
    '| | 理由 |',
    '|---|---|',
    '| **(甲)** | 套件登记闸门：`33 个 *.test.mjs 不会被任何套件执行` |',
    '| **(乙)** | **7 个红套件**：6 个真红（22 条断言）+ **1 个超时** |',
    '',
    '⇒ 已把那一段改成**两个理由并列**，并把七份套件日志的读数**逐份列出来**。',
    '',
    '> ★★★ **"我记得的理由"与"这一轮的理由"是两件事** ——',
    '> 而我只用了一轮就把它们混成了一个（"不是产品红"这句话，今天读起来像是在替产品开脱）。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK 第 109/110 轮小节已插入')
}

if (!/^\| 109 \|/m.test(t)) {
  const ROW = '| 109 | ★★★★★ **一条【超时】的判据，与一条"没跑"的判据，在产物里长得一模一样**（取第 108 轮那个后台全量 CI 的读数：**8/9 PASS**）。'
    + '★ 我第一遍数那 7 份套件日志时**少数掉了一个** —— 我的枚举判的是 `ℹ fail (\\d+)`，而 `boundary-facts` 那份日志里**只有 `[超时现场]`**、'
    + '**既没有 `ℹ pass` 也没有 `ℹ fail`** ⇒ 正则**根本匹配不到它**。'
    + '> ★★★★★ 而这条套件存在的**全部意义**就是"**别让一个数字静默失去检查对象**"。'
    + '★★★★★ **二、那个超时确实是我造成的、而且能算出来**：第 107 轮我给 `boundary-facts` 加了**四条**事实（四条机械边界各"扫了多少文件"），'
    + '**每条各调一次 `checkRepo()`** —— 实测 `checkRepo()` 单次 **372ms**、四条 **+1488ms/次（+19%）**，'
    + '而该套件用例③（"逐条做反面控制"）会调 `checkFacts()` **每条事实一次（~30 次）** ⇒ **约 +45 秒**。'
    + '★ 而 `run-ci.mjs:130` 写着 `TEST_SUITE_TIMEOUT_MS = 300000`（5 分钟），该套件实测 **347 秒** ⇒ **被杀**。'
    + '★★★★★ **三、修法**：给 `checkRepo()` 加**按进程缓存** ⇒ **347s → 281s**（省 66 秒），且 **64 → 65 通过 / 2 失败**（条数没少、红数没多）。'
    + '★ 加测试 ㉕ 钉住缓存**两个方向**：① 两次 `checkRepo()` 必须是**同一对象**（否则那 45 秒还在）；② `clearCheckRepoMemo()` 之后必须是**新对象**。'
    + '> ★★ 少了第 ② 条，"**缓存没清**"与"**判据咬不住**"在输出里长得一样，而处置相反。'
    + '★★ 缓存的**边界**写进了代码注释（它**会读目标文档** ⇒ 一趟进程里改了那份文档再调它会拿到旧结果；★ 而本套件 mutate 的是交付物/人工清单，不受影响）。'
    + '★★★★★ **四、"修好了"只能说到这里**：**281 秒离 300 秒只有 19 秒** ⇒ 缓存只还回了**我加的那 45 秒**，'
    + '**没有解决"这个套件本来就贴着上限"** ⇒ 一个贴着上限的套件坏起来**不是变红、是变成没有判决**，而这恰是这门判据**最不该**处于的状态。'
    + '★ 记在这里、**不假装解决**：要真关上它得把套件**成本**降下来（用例③是 `O(事实数 × 全仓扫描)`），而不是再靠一次缓存。 |'
  const lines = t.split('\n')
  const i108 = lines.findIndex((l) => /^\| 108 \|/.test(l))
  if (i108 < 0) bad.push('  x 找不到 | 108 |')
  else { lines.splice(i108 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 109 行') }
}

if (!/^\| 110 \|/m.test(t)) {
  const ROW = '| 110 | ★★★★★ **推翻了我第 106 轮自己写下的那句话**：第 106 轮我在交付物开头写 `test` FAIL 的理由「**不是产品红**，是**套件登记闸门**」。'
    + '★★ 那句对 `r100-full` 成立（那次闸门提前 `return`、套件一个都没跑），**对今天这一轮不成立** ——'
    + '今天 `test` FAIL 有**两个互不相干的理由**：**(甲)** 套件登记闸门 `33 个 *.test.mjs 不会被任何套件执行`；'
    + '**(乙)** **7 个红套件**：6 个真红（22 条断言）+ **1 个超时**。'
    + '⇒ 已把那一段改成**两个理由并列**，并把七份套件日志的读数**逐份列进交付物**（`runtime-contract-cross-process` 10/9、`runtime-host-row-dsh-process` 5/5、'
    + '`runtime-host-binding-unblocked` 3/4、`runtime-host-registrar-dsh-process` 4/2、`model-api` 14/1、`model-config` 17/1、★ `boundary-facts` **无读数/超时**）。'
    + '> ★★★ **"我记得的理由"与"这一轮的理由"是两件事** —— 而我只用了一轮就把它们混成了一个'
    + '> （"不是产品红"这句话，今天读起来**像是在替产品开脱**）。'
    + '★ 顺带把那一行改成**结构性不会过期**的写法：只写**结果 8/9 PASS** 与**取法**，不写目录名。 |'
  const lines = t.split('\n')
  const i109 = lines.findIndex((l) => /^\| 109 \|/.test(l))
  if (i109 < 0) bad.push('  x 找不到 | 109 |')
  else { lines.splice(i109 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 110 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
