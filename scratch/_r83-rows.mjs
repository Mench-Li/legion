// scratch/_r83-rows.mjs —— 第 83 轮：补家族表第 78～82 行，并把读数标题推到第 83 轮
//
// ★ 为什么必须一起做：判据 ⑰ 要求「**标题轮次 = 家族表最大轮次**」（boundary-facts.test.mjs:1141）。
//   第 78～82 轮我改了文档却没加家族行 ⇒ 两者已经在**分岔**，只是我这五轮没跑那条判据。
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []

const ROWS = [
  '| 78 | ★★★★ **交付物 §2.3 那一行有三处错，而它在我自己的门禁里活了 14 轮** —— '
  + '`PRT-FINAL-REPORT` §2.3「需要时间的（**唯一一个纯等待项**）」那一行：'
  + '① 状态写 `⬜`，而权威台账里 PRT-316 是 **🟡**（台账里唯一的 🟡），**本报告 §2.0 自己那一行也写 🟡** —— '
  + '同一份报告里两处说了不同状态；② 写「今天 2026-09-18 ⇒ **还差 6 天**」，而 §2.0 **明令**'
  + '「最早 2026-09-24（**绝对日期**，**不写"还差几天"**）」—— §2.3 违反了 §2.0 立的规矩；'
  + '③「今天」也过时了。★ 另外 `## 三、逐轮留档…截至第 52 轮` 不说明后面还有没有（实际已到 77）。'
  + '★★★★★ **最值得记的是：这个错在我自己的门禁里活了 14 轮**（第 63～77 轮每轮都跑十三个套件 + CLI，**全绿**；改完**仍全绿**）'
  + '⇒ **判据盯的是"台账分档与引用坐标"，不是"交付物里每一处状态字写对了没有"。**'
  + '> 一个只核"读数的形状"的门禁，会让**读数本身写错**的地方长期存活 ——'
  + '> 而且它每一轮都报绿，让核的人以为"没问题的都核过了"。 |',

  '| 79 | ★★★★ **给交付物"L5 硬断"那张历史表补一条后加的结局注**（历史一字未改）—— '
  + '报告 §三的九节表里 `L5 ✖ 硬断` 是**那一轮的读数**，那时是真的 ⇒ 本仓惯例是「历史留档 + 后加的注」，'
  + '`git diff --stat` = **16 insertions(+), 0 deletions(-)**。那条注接上一个完整的因果弧：'
  + '① 业主选**甲**，另一会话以 `2f5a4b3` 落地；② **今天复跑 `alpha-chain-trace`，`L5` 已是 `✔ 有活实现`** —— 硬断确实修好了；'
  + '③ ★★★★★ **而代价写那一节时还不知道**：那两行挂上后启动自检**恒**判不兼容 ⇒ `autoExecutionForbidden` 恒 true ⇒ **21 条端到端用例转红**。'
  + '⇒ 报告"整条链不全绿"的结论**今天仍成立**，只是**原因换了一个**。'
  + '★ 本轮差点做错：我一开始想把同节「读数：L5 硬断」当**过期活结论**改掉，读了上下文才发现那是**逐轮留档** —— 改它就是**篡改历史**。'
  + '> 一个"这句子今天不对"的直觉，与一个"这段是历史留档"的事实，'
  + '> 会让同一次修改一边像在纠错、一边其实在改档案。 |',

  '| 80 | ★★★★ **交付物 §一 的两处活读数过期** —— '
  + 'L3「追加至第 **45** 轮」（实际已到 79）、L9「**最近一轮 CI：`.ci/r45c`**」（最新是 **`.ci/r51`**，2026-09-20 19:33）。'
  + '★ L9 的错法是**按名字猜最新**：`.ci` 下**混了两种命名**（人手的 `r45c`/`r51` 与自动的 `2026-09-20T…`）'
  + '⇒ 名字看起来"r45c 之后就是 r51"，而真正的最新要**按修改时间**排（本仓记过的"字母序 ≠ 最新"）。'
  + '★ 更正时**保留了原句**（写清原来写的是什么、为什么不对）—— 读的人需要知道**这份报告曾经那样说过**。'
  + '★★ 顺带把 r51 那次红的结论接了上去（真的 1 簇 = 自检恒不兼容 ⇒ 21 红；其余"被杀"的空载 1~3 秒即绿 = 假红）。'
  + '★ 这是第 78 轮那条结论的**第二次应验**（同轴、隔一轮、又一处，而门禁都全绿）。 |',

  '| 81 | ★★★ **给交付物"先看 0"那一格补上它的实测代价** —— 那一格原来只说「★ 但它**今天仍然干不了活**」'
  + '⇒ 读者会读成"还没接完，接完就好了"。本轮补上第 63～79 轮量到的：'
  + '**这不是"这次恰好不行"，是恒的** —— 四项必需能力里**三项是写死的 `satisfied: false` 字面量**，'
  + '四种现场下取值集合都是 `{false}`，而 probe 要求"显式为 true" ⇒ `autoExecutionForbidden` 恒 true（与本机平台无关）；'
  + '**代价是 21 条端到端用例红**，而那是**能自己解释自己**的红。'
  + '> 一个写着"仍然干不了活"的格子，会让读的人期待"接完就好"；'
  + '> 而它真正要传达的是"**这是一条设计边界，不是一条待办**"。'
  + '★ 实现上只在那**一格内**追加、**不动表格结构**（不引入 `|`），否则会触发 `doc-table-integrity` 的列数规则。 |',

  '| 82 | ★★★★★ **跑真 CI 抓到一处我自己在第 55 轮提交的编码损坏（env 阶段 FAIL）** —— '
  + '第 78～81 轮我一直在读交付物的结论句；本轮换回来跑**真 CI 的快阶段**（`--skip test --skip smoke`），'
  + '理由是这几轮改了很多文档，而 `doc`/`env` 是**我的十三个套件不覆盖的**。'
  + '读数：十三个套件 247/0、CLI 23/23 红 0，而 CI `syntax` PASS · **`env` FAIL** · `boundary` PASS · `deps` PASS · `build` PASS · `test` FAIL · `stage` PASS · `doc` PASS。'
  + '★ env 抓到的是**我自己造的**：`scratch/_mutate-r55-s5index.mjs` **含 33 个 U+FFFD、横跨 12 行**，'
  + '而且 `import` 被**并进了注释里** ⇒ 脚本根本跑不起来，**从第 55 轮起躺了 27 轮**。'
  + '★★★★★ **而它在我自己的门禁里全程全绿**（十三个套件 247/0、CLI 23/23、CI `syntax` PASS、CI `doc` PASS —— 只有 `env` 抓到）。'
  + '★ 编码检查器自己写着原因：「被吃掉的字符在字符串字面量内部时，**文件仍然能被解析、测试照样能跑**，只是信息悄悄变了。」'
  + '⇒ 这是第 78 轮那条结论的**第三次**应验，也是最硬的一次：**换一个轴，第一轮就撞上一处 27 轮没人看见的缺陷。**'
  + '★ 处置：**移除**，不"重建"（跑不起来 + 12 行损坏 ⇒ 忠实重建 = 猜；本仓规矩是**缺失保持缺失**；'
  + '结论已落在受版本控制的文档里；全仓只有一个**未跟踪**的提交信息提到它）。'
  + '重跑：`encoding-check: PASS（2361 个文件）`、`[env] -> PASS`。'
  + '★ 另一处 `test` FAIL **不是我的**（未登记套件 `team-hub/task-lifecycle-routes.test.mjs` 等 = 并行会话正在加的新测试文件）。'
  + '★ 也记下 CI 自己的免责声明：跑在**脏树**上（指纹 `eef49eaf18d3afe9`）⇒ 证明的是「**这棵树**是绿的」，**不是**「提交是绿的」。 |',
]

const lines = t.split('\n')
const i77 = lines.findIndex((l) => /^\| 77 \|/.test(l))
if (i77 < 0) bad.push('  x 找不到 | 77 |')
else {
  lines.splice(i77 + 1, 0, ...ROWS.map((r) => r.split('\n').join(eol)))
  t = lines.join('\n')
  console.log('  OK 家族表补上第 78～82 行')
}

// 标题推进到 83
const FROM = '**第 77 轮结束时的读数**（全部可复跑）：'
if (t.split(FROM).length - 1 !== 1) bad.push('  x 找不到读数标题（第 77 轮）')
else { t = t.replace(FROM, '**第 83 轮结束时的读数**（全部可复跑）：'); console.log('  OK 读数标题推到第 83 轮') }

// 第 83 轮那一行
if (!/^\| 83 \|/m.test(t)) {
  const ROW83 = '| 83 | ★★★ **核我提交的那 105 个 scratch 脚本**（第 82 轮抓到 1 个坏的 ⇒ 要问"还有几个"）—— '
    + '`node --check` 逐个跑：**105 个全部能解析，0 个坏的**（r55 那个是唯一一个，已移除）。'
    + '★ 但顺带查"脚本读的路径今天还在不在"时抓到**另一个同族缺陷**：`scratch/_peek-peak.mjs` 对**不存在的文件**'
    + '只打 `= ? (exit≠0)` —— 那个读数看起来像"**那个套件是红的**"，而真因是"**我在指一个不在那儿的文件**"。'
    + '⇒ 已修成先判存在、不在就**点名说出来**（现在打 `★ **文件不存在**（不是"套件是红的"）`），'
    + '并把它自称的「（**不提交**）」去掉（它**早就提交了** —— 一句自称的意图与实际状态不一致）。'
    + '> 一个读不到的文件与一个跑红了的套件，在输出里长得一样 ——'
    + '> 而它们的修法完全不同（一个要改路径，一个要改代码）。 |'
  const l2 = t.split('\n')
  const i82 = l2.findIndex((l) => /^\| 82 \|/.test(l))
  if (i82 < 0) bad.push('  x 找不到 | 82 |')
  else { l2.splice(i82 + 1, 0, ROW83); t = l2.join('\n'); console.log('  OK 家族表加第 83 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
