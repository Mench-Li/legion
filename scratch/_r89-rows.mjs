// scratch/_r89-rows.mjs —— 第 89 轮：修**我自己**那份文档里的陈旧相对日数，并**报出**台账里的同形问题（那些行归并行会话）
//
// ★ 判据来自报告 §2.0 自己立的规矩：「最早 2026-09-24（**绝对日期**，**不写"还差几天"**）」。
//   而今天系统日期是 **2026-09-21**。
// ★★ 三处「还差 N 天」里，一处是**历史**（`PRT-SESSION-REPORT-2026-09-17.md`，那份报告的名字就写着 09-17，
//    它的"还差 6 天"**在那天是对的**）⇒ **不动它**。
// ★★★ 台账 `PRT-PROGRESS.md` 的 `PRT-316` 那一行**每切一片就被并行会话追加一大段**（现在已到切片 42）
//    ⇒ 那一行**事实上归他们**。我只**报出来**，不进去改（改了会与他们的下一次追加撞上）。
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 46)}`); return }
  t = t.replace(f, tt)
}

sub('**第 88 轮结束时的读数**（全部可复跑）：', '**第 89 轮结束时的读数**（全部可复跑）：')

// ① 我自己的那处相对日数
sub(
  '3. **等到 2026-09-24**（还差 4 天）⇒ 收口 **PRT-316**。',
  '3. **等到 2026-09-24**（**绝对日期**）⇒ 收口 **PRT-316**。\n'
  + '   ★ **第 89 轮更正**：这一行原来写「（**还差 4 天**）」，是按 2026-09-20 算的；'
  + '而今天是 **2026-09-21** ⇒ 那个数**已经过期**。\n'
  + '   ⇒ 相对天数会**随读者打开的时刻**变，绝对日期不会 —— 这正是报告 §2.0 立那条规矩的原因。',
)

const ANCHOR = '#### ★★★★★ 第 87 轮：**更正我自己**第 75/76 轮的结论'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 87 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★ 第 89 轮：那条"绝对日期"的规矩，**在它自己该管的地方没被应用**',
    '',
    '报告 §2.0 自己立过一条规矩：',
    '',
    '> 最早 **2026-09-24**（**绝对日期**，**不写"还差几天"**）',
    '',
    '★ 本轮去搜全仓受版本控制的文档，量到**三处**「还差 N 天」：',
    '',
    '| 位置 | 原文 | 今天还算不算对 | 处置 |',
    '|---|---|---|---|',
    '| `PRT-SESSION-REPORT-2026-09-17.md:3068` | `PRT-316  ⬜  EXTERNAL（日期闸门，还差 6 天）` | ★ **在那天是对的**（文件名的日期就是 09-17） | **不动** —— 它是**历史留档** |',
    '| `PRT-HUMAN-INTERVENTION-2026-09-20.md:932` | `等到 2026-09-24（**还差 4 天**）` | ✖ 过期（今天 **09-21**） | ★ 本轮**已改成绝对日期** |',
    '| `PRT-PROGRESS.md:131`（**权威台账**） | `…⇒ 最早可启动 2026-09-24；今天 2026-09-18 ⇒ 还差 6 天` | ✖ 过期（今天 **09-21**） | ★★ **只报不改**，见下 |',
    '',
    '#### ★★ 为什么台账那一处我**没有**动手',
    '',
    '`PRT-PROGRESS.md` 的 `PRT-316` 那一行**不是一行普通的状态行** ——',
    '并行会话**每切一片就往这一行追加一大段**（今天已到**切片 42**），那一行现在有几万字节。',
    '⇒ 那一行**事实上归他们**：我进去改它的结尾，会与他们**下一次追加**撞上，',
    '而撞坏的是**两边的**记录。',
    '',
    '★ 所以我把那一处**报在这里**（附原文与日期），由他们或您收口。',
    '',
    '#### ★★★ 这条值得单独记：**一条规矩写在 A 处，不等于它在 B 处会被应用**',
    '',
    '更值得记的是**它被应用过**：报告 §2.3 那一行（第 78 轮我改的）就是按这条规矩写的绝对日期。',
    '⇒ 所以这条规矩**不是"没人知道"**，而是"**知道、写下来了、在别处也用了，就是没在自己这儿用**"。',
    '',
    '> 一条只写在**一处**的规矩，会让**所有其他地方**都说成另一个样子 ——',
    '> 而每一个地方单独看，都像是对的。',
    '',
    '★ 这和第 87 轮那次更正**是同一个形状**（那条"别把一次读数推广成规律"的话，我写下来了、',
    '也用在了别人身上，却没回头用在自己已经发出去的那句话上）——',
    '**第三次**了。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK §三之三 已插入第 89 轮那节')
}

if (!/^\| 89 \|/m.test(t)) {
  const ROW = '| 89 | ★★★ **那条"绝对日期"的规矩，在它自己该管的地方没被应用** —— '
    + '报告 §2.0 立过「最早 **2026-09-24**（**绝对日期**，**不写"还差几天"**）」；'
    + '本轮搜全仓受版本控制的文档，量到**三处**「还差 N 天」：'
    + '① `PRT-SESSION-REPORT-2026-09-17.md:3068` 的 `（日期闸门，还差 6 天）` —— ★ **在那天是对的**'
    + '（文件名的日期就是 09-17）⇒ **历史留档，不动**；'
    + '② `PRT-HUMAN-INTERVENTION:932` 的「**还差 4 天**」—— ✖ 过期（今天 **09-21**）⇒ ★ **已改成绝对日期**；'
    + '③ `PRT-PROGRESS.md:131`（**权威台账**）的「今天 2026-09-18 ⇒ **还差 6 天**」—— ✖ 过期 ⇒ ★★ **只报不改**。'
    + '★ 为什么台账那处不动手：那一行**不是普通状态行** —— 并行会话**每切一片就往这一行追加一大段**'
    + '（今天已到**切片 42**），现在有几万字节 ⇒ 那一行**事实上归他们**，我进去改结尾会与他们**下一次追加**撞上，'
    + '而撞坏的是**两边的**记录。'
    + '★★★ 本条值得单独记的是：**它被应用过** —— 报告 §2.3 那一行（第 78 轮）就是按这条规矩写的绝对日期 ⇒ '
    + '这条规矩**不是"没人知道"**，而是"**知道、写下来了、在别处也用了，就是没在自己这儿用**"。'
    + '> 一条只写在**一处**的规矩，会让**所有其他地方**都说成另一个样子 —— 而每一个地方单独看，都像是对的。'
    + '★ 这与第 87 轮那次更正**是同一个形状**（"别把一次读数推广成规律"那句话，我写下来了、也用在了别人身上，'
    + '却没回头用在自己已发出去的那句话上）—— **第三次**了。 |'
  const lines = t.split('\n')
  const i88 = lines.findIndex((l) => /^\| 88 \|/.test(l))
  if (i88 < 0) bad.push('  x 找不到 | 88 |')
  else { lines.splice(i88 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 89 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
