// scratch/_r94-brief.mjs —— 第 94 轮：DECISION-BRIEF 把 PRT-253 **归进了"您不需要做任何事"**，而第 67～93 轮证明相反
//
// ★ 这份文档唯一的用途就是"告诉业主该说什么话" ⇒ 把一个**需要决定**的项归进"您不需要做任何事"，
//   是这份文档能犯的**最重**的一种错：它把一个项从业主的视野里**移出去**。
//   ★★ 而它同时**通篇没有提到那 21 条红**。
import { readFileSync, writeFileSync } from 'node:fs'

const D = 'D:/project/DSH/legion/docs/DECISION-BRIEF.md'
let t = readFileSync(D, 'utf8')
const bad = []
const sub = (from, to) => {
  const n = t.split(from).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 46)}`); return }
  t = t.replace(from, to)
}

// ① §3 的标题：加上限定
sub(
  '## 3. 不是裁决——等日期 / 等环境（**您不需要做任何事**）',
  '## 3. 不是裁决——等日期 / 等环境（**您不需要做任何事**）\n'
  + '\n'
  + '> ★★★★★ **第 94 轮更正这一节的标题**：它原来把 **PRT-253** 也收在这里面。',
  + '',
)

// ② PRT-253 那一格：改成"已移出本节"
sub(
  '| PRT-253 | 一次**真的自动执行** | 真额度**已花过且已验**（`dsh --profile headless` → exit=0、9.2s）；剩下是平台边界（Windows 上不会有自动执行） |',
  '| ~~PRT-253~~ | ★★ **第 94 轮移出本节** —— 它**不是**"等环境"，见下面的 §3A | 真额度**已花过且已验**（`dsh --profile headless` → exit=0、9.2s）；而"剩下是平台边界"这个读法**第 68 轮已被证伪** |',
)

// ③ 在 §4 之前插入 §3A
sub(
  '---\n\n## 4. 您点了头之后，我立刻能做的（**按"它挡着产品完成标准"排序**）',
  '---\n'
  + '\n'
  + '## 3A. ★★★★★ 第 94 轮从上一节**移出来**的：**PRT-253 不是"等环境"，而这一页原来没告诉您**\n'
  + '\n'
  + '上一节原来的标题是「不是裁决 —— 等日期 / 等环境（**您不需要做任何事**）」，而 PRT-253 就在那张表里。',
  + '**第 68 轮已经把那个读法证伪了**，只是**这一页没有跟着改**：',
  + '',
  + '> 启动自检**恒**判不兼容 —— `runtimeCapabilityEvidence()` 要的四项必需能力里，',
  + '> **三项是写死的 `satisfied: false` 字面量**（`runtime-host-registrar-row.mjs` 函数体内恰好 3 处），',
  + '> 而 probe 要求"**显式为 true**" ⇒ **`autoExecutionForbidden` 恒为 true，与本机平台无关**。',
  + '>',
  + '> ⇒ PRT-253 等的那个对象（"一次真的自动执行"）**在这条路上不存在**。',
  + '',
  + '#### ★★ 而它有一个**后果**，这一页通篇没有提：**21 条端到端用例红**',
  + '',
  + '| 簇 | 条数 | 是什么 | 修法 |',
  + '|---|---|---|---|',
  + '| **自检恒不兼容**（设计常量） | 多数 | 断言"自检通过 / `dshRuntimeBound() === true` / 解除阻塞"这类**构造上不可达**的状态 | 把没跟上的用例**对齐**到仓里**已经有**的结论 |',
  + '| **路由抽取器只读一个文件** | 2 | `extractRoutes(server.mjs)` 看不见已搬进族模块的路由 | ★ **改工具**（另一个会话的搬运工作）—— 与您无关 |',
  + '| **反向对照的前提没了** | 少数 | 「不挂它就该 `absent`」—— 而它**默认就挂着** | ★★ **不能改期望**：要重新设计对照条件 |',
  + '',
  + '#### ★★★★★ 而第 93 轮量到一件把这件事**变轻**的事实',
  + '',
  + '**"接受这个设计"不是一次新规范** —— 那个规范**仓里已经写了，而且已经有一条绿的用例**：',
  + '',
  + '- 同文件的**头**（`runtime-host-binding-unblocked-dsh-process.test.mjs:374`）写着「③ **`dshRuntimeBound() === false`**」；',
  + '- 同文件 **②**（L623）断言 `exit 0` / `SELF_CHECK_INCOMPATIBLE` / `incompatible` / `forbid:true` —— ★ **而 ② 是绿的**。',
  + '',
  + '> ⇒ 缺的**不是**"要不要接受一个设计"，而是**让那几条没跟上的用例跟上**。',
  + '',
  + '#### ⇒ 所以这一条要您说的，比原来那三条路**简单**',
  + '',
  + '| 路 | 现在看起来的代价（第 94 轮按第 91～93 轮的量法重写） |',
  + '|---|---|',
  + '| **乙（对齐用例）** | 把没跟上的用例对齐到**仓里已有**的结论（② + 文件头），外加**重新设计少数几条反向对照** —— ★ **不是发明规范** |',
  + '| **甲（放宽自检）** | ★ 仍是"在我不知道的地方把一个 fail-closed 的自检变松" —— 而**乙变轻之后，甲更不该选** |',
  + '| **丙（给三项能力真来源）** | ★ 要改**引擎契约**（`SubagentResult` 没有用量字段）—— **不在本仓范围内** |',
  + '',
  + '★ 也就是说：**您只需要说"按乙办"** —— 而这一页原来把这一项写成"您不需要做任何事"。',
  + '',
  + '---\n'
  + '\n'
  + '## 4. 您点了头之后，我立刻能做的（**按"它挡着产品完成标准"排序**）',
)

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(D, t)
console.log('  OK DECISION-BRIEF：PRT-253 已移出 §3，并补上 §3A（含那 21 条与"乙不是发明"）')
