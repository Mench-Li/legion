// scratch/_r94-options.mjs —— 第 94 轮：用第 91/92/93 轮的量法，**重写 Ⅰ 那三条路的代价**
//
// ★ 那三行是第 63 轮写的，而第 91～93 轮量到三件它不知道的事：
//   ① 那 21 条**不是一个原因**（至少三簇，修法各不相同）—— 第 91/92 轮；
//   ② **"接受设计"那一侧仓里已经写了、而且已经有一条绿的用例**（② 与文件头）—— 第 93 轮；
//   ③ 其中一族（**反向对照**）的修法**不是改期望** —— 第 92 轮。
//   ⇒ 于是**乙的代价被写大了**："等于把'执行器不可用'写成预期" —— 那个预期**已经在仓里**。
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 46)}`); return }
  t = t.replace(f, tt)
}
sub('**第 93 轮结束时的读数**（全部可复跑）：', '**第 94 轮结束时的读数**（全部可复跑）：')

// ① 标题改成"第 94 轮重写"
sub('#### 要您裁的：三条路，各有代价', '#### 要您裁的：三条路，各有代价（★ **第 94 轮按第 91～93 轮的量法重写了代价**）')

// ② 乙 那一格的代价：更正
sub(
  '| **乙** | 改那 21 条用例的**期望**（承认"自检不兼容"是当前正确行为） | ★ 等于把"执行器不可用"写成预期。门禁会变绿，而**产品仍然不能自动执行** |',
  '| **乙** | 把没跟上的用例**对齐**到"自检不兼容是当前正确行为" | ★★★ **第 94 轮更正这一格的代价**：原文写「等于把"执行器不可用"**写成**预期」—— '
  + '而第 93 轮量到**那个预期已经在仓里**：同文件的头（`runtime-host-binding-unblocked-dsh-process.test.mjs:374`）写着 '
  + '「③ **`dshRuntimeBound() === false`**」、而同文件 **②**（L623）断言 `exit 0` / `SELF_CHECK_INCOMPATIBLE` / '
  + '`incompatible` / `forbid:true` —— **② 是绿的**。⇒ 乙**不是发明一个规范**，是**把仓里已有的规范补齐到没跟上的那几条**。'
  + '★ 仍要付的代价：门禁变绿而**产品仍然不能自动执行**（这一半原文是对的）；'
  + '外加第 92 轮量到的——那 21 条里有一族是**反向对照**，它们的修法**不是改期望**，要**重新设计对照条件** |',
)

// ③ 表格后补一段"三簇与工量"
sub(
  '★ **我选的是"不动、报上来"**：让这两行豁免，是在我不知道的地方把一个自检变松；\n而"变绿"与"变可用"是两件事 —— 这条路我判不了，也不该替您判。',
  '★ **我选的是"不动、报上来"**：让这两行豁免，是在我不知道的地方把一个自检变松；\n'
  + '而"变绿"与"变可用"是两件事 —— 这条路我判不了，也不该替您判。\n'
  + '\n'
  + '#### ★★★★★ 第 94 轮：这三条路里的"21 条"，第 91～93 轮量出来**不是一个东西**',
  '',
  '第 63 轮我写这张表时说的是"那 21 条用例"。第 91～93 轮**逐条读**（读标题、读输入、读文件头）之后：',
  '',
  '| 簇 | 是什么 | 修法 |',
  '|---|---|---|',
  '| **自检恒不兼容**（设计常量） | 断言"自检通过 / `dshRuntimeBound() === true` / 解除阻塞"这类**构造上不可达**的状态 | 改**期望** —— 而对齐目标**仓里已经有**（② 是绿的） |',
  '| **路由抽取器只读一个文件** | `extractRoutes(server.mjs)` 看不见已搬进族模块的路由 | 改**工具**（并行会话的搬运工作）—— ★ **与您无关** |',
  '| **反向对照的前提没了** | B. / ④ / ⑥："不挂它就该 `absent`" —— 而它**默认就挂着**（`2f5a4b3` 放进了 `PATCH_LAYER_ROWS`） | ★★ **不能改期望**：`absent` 在新架构里不可表达 ⇒ 要么**换一种真能摘掉它的"不挂"办法**，要么**承认这条对照失效并删掉** |',
  '',
  '⇒ **所以 乙 的工量不是"改 21 条"，而是"改大多数 + 重新设计少数几条反向对照"**。',
  '⇒ 而 **甲**（放宽自检）与 **丙**（给三项能力真来源）**都不受影响** —— 它们改的是产品，不是用例。',
  '',
  '★★★★★ **而这一轮真正的收获是把 Ⅰ 的重量称准了**：',
  '',
  '> 第 63 轮我把 Ⅰ 摆成"要不要接受这个设计"。',
  '> 而第 93 轮量到：**这个设计仓里早就接受了** —— 同一份文件的头写着 `false`、② 断言着 `incompatible`、② 是绿的。',
  '>',
  '> ⇒ Ⅰ 里"接受设计"那一路，**不是一次设计决定**，而是**把没跟上的用例补齐**。',
  '> 缺的从来不是一个决定，是让那几条跟上。',
  '',
)

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
console.log('  OK Ⅰ 的三条路已按第 91～93 轮重写')
