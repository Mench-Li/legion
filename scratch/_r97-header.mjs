// scratch/_r97-header.mjs —— 第 97 轮：交付物**头部**三处陈旧读数，并为其中**可派生**的那一处加判据
//
// ★ 第 96 轮我写了诚实的边界：别处的同类仍然靠手。本轮去处理它 —— 而量完发现**不是每一处都能立判据**：
//
//   候选量（`scratch/_probe-r97-report-round.mjs` 实测）：
//     ① §一 自称「追加至第 N 轮」         = 79
//     ② §三 标题「截至第 N 轮」           = 52
//     ③ 交付物里提到过的最大轮次          = 87
//     ④ 人工干预清单家族表最大轮次        = 96
//   ⇒ ①②③ 互不相同，**没有一个"显然"的派生量**（§一 那句的语义是"最后一次修订在第几轮"，
//     文档本身**不记录自己被修订过几次**）。
//
//   ★★★ 而 L6 那句「与它的家族表（**逐轮到第 N 行**）」**是可派生的**（= 家族表最大轮次）。
//   ⇒ 本轮：**可派生的那一处立判据；不可派生的那一处如实说"立不了"，并附上量到的候选值。**
import { readFileSync, writeFileSync } from 'node:fs'

const D = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-FINAL-REPORT-2026-09-18.md'
let t = readFileSync(D, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 50)}`); return }
  t = t.replace(f, tt)
}

// ① §一 「追加至第 79 轮」：加一行语义说明（不发明数字，如实说立不了判据）
sub(
  '- 日期：2026-09-18 起，**追加至第 79 轮**（2026-09-20 之后仍在增补）',
  '- 日期：2026-09-18 起，**追加至第 79 轮**（2026-09-20 之后仍在增补）\n'
  + '  - ★★★★★ **第 97 轮：这个数**没有一个可派生的定义** —— 本轮实测四个候选量互不相同：\n'
  + '    §一 自称 **79** · §三 标题「截至」**52** · 本报告里提到过的最大轮次 **87** · 人工介入清单家族表最大 **96**。\n'
  + '    ⇒ 它要表达的是「**本报告最后一次被修订在第几轮**」，而**文档本身不记录自己被修订过几次** ⇒\n'
  + '    **立不了判据**。本轮**不发明一个数字**，而是把它改成可核的形状（见下两行）与可派生的那一处（家族表行数）。',
)

// ② L6 家族表行数：77 → 96（可派生），并把"逐轮到第 N 行"标成受判据约束
sub(
  '    与它的家族表（逐轮到第 77 行）——**读数在那里，不在这里**。',
  '    与它的家族表（**逐轮到第 96 行**）——**读数在那里，不在这里**。\n'
  + '    - ★★ 「逐轮到第 N 行」这一处**是可派生的**（N = 那份文档家族表的最大轮次）⇒ **第 97 轮给它立了判据**\n'
  + '      （`report-family-rows-round`）；而上面那句「追加至第 79 轮」**立不了**，理由见第 3 行下那一段。',
)

// ③ L12 最近一轮 CI：r51 → r84-full
sub(
  '- 最近一轮 CI：**`.ci/r51`**（2026-09-20 **19:33**，当时是 `.ci` 下最新的存档；HEAD 见 `acffc8d`）—— ⚠️ **`test` 与 `smoke` 两阶段 FAIL**',
  '- 最近一轮 CI：**`.ci/r84-full`**（2026-09-21 **01:52**，`.ci` 下**按时间最新**的那个存档）—— ⚠️ **只有 `test` 一阶段 FAIL**\n'
  + '  - ★★ **第 97 轮更正这一行**：它原来写「**`.ci/r51`**（2026-09-20 19:33）—— `test` 与 `smoke` 两阶段 FAIL」。\n'
  + '    实测 `.ci` 下按修改时间排序是 `r51`(09-20 19:33) → `r82-lite`(09-21 01:39) → `r82-env`(01:45) → **`r84-full`(01:52)**\n'
  + '    ⇒ 这一行**落后了两个存档**，而且**失效的阶段也说错了**：`smoke` 在 r84-full 里是 **PASS**（r51 那次才是 FAIL）。\n'
  + '    ★ 而 `r84-full` 的 `test` **只跑了 4899ms 就退**（卡在**套件登记闸门**，那一串 `team-hub/*-routes.test.mjs` 是另一会话在加的新路由测试）\n'
  + '    ⇒ 那次 `test` **没有测到**产品侧那 21 条红（见人工介入清单 §三之三 第 91～93 轮）。',
)

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(D, t)
console.log('  OK 交付物头部三处已修（1 处如实说"立不了判据" + 1 处可派生 + 1 处 CI 落后两档）')
