// scratch/_r97-rows.mjs —— 第 97 轮：可派生的那一处立判据；**不可派生的那一处如实说立不了**
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 96 轮结束时的读数**（全部可复跑）：', '**第 97 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 96 轮**', '本文件已**复核到第 97 轮**')

const ANCHOR = '#### ★★★★★ 第 96 轮：把"我用手找了四次的那一类"变成**一条判据**'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 96 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 97 轮：接着上一轮的边界 —— **可派生的立判据，不可派生的如实说"立不了"**',
    '',
    '第 96 轮我留了一条诚实边界：那条新判据只钉**这份文档里那一句活口径**，',
    '别处的同类（**交付物**那句「追加至第 N 轮」）**仍然靠手**。本轮去处理它 ——',
    '而量完发现：**不是每一处都能立判据。**',
    '',
    '#### 一、先去量：那句「追加至第 N 轮」到底该等于哪个可派生的量',
    '',
    '`scratch/_probe-r97-report-round.mjs`（实测，不是推断）：',
    '',
    '| 候选量 | 今天的值 |',
    '|---|---|',
    '| ① 交付物 §一 **自称**「追加至第 N 轮」 | **79** |',
    '| ② 交付物 §三 标题「截至第 N 轮」 | **52** |',
    '| ③ 交付物里**提到过**的最大轮次 | **87** |',
    '| ④ 那份清单的家族表最大轮次 | **96** |',
    '',
    '⇒ ①②③ **互不相同**，**没有一个"显然"的派生量**。',
    '★ 而 §一 那句真正要表达的是「**本报告最后一次被修订在第几轮**」——',
    '**文档本身不记录自己被修订过几次** ⇒ **它不可派生。**',
    '',
    '★★★★★ **所以本轮不给它立判据**：给一个**没有定义**的东西立判据，',
    '等于**把我猜的数字写成规矩** —— 那比不立更坏。',
    '（本轮的做法是在交付物那一行下面**写明它立不了**，并把上面这张表放进去当证据。）',
    '',
    '#### 二、而同一屏里有一处**是可派生的** —— 它立了判据 `report-family-rows-round`',
    '',
    '交付物头部那句「与它的家族表（**逐轮到第 N 行**）」：**N = 那份清单家族表的最大轮次**（清晰可派生）。',
    '',
    '★ 而它当时写着 **77**，真实是 **96** ⇒ **落后 19 轮**。',
    '',
    '#### 三、★★★ 而修它的时候撞到"同一个声明散在两处"',
    '',
    '那个数字在交付物里出现了**两处**：头部一行、以及 §三 的「第 78 轮注」一行 —— **两处都写着 77**。',
    '',
    '★ 本套件的 claim 模型**要求锚点唯一**（命中多于一处 ⇒ `ANCHOR_AMBIGUOUS` 判红；',
    '`boundary-facts.mjs` 里 `patch-rows-doc-count` 那段记过同一个形状上踩的坑）。',
    '',
    '⇒ 于是有两条路：',
    '',
    '| 路 | 后果 |',
    '|---|---|',
    '| 用**加粗形状**只钉住其中一处 | ★ 另一处**留给手** —— 而这一处的毛病恰恰是"两处都会漂" |',
    '| **先把重复的那处去掉数字**，只留一处可判 | ★★ 声明只剩一处 ⇒ 改一处就够 |',
    '',
    '★ 选了后者。',
    '',
    '> 一个"两处都写着同一个数"的文档，与一个"只有一处写了、另一处指向它"的文档，',
    '> 在"下一轮会不会漂"这个读数上不一样：前者要改两处，后者只需改一处。',
    '',
    '#### 四、验证（三步，每步都是读数）',
    '',
    '| 步 | 动作 | 读数 |',
    '|---|---|---|',
    '| ① | 加判据后跑 CLI | 参与比对 **24 → 25**，且 `✔ report-family-rows-round` |',
    '| ② | **变异**：交付物 `96` → `95`，另起进程 | 红 **1 → 2** ⇒ **咬住了**（哨兵已清、原文件已还原） |',
    '| ③ | 加单元用例 ⑰d（真仓绿 + 反向控制 + **锚点唯一**） | 套件 **60 → 61 通过**，失败**仍是那 2 条** |',
    '',
    '★ 第 ③ 步里我**另加了一条断言**：那句坐标在交付物里**必须只出现一处** ——',
    '这样"有人把去重撤销了"会当场红，而不是等到下一轮漂了才发现。',
    '',
    '#### 五、顺带：交付物头部**另有两处陈旧读数**',
    '',
    '1. 「最近一轮 CI：**`.ci/r51`**（2026-09-20 19:33）—— `test` 与 `smoke` 两阶段 FAIL」',
    '   ⇒ ★ 实测 `.ci` 下按修改时间最新的是 **`.ci/r84-full`**（09-21 01:52），**落后两个存档**；',
    '   而且**失效的阶段也说错了** —— `smoke` 在 r84-full 里是 **PASS**。',
    '2. 已改为 r84-full 并记明：那次 `test` **只跑了 4899ms 就退**（卡在套件登记闸门）⇒ **没测到**那 21 条红。',
    '',
    '#### ★★★★★ 六、这一轮与上一轮合起来是一条完整的做法',
    '',
    '| 情形 | 做法 |',
    '|---|---|',
    '| 有清晰派生量（家族表最大轮次） | **立判据** + 变异验证它咬得住（第 96、97 轮各一条） |',
    '| **没有**清晰派生量（"最后一次修订在第几轮"） | ★ **如实说"立不了"**，把量到的候选值写下来当证据 —— **不发明数字** |',
    '',
    '> 而"我在这处立了判据"与"我在每一处都立了判据"，在**我没量过它到底能不能派生**的时候是同一个说法。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK §三之三 已插入第 97 轮那节')
}

if (!/^\| 97 \|/m.test(t)) {
  const ROW = '| 97 | ★★★★★ **可派生的立判据，不可派生的如实说"立不了"** —— '
    + '第 96 轮留的边界（别处同类仍靠手）本轮去处理，量完发现**不是每一处都能立判据**。'
    + '★ `scratch/_probe-r97-report-round.mjs` 实测四个候选量：§一 自称 **79** · §三「截至」**52** · '
    + '交付物里提到过的最大轮次 **87** · 清单家族表最大 **96** ⇒ ①②③ **互不相同、没有一个"显然"的派生量**；'
    + '而 §一 那句真正要表达的是「**本报告最后一次被修订在第几轮**」，**文档本身不记录自己被修订过几次** ⇒ **不可派生**。'
    + '★★★★★ **所以不给它立判据** —— 给一个**没有定义**的东西立判据，等于**把我猜的数字写成规矩**，那比不立更坏；'
    + '本轮的做法是在那一行下面**写明它立不了**，并把那张候选值表放进去当证据。'
    + '★★ 而同一屏里「与它的家族表（**逐轮到第 N 行**）」**是可派生的** ⇒ 立了判据 **`report-family-rows-round`**：'
    + '它当时写 **77**、真实 **96**（**落后 19 轮**）。'
    + '★★★ 修它时撞到**同一个声明散在两处**（头部一行 + §三「第 78 轮注」一行，**都写 77**），'
    + '而本套件 claim 模型**要求锚点唯一**（多处命中 ⇒ `ANCHOR_AMBIGUOUS`）⇒ '
    + '**主动放弃**"用加粗形状只钉住一处"（那会把另一处**留给手**），改为**先把重复那处去掉数字**、只留一处可判。'
    + '★ 验证三步：① CLI 参与比对 **24 → 25** 且 `✔`；② **变异**（交付物 `96`→`95`，另起进程）⇒ 红 **1 → 2** ⇒ **咬住了**'
    + '（`scratch/_mutate-r97-familyrows.mjs`，哨兵已清、原文件已还原）；'
    + '③ 单元用例 ⑰d（真仓绿 + 反向控制 + **锚点唯一**）⇒ 套件 **60 → 61 通过**、失败仍是那 2 条。'
    + '★ 顺带修交付物头部另一处陈旧读数：最近一轮 CI 原写 **`.ci/r51`**（落后**两个**存档），'
    + '实测最新是 **`.ci/r84-full`**（09-21 01:52），且**失效的阶段也说错了**（`smoke` 在 r84-full 里是 PASS）。'
    + '★★★★★ 两轮合起来是一条完整做法：**有清晰派生量 ⇒ 立判据 + 变异验证；没有 ⇒ 如实说立不了、把候选值写下来**。'
    + '> 而"我在这处立了判据"与"我在每一处都立了判据"，在**我没量过它到底能不能派生**的时候是同一个说法。 |'
  const lines = t.split('\n')
  const i96 = lines.findIndex((l) => /^\| 96 \|/.test(l))
  if (i96 < 0) bad.push('  x 找不到 | 96 |')
  else { lines.splice(i96 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 97 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
