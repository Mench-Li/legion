// scratch/_r98-rows.mjs —— 第 98 轮：★ **不是每一条"重复的手工发现"都该变成判据** —— 本轮量到了判据机制的边界
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 97 轮结束时的读数**（全部可复跑）：', '**第 98 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 97 轮**', '本文件已**复核到第 98 轮**')

const ANCHOR = '#### ★★★★★ 第 97 轮：接着上一轮的边界'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 97 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 98 轮：**不是每一条"重复的手工发现"都该变成判据** —— 本轮量到了判据机制的边界',
    '',
    '第 96、97 两轮我立了两条判据，做法是"把重复的手工发现变成判据"。',
    '★ 本轮顺着这个做法往下找，候选有两个，**而量完之后两个都不该立** —— 理由各不相同，都值得记。',
    '',
    '#### 候选一：交付物那句「最近一轮 CI：`.ci/XXX`」—— ★★ **它立不了，因为输入不在这个仓里**',
    '',
    '它在第 97 轮刚被我手工修过（原写 `.ci/r51`、落后**两个**存档）。看起来正是"该立判据"的形状。',
    '',
    '★★ 而我去量了 `.ci/` 本身：',
    '',
    '```',
    'git check-ignore -v .ci   →   .gitignore:43:.ci/    .ci',
    'git ls-files .ci          →   （0 个被跟踪的文件）',
    '```',
    '',
    '⇒ **`.ci/` 被 gitignore**、仓里**一个文件都不跟踪**。',
    '⇒ 任何读它的判据，在**另一个人的检出上**（或一次干净的 clone 上）会**没有输入** ——',
    '它要么红（假红），要么静默跳过（那正是本套件最反对的那种"不许静默跳过"）。',
    '',
    '★★★★★ **所以不立。** 而这一条的理由与第 97 轮那条**不同**：',
    '',
    '> 第 97 轮那条是「**没有一个清晰的派生量**」；',
    '> 这条是「**派生量存在，但它的输入不在这个仓里**」。',
    '',
    '#### 候选二：那条"绝对日期"的规矩（**不写"还差几天"**）—— ★★★ **它立不了，因为要区分"活读数"与"引用旧值"，而那是语义区分**',
    '',
    '本仓有明文规矩（交付物 §2.0）：「最早 **2026-09-24**（**绝对日期**，**不写"还差几天"**）」。',
    '★ 而第 89 与第 95 两轮，我**各抓到一处**违反它的地方 —— 两次都是**用手找的**。',
    '⇒ 看起来是立判据的最佳候选。',
    '',
    '★★ 而 `scratch/_probe-r98-relativedays.mjs` 量出来：那类写法在四份人类文档里还剩 **11 处**，',
    '**而它们几乎全是历史或引证**：',
    '',
    '| 位置 | 是什么 | 该不该红 |',
    '|---|---|---|',
    '| 本文件 L668-670 | 一张**专门记录"哪几处读数过期了"的表** —— 三处都是**引证** | ★ **不该** |',
    '| 本文件 L1258/1259/1276 | **家族表的历史行**（记录第 58/60/78 轮当时的发现） | ★ **不该** |',
    '| 本文件 L1287/1293/1379 | 我**自己写的更正说明**（引用我改掉的旧值） | ★ **不该** |',
    '| 交付物 L223 | 同上（§2.3 那条更正） | ★ **不该** |',
    '| 本文件 L28 | ★★ 那一格**今天写的是绝对日期**（`绝对日期 2026-09-24`），"还差 4 天"在**引号里** | ★ **不该** |',
    '',
    '⇒ ★★★ **一条正则判据会把上面每一处都判红** —— 而它们**全部是合法的**。',
    '那条判据要的其实是「**活读数不许用相对日数，但引用旧值可以**」，',
    '而**"这是活读数还是引用"是个语义判断**，正则做不了。',
    '',
    '★★★★★ **所以也不立。** 立了它，我就得**为了让门禁变绿而去删掉自己的历史** ——',
    '那是这一整批里我最不该做的事。',
    '',
    '#### ⇒ 于是"该不该立判据"有了**四个**桶，而不是两个',
    '',
    '| 桶 | 判据能不能立 | 例子 |',
    '|---|---|---|',
    '| **A. 有清晰派生量** | ★ **立** + 变异验证它咬得住 | 第 96 轮（口径轮次）、第 97 轮（家族表行数） |',
    '| **B. 没有清晰的派生量** | ✖ 立不了 —— 如实说，别发明数字 | 第 97 轮的「追加至第 N 轮」（实测四个候选值互不相同） |',
    '| **C. 派生量存在，但输入不在仓里** | ✖ 立不了 —— 换台机器就没输入 | 本轮候选一（`.ci/` 被 gitignore） |',
    '| **D. 派生量存在，但判据要区分语义** | ✖ 立不了 —— 会误伤历史与引证 | 本轮候选二（活读数 vs 引用旧值） |',
    '',
    '★ 而 B/C/D 三个桶的处置是**同一个**：**在文档里写明它立不了、附上量到的证据**，',
    '**并且继续用手看它** —— 而不是为了"门禁覆盖得更全"去立一条会误伤东西的判据。',
    '',
    '★★★★★ **这一轮最值记的一点**：',
    '',
    '> 第 96 轮我写下「一条只会被我读出来的缺陷，值一条判据」。',
    '> ★ 那句话**是对的，但不完整** —— 它还缺一个条件：',
    '> **那条判据必须能在别人的检出上、在不清除历史的前提下，得出同一个结论。**',
    '>',
    '> 少了这个条件，"给它立一条判据"就会变成"把历史改成门禁喜欢的样子"。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK §三之三 已插入第 98 轮那节')
}

if (!/^\| 98 \|/m.test(t)) {
  const ROW = '| 98 | ★★★★★ **不是每一条"重复的手工发现"都该变成判据** —— 本轮量到了判据机制的**边界**。'
    + '第 96/97 两轮的做法是"把重复的手工发现变成判据"，本轮顺着往下找，**两个候选量完之后都不该立**，理由各不相同。'
    + '★ **候选一**：交付物那句「最近一轮 CI：`.ci/XXX`」（第 97 轮刚手工修过，看着正该立判据）—— '
    + '★ 而实测 `git check-ignore -v .ci` → `.gitignore:43:.ci/`、`git ls-files .ci` → **0 个文件** ⇒ '
    + '**`.ci/` 被 gitignore、仓里一个都不跟踪** ⇒ 读它的判据在**别人的检出上会没有输入**（要么假红、要么静默跳过）。'
    + '★★ 理由与第 97 轮那条**不同**：那条是「**没有清晰的派生量**」，这条是「**派生量存在，但输入不在这个仓里**」。'
    + '★★★ **候选二**：那条"绝对日期"的规矩（交付物 §2.0「不写"还差几天"」）—— 第 89/95 两轮我各用手抓到一处，'
    + '看着是立判据的最佳候选；而 `scratch/_probe-r98-relativedays.mjs` 量出**四份人类文档里还剩 11 处，几乎全是历史或引证**'
    + '（一张专门记录"哪几处读数过期"的表、家族表的历史行、我自己写的更正说明、以及本文件 L28 那格**今天写的已是绝对日期**、"还差 4 天"在引号里）'
    + '⇒ ★★★ **一条正则判据会把它们全部判红，而它们全都合法**：判据要的是「活读数不许用相对日数、引用旧值可以」，'
    + '而**"这是活读数还是引用"是语义判断**，正则做不了。'
    + '★★★★★ 所以也不立 —— 立了它，我就得**为了让门禁变绿而去删掉自己的历史**。'
    + '⇒ 「该不该立判据」于是有**四个桶**：**A** 有清晰派生量 ⇒ **立** + 变异验证（96/97 轮）；'
    + '**B** 没有清晰派生量 ⇒ 立不了、别发明数字（97 轮的「追加至第 N 轮」）；'
    + '**C** 派生量在、输入不在仓里 ⇒ 立不了（本轮候选一）；**D** 派生量在、但要区分语义 ⇒ 立不了（本轮候选二）。'
    + '★ B/C/D 的处置同一个：**在文档里写明它立不了、附上量到的证据，并继续用手看它**。'
    + '★★★★★ 最值记的一点：第 96 轮我写下「一条只会被我读出来的缺陷，值一条判据」—— '
    + '★ 那句话**对，但不完整**，它还缺一个条件：**那条判据必须能在别人的检出上、在不清除历史的前提下，得出同一个结论。**'
    + '> 少了这个条件，"给它立一条判据"就会变成"把历史改成门禁喜欢的样子"。 |'
  const lines = t.split('\n')
  const i97 = lines.findIndex((l) => /^\| 97 \|/.test(l))
  if (i97 < 0) bad.push('  x 找不到 | 97 |')
  else { lines.splice(i97 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 98 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
