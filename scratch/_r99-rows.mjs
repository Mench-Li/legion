// scratch/_r99-rows.mjs —— 第 99 轮：① 我名下的四个 CI 阶段全绿；② ★ 更正第 93 轮那条补记
import { readFileSync, writeFileSync } from 'node:fs'

const I = 'D:/project/DSH/legion/docs/superpowers/prt/PRT-HUMAN-INTERVENTION-2026-09-20.md'
let t = readFileSync(I, 'utf8')
const eol = t.includes('\r\n') ? '\r\n' : '\n'
const bad = []
const sub = (from, to) => {
  const f = from.split('\n').join(eol)
  const tt = to.split('\n').join(eol)
  const n = t.split(f).length - 1
  if (n !== 1) { bad.push(`  x 命中 ${n} 次：${from.slice(0, 48)}`); return }
  t = t.replace(f, tt)
}
sub('**第 98 轮结束时的读数**（全部可复跑）：', '**第 99 轮结束时的读数**（全部可复跑）：')
sub('本文件已**复核到第 98 轮**', '本文件已**复核到第 99 轮**')

const ANCHOR = '#### ★★★★★ 第 98 轮：**不是每一条"重复的手工发现"都该变成判据**'
const at = t.indexOf(ANCHOR)
if (at < 0) bad.push('  x 找不到第 98 轮那节的锚点')
else {
  const SEC = [
    '#### ★★★★★ 第 99 轮：更正第 93 轮那条补记 —— **那段说明一直都在，是我没读到它**',
    '',
    '第 93 轮的补记里我写下：',
    '',
    '> `boundary-facts` 读的是**工作区**，而工作区正被另一个会话实时改写',
    '> ⇒ 我先前报的「89/89」**不是我这批提交的性质**，而是「我跑它的那一刻，那棵树恰好是一致的」。',
    '',
    '★★ 本轮我跑了**自己名下的四个 CI 阶段**，而 `run-ci.mjs` 的输出末尾**一直印着这样一段**：',
    '',
    '```',
    '⚠⚠ 本次 CI 跑在一棵**脏树**上：已改 18 个文件 + 未跟踪 717 个（指纹 0beb8d904a34edc2）。',
    '   ⇒ 这次读数证明的是「**这棵树**是绿的」，**不是**「提交 8d9cbe9 是绿的」。',
    '   引用它时请连树一起引用；`git head=` 回答的是"哪个提交"，不是"跑的哪棵树"。',
    '```',
    '',
    '★★★★★ ⇒ **第 93 轮我当成"自己发现的一条隐藏依赖"的那件事，工具把它印在每一次运行的末尾** ——',
    '连**树指纹**都给了。我没有读到它，是因为我在输出里 `Select-String` 只挑了自己关心的行。',
    '',
    '★ 所以第 93 轮那条补记的**措辞太重**：它说"我从没问过它读的那些文件里有没有别人正在写"，',
    '而正确的说法是「**它每次都告诉我了，我没读那一段**」。',
    '',
    '#### 本轮名下的四个阶段（都是绿的）',
    '',
    '| 阶段 | 读数 |',
    '|---|---|',
    '| `syntax` | **PASS**（6015ms） |',
    '| `env` | **PASS** —— `config scan` 1169 个疑似字面量、`encoding-check` **2425 个文本文件无 U+FFFD**、`config sync` 白板副本一致 |',
    '| `doc` | **PASS** —— `check-docs` 11 类校验项全绿、`spec-progress` **140/145 与台账一致** |',
    '| `boundary` | **PASS** —— `dsh-boundary`（执行面依赖未增长：7 文件/85 处，均在基线内）、`dsh-pin-drift`（6 条结论、15 个锚点逐字命中） |',
    '',
    '#### ★★★ 而本轮我**没有**做的一件事，以及为什么',
    '',
    '第 92/93 轮量出：那 21 条红里有一族（自检恒不兼容）的对齐目标**仓里已经写好**（同文件头 `:374` + 一条**绿的**用例 ②）。',
    '★ 而本轮我核过：那 5 个失败套件**全部空着**（没有别人在改）—— 也就是说**动手的条件是有的**。',
    '',
    '★★★★★ **我没有动，理由是**：裁决 **Ⅰ** 是您还没回答的问题，而**乙的已知代价**恰恰是',
    '「**门禁会变绿，而产品仍然不能自动执行**」。',
    '',
    '> 我现在去把那 19 条改成断言"不兼容是正确的"，门禁会绿，',
    '> 而**那个绿正是这一整批里我一直在批评的那种绿** —— 把产品的一个真实限制**盖住**。',
    '',
    '★ 所以本轮只做了**可核的、不改变任何断言**的事（跑四个阶段、更正上面那条措辞），',
    '并把"动手条件已具备"这一条**写清楚**，让它等着您那一句话（第 94 轮 §3A：「按乙办」）。',
    '',
    '★★ 另一条同期的阻塞没变：**#28 那三个改点文件仍被并行会话占着**（`product/config-schema.mjs` · `product/launcher/launcher.mjs` · `product/process-manifest.mjs` —— 三个都是 ` M `）。',
    '',
  ].join('\n')
  t = t.slice(0, at) + SEC + t.slice(at)
  console.log('  OK §三之三 已插入第 99 轮那节')
}

if (!/^\| 99 \|/m.test(t)) {
  const ROW = '| 99 | ★★★★★ **更正第 93 轮那条补记 —— 那段说明一直都在，是我没读到它** —— '
    + '第 93 轮我把「`boundary-facts` 读的是工作区、而工作区正被另一会话实时改写」写成一条**自己发现的隐藏依赖**。'
    + '★★ 本轮跑自己名下的四个 CI 阶段，而 `run-ci.mjs` 的输出末尾**一直印着**：'
    + '「⚠⚠ 本次 CI 跑在一棵**脏树**上：已改 18 个文件 + 未跟踪 717 个（**指纹** 0beb8d904a34edc2）'
    + '⇒ 这次读数证明的是「**这棵树**是绿的」，**不是**「提交 8d9cbe9 是绿的」」。'
    + '★★★★★ ⇒ **工具每次都告诉我了**（连树指纹都给了）；我没读到，是因为我在输出里 `Select-String` 只挑了自己关心的行。'
    + '★ 所以第 93 轮那条补记**措辞太重** —— 正确的说法是「**它每次都告诉我了，我没读那一段**」。'
    + '★ 本轮名下四个阶段**全绿**：`syntax` PASS·6015ms；`env` PASS（1169 个疑似字面量、**encoding-check 2425 个文本文件无 U+FFFD**、白板副本一致）；'
    + '`doc` PASS（`check-docs` 11 类校验项全绿、**spec-progress 140/145 与台账一致**）；'
    + '`boundary` PASS（`dsh-boundary` 执行面依赖未增长 7 文件/85 处均在基线内、`dsh-pin-drift` 6 条结论 15 个锚点逐字命中）。'
    + '★★★ **而本轮我特意没有做的一件事**：第 92/93 轮量出那 21 条红里"自检恒不兼容"那一族的对齐目标**仓里已写好**'
    + '（同文件头 `:374` + 一条**绿的**用例 ②），而本轮核过那 5 个失败套件**全部空着** ⇒ **动手条件已具备**。'
    + '★★★★★ **我没动，理由是**：裁决 **Ⅰ** 是您还没回答的问题，而**乙的已知代价**恰恰是「**门禁会变绿，而产品仍然不能自动执行**」——'
    + '现在把那 19 条改成断言"不兼容是正确的"，**那个绿正是我这一整批一直在批评的那种绿**（把产品的一个真实限制盖住）。'
    + '★ 所以只做了**不改变任何断言**的事，并把"动手条件已具备"写清楚，等您那一句话（第 94 轮 §3A「按乙办」）。'
    + '★★ 同期阻塞未变：**#28 那三个改点文件仍被占着**。 |'
  const lines = t.split('\n')
  const i98 = lines.findIndex((l) => /^\| 98 \|/.test(l))
  if (i98 < 0) bad.push('  x 找不到 | 98 |')
  else { lines.splice(i98 + 1, 0, ROW); t = lines.join('\n'); console.log('  OK 家族表加第 99 行') }
}

if (bad.length > 0) { for (const b of bad) console.log(b); process.exit(1) }
writeFileSync(I, t)
const m = /⇒ \S*套件合计 \*\*(\d+) 通过 \/ 0 失败\*\*/.exec(t)
const head = t.slice(0, m.index)
const block = head.slice(head.lastIndexOf('结束时的读数'))
let sum = 0
for (const x of block.matchAll(/\*\*(\d+)\/(\d+)\*\*/g)) sum += Number(x[1])
console.log(`  逐项求和 = ${sum}；声明 ${m[1]}  ${sum === Number(m[1]) ? 'OK 一致' : 'x 不一致'}`)
process.exit(sum === Number(m[1]) ? 0 : 1)
